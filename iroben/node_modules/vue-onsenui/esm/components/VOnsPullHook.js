import 'onsenui/esm/elements/ons-pull-hook.js';
import { deriveEvents } from '../mixins/index.js';
import { openBlock, createElementBlock, renderSlot } from 'vue';

const name = 'v-ons-pull-hook';

  var script = {
    name,
    mixins: [deriveEvents(name)],

    props: {
      action: {
        type: Function
      }
    }
  };

const _hoisted_1 = [".onAction"];

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return (openBlock(), createElementBlock("ons-pull-hook", { ".onAction": $props.action }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_1))
}

script.render = render;

export { script as default };
