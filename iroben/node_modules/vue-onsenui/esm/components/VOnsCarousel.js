import 'onsenui/esm/elements/ons-carousel.js';
import { deriveEvents } from '../mixins/index.js';
import { openBlock, createElementBlock, withModifiers, createElementVNode, renderSlot } from 'vue';

const name = 'v-ons-carousel';

  var script = {
    name,
    mixins: [deriveEvents(name)],
    emits: ['update:activeIndex'],
  };

const _hoisted_1 = /*#__PURE__*/createElementVNode("div", null, null, -1);

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return (openBlock(), createElementBlock("ons-carousel", {
    onPostchange: _cache[0] || (_cache[0] = withModifiers($event => (_ctx.$emit('update:activeIndex', $event.activeIndex)), ["self"]))
  }, [
    createElementVNode("div", null, [
      renderSlot(_ctx.$slots, "default")
    ]),
    _hoisted_1
  ], 32))
}

script.render = render;

export { script as default };
