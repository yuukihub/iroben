import {
  AnimatorFactory,
  DoorLock,
  ModifierUtil,
  PageLoader,
  animit_default,
  autostyle_default,
  contentReady,
  defaultPageLoader,
  device_back_button_dispatcher_default,
  elements_default,
  esm_default,
  gesture_detector_default,
  instantPageLoader,
  internal_default,
  orientation_default,
  platform_default,
  styler_default,
  util_default
} from "./chunk-DLGUDK6Q.js";
import {
  Fragment,
  createBaseVNode,
  createBlock,
  createElementBlock,
  createTextVNode,
  createVNode,
  guardReactiveProps,
  mergeProps,
  normalizeProps,
  normalizeStyle,
  openBlock,
  render,
  renderList,
  renderSlot,
  resolveComponent,
  resolveDynamicComponent,
  toDisplayString,
  vModelSelect,
  withDirectives,
  withModifiers
} from "./chunk-PBWN42R4.js";
import "./chunk-6NOISVLF.js";

// node_modules/onsenui/esm/elements/base/base-element.js
function getElementClass() {
  if (typeof HTMLElement !== "function") {
    const BaseElement2 = () => {
    };
    BaseElement2.prototype = document.createElement("div");
    return BaseElement2;
  } else {
    return HTMLElement;
  }
}
var BaseElement = class extends getElementClass() {
  constructor() {
    super();
  }
};

// node_modules/onsenui/esm/elements/ons-toolbar.js
var defaultClassName = "toolbar";
var scheme = {
  "": "toolbar--*",
  ".toolbar__left": "toolbar--*__left",
  ".toolbar__center": "toolbar--*__center",
  ".toolbar__right": "toolbar--*__right"
};
var ToolbarElement = class extends BaseElement {
  /**
   * @attribute inline
   * @initonly
   * @description
   *   [en]Display the toolbar as an inline element.[/en]
   *   [ja]ツールバーをインラインに置きます。スクロール領域内にそのまま表示されます。[/ja]
   */
  /**
   * @attribute static
   * @description
   *   [en]Static toolbars are not animated by `ons-navigator` when pushing or popping pages. This can be useful to improve performance in some situations.[/en]
   *   [ja][/ja]
   */
  /**
   * @property static
   * @type {Boolean}
   * @description
   *   [en]Static toolbars are not animated by `ons-navigator` when pushing or popping pages. This can be useful to improve performance in some situations.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute modifier
   * @description
   *   [en]The appearance of the toolbar.[/en]
   *   [ja]ツールバーの表現を指定します。[/ja]
   */
  /**
   * @property visible
   * @description
   *   [en]Whether the toolbar is shown or not.[/en]
   *   [ja][/ja]
   */
  constructor() {
    super();
    this._visible = true;
    contentReady(this, () => {
      this._compile();
    });
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName, scheme);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme);
        break;
    }
  }
  /**
   * @method setVisibility
   * @signature setVisibility(visible)
   * @param {Boolean} visible
   *   [en]Set to true to show the toolbar, false to hide it[/en]
   *   [ja][/ja]
   * @description
   *   [en]Shows the toolbar if visible is true, otherwise hides it.[/en]
   *   [ja][/ja]
   */
  setVisibility(visible) {
    contentReady(this, () => {
      this._visible = visible;
      this.style.display = visible ? "" : "none";
      if (this.parentNode) {
        const siblingBackground = util_default.findChild(this.parentNode, ".page__background");
        if (siblingBackground) {
          siblingBackground.style.top = visible ? null : 0;
        }
        const siblingContent = util_default.findChild(this.parentNode, ".page__content");
        if (siblingContent) {
          siblingContent.style.top = visible ? null : 0;
        }
      }
    });
  }
  /**
   * @method show
   * @signature show()
   * @description
   *   [en]Show the toolbar.[/en]
   *   [ja][/ja]
   */
  show() {
    this.setVisibility(true);
  }
  /**
   * @method hide
   * @signature hide()
   * @description
   *   [en]Hide the toolbar.[/en]
   *   [ja][/ja]
   */
  hide() {
    this.setVisibility(false);
  }
  get visible() {
    return this._visible;
  }
  set visible(value) {
    this.setVisibility(value);
  }
  /**
   * @return {HTMLElement}
   */
  _getToolbarLeftItemsElement() {
    return this.querySelector(".left") || internal_default.nullElement;
  }
  /**
   * @return {HTMLElement}
   */
  _getToolbarCenterItemsElement() {
    return this.querySelector(".center") || internal_default.nullElement;
  }
  /**
   * @return {HTMLElement}
   */
  _getToolbarRightItemsElement() {
    return this.querySelector(".right") || internal_default.nullElement;
  }
  /**
   * @return {HTMLElement}
   */
  _getToolbarBackButtonLabelElement() {
    return this.querySelector("ons-back-button .back-button__label") || internal_default.nullElement;
  }
  /**
   * @return {HTMLElement}
   */
  _getToolbarBackButtonIconElement() {
    return this.querySelector("ons-back-button .back-button__icon") || internal_default.nullElement;
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName);
    this._ensureToolbarItemElements();
    ModifierUtil.initModifier(this, scheme);
  }
  _ensureToolbarItemElements() {
    for (let i = this.childNodes.length - 1; i >= 0; i--) {
      if (this.childNodes[i].nodeType != 1) {
        this.removeChild(this.childNodes[i]);
      }
    }
    const center = this._ensureToolbarElement("center");
    center.classList.add("toolbar__title");
    if (this.children.length !== 1 || !this.children[0].classList.contains("center")) {
      const left = this._ensureToolbarElement("left");
      const right = this._ensureToolbarElement("right");
      if (this.children[0] !== left || this.children[1] !== center || this.children[2] !== right) {
        this.appendChild(left);
        this.appendChild(center);
        this.appendChild(right);
      }
    }
  }
  _ensureToolbarElement(name44) {
    if (util_default.findChild(this, ".toolbar__" + name44)) {
      const element2 = util_default.findChild(this, ".toolbar__" + name44);
      element2.classList.add(name44);
      return element2;
    }
    const element = util_default.findChild(this, "." + name44) || util_default.create("." + name44);
    element.classList.add("toolbar__" + name44);
    return element;
  }
};
util_default.defineBooleanProperties(ToolbarElement, ["static"]);
elements_default.Toolbar = ToolbarElement;
customElements.define("ons-toolbar", ToolbarElement);

// node_modules/vue-onsenui/esm/internal/util.js
var capitalize = (string) => string.charAt(0).toUpperCase() + string.slice(1);
var camelize = (string) => string.toLowerCase().replace(/-([a-z])/g, (m, l) => l.toUpperCase());
var eventToHandler = (name44) => "_on" + capitalize(name44);
var handlerToProp = (name44) => name44.slice(2).charAt(0).toLowerCase() + name44.slice(2).slice(1);

// node_modules/vue-onsenui/esm/mixins/derive.js
var _setupDBB = (component) => {
  const dbb = "onDeviceBackButton";
  const handler = component[dbb] || component.$el[dbb] && component.$el[dbb]._callback || ((e) => e.callParentHandler());
  component.$el[dbb] = (event) => {
    let runDefault = true;
    component.$emit(handlerToProp(dbb), {
      ...event,
      preventDefault: () => runDefault = false
    });
    runDefault && handler(event);
  };
  component._isDBBSetup = true;
};
var deriveDBB = {
  emits: ["deviceBackButton"],
  mounted() {
    _setupDBB(this);
  },
  // Core destroys deviceBackButton handlers on disconnectedCallback.
  // This fixes the behavior for <keep-alive> component.
  activated() {
    this._isDBBSetup === false && _setupDBB(this);
  },
  deactivated() {
    this._isDBBSetup === true && (this._isDBBSetup = false);
  },
  destroyed() {
    this.$el.onDeviceBackButton && this.$el.onDeviceBackButton.destroy();
  }
};
var deriveEvents = (name44) => {
  const nativeEvents = esm_default.elements[capitalize(camelize(name44.slice(6)))].events || [];
  return {
    emits: nativeEvents,
    mounted() {
      this._handlers = {};
      nativeEvents.forEach((key) => {
        this._handlers[eventToHandler(key)] = (event) => {
          if (event.target === this.$el || !/^ons-/i.test(event.target.tagName)) {
            this.$emit(key, event);
          }
        };
        this.$el.addEventListener(key, this._handlers[eventToHandler(key)]);
      });
    },
    beforeDestroy() {
      Object.keys(this._handlers).forEach((key) => {
        this.$el.removeEventListener(key, this._handlers[key]);
      });
      this._handlers = null;
    }
  };
};
var unrecognizedListeners = (nativeElement) => ({
  computed: {
    unrecognizedListeners() {
      const isListener = ([attribute]) => /^on[^a-z]/.test(attribute);
      const isUnknown = ([attribute]) => !nativeElement.events.includes(camelize(attribute.slice(2)));
      return Object.fromEntries(Object.entries(this.$attrs).filter((attribute) => isListener(attribute) && isUnknown(attribute)));
    }
  }
});

// node_modules/vue-onsenui/esm/mixins/common.js
var _toggleVisibility = function() {
  if (typeof this.visible === "boolean" && this.visible !== this.$el.visible) {
    this.$el[this.visible ? "show" : "hide"].call(this.$el, this.normalizedOptions || this.options);
  }
};
var _teleport = function() {
  if (!this._isDestroyed && (!this.$el.parentNode || this.$el.parentNode !== document.body)) {
    document.body.appendChild(this.$el);
  }
};
var _unmount = function() {
  if (this.$el.visible === true) {
    this.$el.hide().then(() => this.$el.remove());
  } else {
    this.$el.remove();
  }
};
var hidable = {
  props: {
    visible: {
      type: Boolean,
      default: void 0
      // Avoid casting to false
    }
  },
  watch: {
    visible() {
      _toggleVisibility.call(this);
    }
  },
  mounted() {
    this.$nextTick(() => _toggleVisibility.call(this));
  },
  activated() {
    this.$nextTick(() => _toggleVisibility.call(this));
  }
};
var hasOptions = {
  props: {
    options: {
      type: Object,
      default() {
        return {};
      }
    }
  }
};
var selfProvider = {
  provide() {
    return {
      [this.$options.name.slice(6)]: this
    };
  }
};
var portal = {
  mounted() {
    _teleport.call(this);
  },
  updated() {
    _teleport.call(this);
  },
  activated() {
    _teleport.call(this);
  },
  deactivated() {
    _unmount.call(this);
  },
  beforeDestroy() {
    _unmount.call(this);
  }
};
var modifier = {
  props: {
    modifier: {
      type: [String, Array, Object]
    }
  },
  computed: {
    normalizedModifier() {
      const modifier2 = this.modifier;
      if (typeof modifier2 === "string") {
        return modifier2;
      }
      if (Array.isArray(modifier2)) {
        return modifier2.join(" ");
      }
      if (typeof modifier2 === "object") {
        return Object.keys(modifier2).reduce((acc, key) => acc + (modifier2[key] ? ` ${key}` : ""), "").trim();
      }
      return null;
    }
  }
};

// node_modules/vue-onsenui/esm/mixins/model.js
var modelInput = {
  emits: ["update:modelValue"],
  props: {
    modelValue: [Number, String],
    modelEvent: {
      type: String,
      default: "input"
    }
  },
  methods: {
    _updateValue() {
      if (this.modelValue !== void 0 && this.$el.value !== this.modelValue) {
        this.$el.value = this.modelValue;
      }
    },
    _onModelEvent(event) {
      this.$emit("update:modelValue", event.target.value);
    }
  },
  watch: {
    modelValue() {
      this._updateValue();
    }
  },
  mounted() {
    this._updateValue();
    this.$el.addEventListener(this.modelEvent, this._onModelEvent);
  },
  beforeDestroy() {
    this.$el.removeEventListener(this.modelEvent, this._onModelEvent);
  }
};
var modelInputNumber = {
  mixins: [modelInput],
  methods: {
    _onModelEvent(event) {
      this.$emit("update:modelValue", event.target.valueAsNumber);
    }
  }
};
var modelCheckbox = {
  mixins: [modelInput],
  props: {
    modelValue: [Array, Boolean],
    modelEvent: {
      type: String,
      default: "change"
    }
  },
  methods: {
    _updateValue() {
      if (this.modelValue instanceof Array) {
        this.$el.checked = this.modelValue.indexOf(this.$el.value) >= 0;
      } else {
        this.$el.checked = this.modelValue;
      }
    },
    _onModelEvent(event) {
      const { value, checked } = event.target;
      let newValue;
      if (this.modelValue instanceof Array) {
        const index = this.modelValue.indexOf(value);
        const included = index >= 0;
        if (included && !checked) {
          newValue = [
            ...this.modelValue.slice(0, index),
            ...this.modelValue.slice(index + 1, this.modelValue.length)
          ];
        }
        if (!included && checked) {
          newValue = [...this.modelValue, value];
        }
      } else {
        newValue = checked;
      }
      newValue !== void 0 && this.$emit("update:modelValue", newValue);
    }
  }
};
var modelRadio = {
  mixins: [modelInput],
  props: {
    modelEvent: {
      type: String,
      default: "change"
    }
  },
  methods: {
    _updateValue() {
      this.$el.checked = this.modelValue === this.$el.value;
    },
    _onModelEvent(event) {
      const { value, checked } = event.target;
      checked && this.$emit("update:modelValue", value);
    }
  }
};

// node_modules/vue-onsenui/esm/mixins/dialog.js
var dialogCancel = {
  emits: ["update:visible"],
  mounted() {
    this._dialogCancelHandler = () => this.$emit("update:visible", false);
    this.$el.addEventListener("dialogcancel", this._dialogCancelHandler);
  },
  beforeDestroy() {
    this.$el.removeEventListener("dialogcancel", this._dialogCancelHandler);
  }
};
var dialogOrderedProps = {
  inheritAttrs: false,
  computed: {
    orderedProps() {
      const { visible, ...rest } = this.$attrs;
      return { ...rest, visible };
    }
  }
};
var dialog = {
  mixins: [dialogCancel, dialogOrderedProps]
};

// node_modules/vue-onsenui/esm/components/VOnsToolbar.js
var name = "v-ons-toolbar";
var script = {
  name,
  mixins: [deriveEvents(name), modifier],
  props: {
    inline: Boolean
  }
};
var _hoisted_1 = ["modifier", "inline"];
function render2(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-toolbar", {
    modifier: _ctx.normalizedModifier,
    inline: $props.inline || null
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_1);
}
script.render = render2;

// node_modules/onsenui/esm/elements/ons-bottom-toolbar.js
var defaultClassName2 = "bottom-bar";
var scheme2 = { "": "bottom-bar--*" };
var BottomToolbarElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the toolbar.[/en]
   *   [ja]ツールバーの見た目の表現を指定します。[/ja]
   */
  constructor() {
    super();
    this.classList.add(defaultClassName2);
    ModifierUtil.initModifier(this, scheme2);
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName2, scheme2);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme2);
        break;
    }
  }
};
elements_default.BottomToolbar = BottomToolbarElement;
customElements.define("ons-bottom-toolbar", BottomToolbarElement);

// node_modules/vue-onsenui/esm/components/VOnsBottomToolbar.js
var name2 = "v-ons-bottom-toolbar";
var script2 = {
  name: name2,
  mixins: [deriveEvents(name2), modifier]
};
var _hoisted_12 = ["modifier"];
function render3(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-bottom-toolbar", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_12);
}
script2.render = render3;

// node_modules/onsenui/esm/elements/ons-ripple/animator-css.js
var AnimatorCSS = class {
  /**
   * @method animate
   * @desc main animation function
   * @param {Element} element
   * @param {Object} finalCSS
   * @param {number} [duration=200] - duration in milliseconds
   * @return {Object} result
   * @return {Function} result.then(callback) - sets a callback to be executed after the animation has stopped
   * @return {Function} result.stop(options) - stops the animation; if options.stopNext is true then it doesn't call the callback
   * @return {Function} result.finish(ms) - finishes the animation in the specified time in milliseconds
   * @return {Function} result.speed(ms) - sets the animation speed so that it finishes as if the original duration was the one specified here
   * @example
   * ````
   *  var result = animator.animate(el, {opacity: 0.5}, 1000);
   *
   *  el.addEventListener('click', function(e){
   *    result.speed(200).then(function(){
   *      console.log('done');
   *    });
   *  }, 300);
   * ````
   */
  animate(el, final, duration = 200) {
    var start = (/* @__PURE__ */ new Date()).getTime(), initial = {}, stopped = false, next = false, timeout = false, properties = Object.keys(final);
    var updateStyles = () => {
      let s = window.getComputedStyle(el);
      properties.forEach(s.getPropertyValue.bind(s));
      s = el.offsetHeight;
    };
    var result = {
      stop: (options = {}) => {
        timeout && clearTimeout(timeout);
        var k = Math.min(1, ((/* @__PURE__ */ new Date()).getTime() - start) / duration);
        properties.forEach((i) => {
          el.style[i] = (1 - k) * initial[i] + k * final[i] + (i == "opacity" ? "" : "px");
        });
        el.style.transitionDuration = "0s";
        if (options.stopNext) {
          next = false;
        } else if (!stopped) {
          stopped = true;
          next && next();
        }
        return result;
      },
      then: (cb) => {
        next = cb;
        if (stopped) {
          next && next();
        }
        return result;
      },
      speed: (newDuration) => {
        if (internal_default.config.animationsDisabled) {
          newDuration = 0;
        }
        if (!stopped) {
          timeout && clearTimeout(timeout);
          const passed = (/* @__PURE__ */ new Date()).getTime() - start;
          const k = passed / duration;
          const remaining = newDuration * (1 - k);
          properties.forEach((i) => {
            el.style[i] = (1 - k) * initial[i] + k * final[i] + (i == "opacity" ? "" : "px");
          });
          updateStyles();
          start = el.speedUpTime;
          duration = remaining;
          el.style.transitionDuration = duration / 1e3 + "s";
          properties.forEach((i) => {
            el.style[i] = final[i] + (i == "opacity" ? "" : "px");
          });
          timeout = setTimeout(result.stop, remaining);
        }
        return result;
      },
      finish: (milliseconds = 50) => {
        var k = ((/* @__PURE__ */ new Date()).getTime() - start) / duration;
        result.speed(milliseconds / (1 - k));
        return result;
      }
    };
    if (el.hasAttribute("disabled") || stopped || internal_default.config.animationsDisabled) {
      return result;
    }
    var style = window.getComputedStyle(el);
    properties.forEach((e) => {
      const v = parseFloat(style.getPropertyValue(e));
      initial[e] = isNaN(v) ? 0 : v;
    });
    if (!stopped) {
      el.style.transitionProperty = properties.join(",");
      el.style.transitionDuration = duration / 1e3 + "s";
      properties.forEach((e) => {
        el.style[e] = final[e] + (e == "opacity" ? "" : "px");
      });
    }
    timeout = setTimeout(result.stop, duration);
    this._onStopAnimations(el, result.stop);
    return result;
  }
  constructor() {
    this._queue = [];
    this._index = 0;
  }
  _onStopAnimations(el, listener) {
    var queue = this._queue;
    var i = this._index++;
    queue[el] = queue[el] || [];
    queue[el][i] = (options) => {
      delete queue[el][i];
      if (queue[el] && queue[el].length == 0) {
        delete queue[el];
      }
      return listener(options);
    };
  }
  /**
  * @method stopAnimations
  * @desc stops active animations on a specified element
  * @param {Element|Array} element - element or array of elements
  * @param {Object} [options={}]
  * @param {Boolean} [options.stopNext] - the callbacks after the animations won't be called if this option is true
  */
  stopAnimations(el, options = {}) {
    if (Array.isArray(el)) {
      return el.forEach((el2) => {
        this.stopAnimations(el2, options);
      });
    }
    (this._queue[el] || []).forEach((e) => {
      e(options || {});
    });
  }
  /**
  * @method stopAll
  * @desc stops all active animations
  * @param {Object} [options={}]
  * @param {Boolean} [options.stopNext] - the callbacks after the animations won't be called if this option is true
  */
  stopAll(options = {}) {
    this.stopAnimations(Object.keys(this._queue), options);
  }
  /**
  * @method fade
  * @desc fades the element (short version for animate(el, {opacity: 0}))
  * @param {Element} element
  * @param {number} [duration=200]
  */
  fade(el, duration = 200) {
    return this.animate(el, { opacity: 0 }, duration);
  }
};
var animator_css_default = AnimatorCSS;

// node_modules/onsenui/esm/elements/ons-ripple.js
var defaultClassName3 = "ripple";
var scheme3 = {
  "": "ripple--*",
  ".ripple__wave": "ripple--*__wave",
  ".ripple__background": "ripple--*__background"
};
var RippleElement = class extends BaseElement {
  /**
   * @attribute color
   * @type {String}
   * @description
   *   [en]Color of the ripple effect.[/en]
   *   [ja]リップルエフェクトの色を指定します。[/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the ripple effect.[/en]
   *   [ja]エフェクトの表現を指定します。[/ja]
   */
  /**
   * @attribute background
   * @type {String}
   * @description
   *   [en]Color of the background.[/en]
   *   [ja]背景の色を設定します。[/ja]
   */
  /**
   * @attribute size
   * @type {String}
   * @description
   *   [en]Sizing of the wave on ripple effect. Set "cover" or "contain". Default is "cover".[/en]
   *   [ja]エフェクトのサイズを指定します。"cover"もしくは"contain"を指定します。デフォルトは"cover"です。[/ja]
   */
  /**
   * @attribute center
   * @type {Boolean}
   * @description
   *   [en]If this attribute presents, change the position of wave effect to center of the target element.[/en]
   *   [ja]この要素を設定すると、エフェクトの位置が要素の真ん中から始まります。[/ja]
   */
  /**
   * @property center
   * @type {Boolean}
   * @description
   *   [en]If this attribute presents, change the position of wave effect to center of the target element.[/en]
   *   [ja]この要素を設定すると、エフェクトの位置が要素の真ん中から始まります。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]If this attribute is set, the ripple effect will be disabled.[/en]
   *   [ja]この属性が設定された場合、リップルエフェクトは無効になります。[/ja]
   */
  constructor() {
    super();
    this._onTap = this._onTap.bind(this);
    this._onHold = this._onHold.bind(this);
    this._onDragStart = this._onDragStart.bind(this);
    this._onRelease = this._onRelease.bind(this);
    contentReady(this, () => this._compile());
    this._animator = new animator_css_default();
    ["color", "center", "start-radius", "background", "modifier"].forEach((e) => {
      this.attributeChangedCallback(e, null, this.getAttribute(e));
    });
  }
  _compile() {
    this.classList.add(defaultClassName3);
    this._wave = this.getElementsByClassName("ripple__wave")[0];
    this._background = this.getElementsByClassName("ripple__background")[0];
    if (!(this._background && this._wave)) {
      this._wave = util_default.create(".ripple__wave");
      this._background = util_default.create(".ripple__background");
      this.appendChild(this._wave);
      this.appendChild(this._background);
    }
    ModifierUtil.initModifier(this, scheme3);
  }
  _getEffectSize() {
    const sizes = ["cover", "contain"];
    if (this.hasAttribute("size")) {
      const size = this.getAttribute("size");
      if (sizes.indexOf(size) !== -1) {
        return size;
      }
    }
    return "cover";
  }
  _calculateCoords(e) {
    let x, y, h, w, r;
    const b = this.getBoundingClientRect();
    const size = this._getEffectSize();
    const error = () => util_default.throw("Ripple invalid state");
    if (this._center) {
      x = b.width / 2;
      y = b.height / 2;
      if (size === "cover") {
        r = Math.sqrt(x * x + y * y);
      } else if (size === "contain") {
        r = Math.min(x, y);
      } else {
        error();
      }
    } else {
      x = (typeof e.clientX === "number" ? e.clientX : e.changedTouches[0].clientX) - b.left;
      y = (typeof e.clientY === "number" ? e.clientY : e.changedTouches[0].clientY) - b.top;
      h = Math.max(y, b.height - y);
      w = Math.max(x, b.width - x);
      if (size === "cover") {
        r = Math.sqrt(h * h + w * w);
      } else if (size === "contain") {
        r = Math.min(Math.round(h / 2), Math.round(w / 2));
      } else {
        error();
      }
    }
    return { x, y, r };
  }
  _rippleAnimation(e, duration = 300) {
    const { _animator, _wave, _background, _minR } = this;
    const { x, y, r } = this._calculateCoords(e);
    _animator.stopAll({ stopNext: 1 });
    _animator.animate(_background, { opacity: 1 }, duration);
    util_default.extend(_wave.style, {
      opacity: 1,
      top: y - _minR + "px",
      left: x - _minR + "px",
      width: 2 * _minR + "px",
      height: 2 * _minR + "px"
    });
    return _animator.animate(_wave, {
      top: y - r,
      left: x - r,
      height: 2 * r,
      width: 2 * r
    }, duration);
  }
  _updateParent() {
    if (!this._parentUpdated && this.parentNode) {
      const computedStyle = window.getComputedStyle(this.parentNode);
      if (computedStyle.getPropertyValue("position") === "static") {
        this.parentNode.style.position = "relative";
      }
      this._parentUpdated = true;
    }
  }
  _onTap(e) {
    if (!this.disabled && !e.ripple) {
      e.ripple = true;
      this._updateParent();
      this._rippleAnimation(e.gesture.srcEvent).then(() => {
        this._animator.fade(this._wave);
        this._animator.fade(this._background);
      });
    }
  }
  _onHold(e) {
    if (!this.disabled && !e.ripple) {
      e.ripple = true;
      this._updateParent();
      this._holding = this._rippleAnimation(e.gesture.srcEvent, 2e3);
      document.addEventListener("release", this._onRelease);
    }
  }
  _onRelease(e) {
    if (this._holding && !e.ripple) {
      e.ripple = true;
      this._holding.speed(300).then(() => {
        this._animator.stopAll({ stopNext: true });
        this._animator.fade(this._wave);
        this._animator.fade(this._background);
      });
      this._holding = false;
    }
    document.removeEventListener("release", this._onRelease);
  }
  _onDragStart(e) {
    if (this._holding) {
      return this._onRelease(e);
    }
    if (["left", "right"].indexOf(e.gesture.direction) != -1) {
      this._onTap(e);
    }
  }
  connectedCallback() {
    this._parentNode = this.parentNode;
    if (internal_default.config.animationsDisabled) {
      this.disabled = true;
    } else {
      this._parentNode.addEventListener("tap", this._onTap);
      this._parentNode.addEventListener("hold", this._onHold);
      this._parentNode.addEventListener("dragstart", this._onDragStart);
    }
  }
  disconnectedCallback() {
    const pn = this._parentNode || this.parentNode;
    pn.removeEventListener("tap", this._onTap);
    pn.removeEventListener("hold", this._onHold);
    pn.removeEventListener("dragstart", this._onDragStart);
  }
  static get observedAttributes() {
    return ["start-radius", "color", "background", "center", "class", "modifier"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName3, scheme3);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme3);
        break;
      case "start-radius":
        this._minR = Math.max(0, parseFloat(current) || 0);
        break;
      case "color":
        if (current) {
          contentReady(this, () => {
            this._wave.style.background = current;
            if (!this.hasAttribute("background")) {
              this._background.style.background = current;
            }
          });
        }
        break;
      case "background":
        if (current || last) {
          if (current === "none") {
            contentReady(this, () => {
              this._background.setAttribute("disabled", "disabled");
              this._background.style.background = "transparent";
            });
          } else {
            contentReady(this, () => {
              if (this._background.hasAttribute("disabled")) {
                this._background.removeAttribute("disabled");
              }
              this._background.style.background = current;
            });
          }
        }
        break;
      case "center":
        if (name44 === "center") {
          this._center = current != null && current != "false";
        }
        break;
    }
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
};
util_default.defineBooleanProperties(RippleElement, ["disabled", "center"]);
elements_default.Ripple = RippleElement;
customElements.define("ons-ripple", RippleElement);

// node_modules/onsenui/esm/elements/base/base-button.js
var BaseButtonElement = class _BaseButtonElement extends BaseElement {
  get _scheme() {
    util_default.throwMember();
  }
  get _defaultClassName() {
    util_default.throwMember();
  }
  get _rippleOpt() {
    return [this];
  }
  constructor() {
    super();
    if (this.constructor === _BaseButtonElement) {
      util_default.throwAbstract();
    }
    contentReady(this, () => this._compile());
  }
  get _icon() {
    return util_default.findChild(this, "ons-icon");
  }
  get _hiddenButton() {
    return util_default.findChild(this, "button");
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(this._defaultClassName);
    if (!this._icon && this.hasAttribute("icon")) {
      util_default.checkMissingImport("Icon");
      const icon = util_default.createElement(`<ons-icon icon="${this.getAttribute("icon")}"></ons-icon>`);
      icon.classList.add(this._defaultClassName.replace("button", "icon"));
      this.insertBefore(icon, this.firstChild);
    }
    if (!this._hiddenButton) {
      const button = util_default.createElement("<button hidden></button>");
      this.appendChild(button);
    }
    this._updateRipple();
    ModifierUtil.initModifier(this, this._scheme);
  }
  _updateIcon() {
    if (this._icon) {
      this._icon.setAttribute("icon", this.getAttribute("icon"));
    }
  }
  _updateRipple() {
    this._rippleOpt && util_default.updateRipple(...this._rippleOpt);
  }
  static get observedAttributes() {
    return ["modifier", "class", "icon", "ripple"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, this._defaultClassName, this._scheme);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, this._scheme);
        break;
      case "icon":
        this._updateIcon();
        break;
      case "ripple":
        this.classList.contains(this._defaultClassName) && this._updateRipple();
        break;
    }
  }
};
util_default.defineBooleanProperties(BaseButtonElement, ["ripple", "disabled"]);

// node_modules/onsenui/esm/elements/ons-toolbar-button.js
var ToolbarButtonElement = class extends BaseButtonElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the button.[/en]
   *   [ja]ボタンの表現を指定します。[/ja]
   */
  /**
   * @attribute icon
   * @type {String}
   * @description
   *  [en]Creates an `ons-icon` component with this string.[/en]
   *  [ja]`ons-icon`コンポーネントを悪性します。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if button should be disabled.[/en]
   *   [ja]ボタンを無効化する場合は指定してください。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  get _scheme() {
    return { "": "toolbar-button--*" };
  }
  get _defaultClassName() {
    return "toolbar-button";
  }
  get _rippleOpt() {
    return [this, void 0, { center: "", "size": "contain", "background": "transparent" }];
  }
};
elements_default.ToolbarButton = ToolbarButtonElement;
customElements.define("ons-toolbar-button", ToolbarButtonElement);

// node_modules/vue-onsenui/esm/components/VOnsToolbarButton.js
var name3 = "v-ons-toolbar-button";
var script3 = {
  name: name3,
  mixins: [deriveEvents(name3), modifier]
};
var _hoisted_13 = ["modifier"];
function render4(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-toolbar-button", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_13);
}
script3.render = render4;

// node_modules/onsenui/esm/elements/ons-alert-dialog-button.js
var AlertDialogButtonElement = class extends BaseButtonElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the button.[/en]
   *   [ja]ボタンの表現を指定します。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if button should be disabled.[/en]
   *   [ja]ボタンを無効化する場合は指定してください。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  get _scheme() {
    return { "": "alert-dialog-button--*" };
  }
  get _defaultClassName() {
    return "alert-dialog-button";
  }
  get _rippleOpt() {
    return [this, void 0, { "modifier": "light-gray" }];
  }
};
elements_default.AlertDialogButton = AlertDialogButtonElement;
customElements.define("ons-alert-dialog-button", AlertDialogButtonElement);

// node_modules/vue-onsenui/esm/components/VOnsAlertDialogButton.js
var name4 = "v-ons-alert-dialog-button";
var script4 = {
  name: name4,
  mixins: [deriveEvents(name4), modifier]
};
var _hoisted_14 = ["modifier"];
function render5(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-alert-dialog-button", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_14);
}
script4.render = render5;

// node_modules/onsenui/esm/elements/ons-button.js
var ButtonElement = class extends BaseButtonElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the button.[/en]
   *  [ja]ボタンの表現を指定します。[/ja]
   */
  /**
   * @attribute ripple
   * @description
   *  [en]If this attribute is defined, the button will have a ripple effect.[/en]
   *  [ja][/ja]
   */
  /**
   * @property ripple
   * @type {Boolean}
   * @description
   *  [en]Whether the button has a ripple effect or not.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if button should be disabled.[/en]
   *   [ja]ボタンを無効化する場合は指定します。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the button is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  get _scheme() {
    return { "": "button--*" };
  }
  get _defaultClassName() {
    return "button";
  }
};
elements_default.Button = ButtonElement;
customElements.define("ons-button", ButtonElement);

// node_modules/vue-onsenui/esm/components/VOnsButton.js
var name5 = "v-ons-button";
var script5 = {
  name: name5,
  mixins: [deriveEvents(name5), modifier]
};
var _hoisted_15 = ["modifier"];
function render6(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-button", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_15);
}
script5.render = render6;

// node_modules/onsenui/esm/elements/ons-icon.js
var autoPrefix = "fa";
var IconElement = class extends BaseElement {
  /**
   * @attribute icon
   * @type {String}
   * @description
   *   [en]
   *     The icon name. `"md-"` prefix for Material Icons, `"fa-"` for Font Awesome and `"ion-"` prefix for Ionicons.
   *
   *     See all available icons on the element description (at the top).
   *
   *     Icons can also be styled based on modifier presence. Add comma-separated icons with `"modifierName:"` prefix.
   *
   *     The code `<ons-icon icon="ion-edit, material:md-edit"></ons-icon>` will display `"md-edit"` for Material Design and `"ion-edit"` as the default icon.
   *
   *     `fa-` prefix is added automatically if none is provided. Check [See also](#seealso) section for more information.
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @attribute size
   * @type {String}
   * @description
   *   [en]
   *     The sizes of the icon. Valid values are lg, 2x, 3x, 4x, 5x, or in the size in pixels.
   *     Icons can also be styled based on modifier presence. Add comma-separated icons with `"modifierName:"` prefix.
   *
   *     The code:
   *
   *     ```
   *     <ons-icon
   *       icon="ion-edit"
   *       size="32px, material:24px">
   *     </ons-icon>
   *     ```
   *
   *     will render as a `24px` icon if the `"material"` modifier is present and `32px` otherwise.
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @attribute rotate
   * @type {Number}
   * @description
   *   [en]Number of degrees to rotate the icon. Valid values are 90, 180 and 270.[/en]
   *   [ja]アイコンを回転して表示します。90, 180, 270から指定できます。[/ja]
   */
  /**
   * @attribute fixed-width
   * @type {Boolean}
   * @default false
   * @description
   *  [en]When used in a list, you want the icons to have the same width so that they align vertically by defining this attribute.[/en]
   *  [ja][/ja]
   */
  /**
   * @property fixedWidth
   * @type {Boolean}
   * @default false
   * @description
   *  [en]When used in a list, you want the icons to have the same width so that they align vertically by defining this attribute.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute spin
   * @description
   *   [en]Specify whether the icon should be spinning.[/en]
   *   [ja]アイコンを回転するかどうかを指定します。[/ja]
   */
  /**
   * @property spin
   * @type {Boolean}
   * @description
   *   [en]Specify whether the icon should be spinning.[/en]
   *   [ja]アイコンを回転するかどうかを指定します。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => {
      this._compile();
    });
  }
  static get observedAttributes() {
    return ["icon", "size", "modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    this._cleanClassAttribute(name44 === "icon" ? last : this.getAttribute("icon"), name44 === "modifier" ? last : void 0);
    this._update();
  }
  _compile() {
    autostyle_default.prepare(this);
    this._update();
  }
  _update() {
    const { classList, style } = this._buildClassAndStyle(this._parseAttr("icon"), this._parseAttr("size"));
    util_default.extend(this.style, style);
    classList.forEach((className) => this.classList.add(className));
  }
  _parseAttr(attrName, modifier2 = this.getAttribute("modifier") || "") {
    const attr = this.getAttribute(attrName) || attrName || "";
    const parts = attr.split(/\s*,\s*/);
    const def = parts[0];
    let md = parts[1];
    md = (md || "").split(/\s*:\s*/);
    return (modifier2 && RegExp(`(^|\\s+)${md[0]}($|\\s+)`, "i").test(modifier2) ? md[1] : def) || "";
  }
  /**
   * Remove unneeded class value.
   */
  _cleanClassAttribute(lastIcon, lastModifier) {
    const { className, prefix } = this._prefixIcon(this._parseAttr(lastIcon, lastModifier));
    const customPrefixRE = className !== prefix ? `|${prefix}$|${prefix}-` : `|${className}$` || "";
    const re = new RegExp(`^(fa$|fa-|ion-|zmdi$|zmdi-|ons-icon--${customPrefixRE})`);
    util_default.arrayFrom(this.classList).filter((className2) => re.test(className2)).forEach((className2) => this.classList.remove(className2));
  }
  _prefixIcon(iconName) {
    const className = autoPrefix + (autoPrefix ? "-" : "") + iconName;
    return { className, prefix: className.split("-")[0] };
  }
  _buildClassAndStyle(iconName, size) {
    const classList = ["ons-icon"];
    const style = {};
    if (iconName.indexOf("ion-") === 0) {
      classList.push(iconName);
      classList.push("ons-icon--ion");
    } else if (iconName.indexOf("fa-") === 0) {
      classList.push(iconName);
      if (!(this.classList.contains("far") || this.classList.contains("fab") || this.classList.contains("fal"))) {
        classList.push("fa");
      }
    } else if (iconName.indexOf("md-") === 0) {
      classList.push("zmdi");
      classList.push("zmdi-" + iconName.split(/-(.+)?/)[1]);
    } else {
      const { className, prefix } = this._prefixIcon(iconName);
      prefix && classList.push(prefix);
      className && classList.push(className);
    }
    if (size.match(/^[1-5]x|lg$/)) {
      classList.push("ons-icon--" + size);
      this.style.removeProperty("font-size");
    } else {
      style.fontSize = size;
    }
    return {
      classList,
      style
    };
  }
  static setAutoPrefix(prefix) {
    autoPrefix = prefix ? typeof prefix === "string" && prefix || "fa" : "";
  }
};
util_default.defineBooleanProperties(IconElement, ["fixed-width", "spin"]);
elements_default.Icon = IconElement;
customElements.define("ons-icon", IconElement);

// node_modules/vue-onsenui/esm/components/VOnsIcon.js
var name6 = "v-ons-icon";
var script6 = {
  name: name6,
  mixins: [deriveEvents(name6), modifier]
};
var _hoisted_16 = ["modifier"];
function render7(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-icon", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_16);
}
script6.render = render7;

// node_modules/onsenui/esm/elements/ons-card.js
var defaultClassName4 = "card";
var scheme4 = {
  "": "card--*",
  ".card__title": "card--*__title",
  ".card__content": "card--*__content"
};
var CardElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the card.[/en]
   *   [ja]リストの表現を指定します。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => {
      this._compile();
    });
  }
  _compile() {
    let title, content;
    for (let i = 0; i < this.children.length; i++) {
      const el = this.children[i];
      if (el.classList.contains("title")) {
        el.classList.add("card__title");
        title = el;
      } else if (el.classList.contains("content")) {
        el.classList.add("card__content");
        content = el;
      }
    }
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName4);
    ModifierUtil.initModifier(this, scheme4);
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName4, scheme4);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme4);
        break;
    }
  }
};
elements_default.Card = CardElement;
customElements.define("ons-card", CardElement);

// node_modules/vue-onsenui/esm/components/VOnsCard.js
var name7 = "v-ons-card";
var script7 = {
  name: name7,
  mixins: [deriveEvents(name7), modifier]
};
var _hoisted_17 = ["modifier"];
function render8(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-card", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_17);
}
script7.render = render8;

// node_modules/onsenui/esm/elements/ons-list.js
var defaultClassName5 = "list";
var scheme5 = { "": "list--*" };
var ListElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the list.[/en]
   *   [ja]リストの表現を指定します。[/ja]
   */
  constructor() {
    super();
    this._compile();
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName5);
    ModifierUtil.initModifier(this, scheme5);
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName5, scheme5);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme5);
        break;
    }
  }
};
elements_default.List = ListElement;
customElements.define("ons-list", ListElement);

// node_modules/vue-onsenui/esm/components/VOnsList.js
var name8 = "v-ons-list";
var script8 = {
  name: name8,
  mixins: [deriveEvents(name8), modifier]
};
var _hoisted_18 = ["modifier"];
function render9(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-list", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_18);
}
script8.render = render9;

// node_modules/onsenui/esm/ons/base-animator.js
var BaseAnimator = class {
  /**
   * @param {Object} options
   * @param {String} options.timing
   * @param {Number} options.duration
   * @param {Number} options.delay
   */
  constructor(options = {}) {
    this.timing = options.timing || "linear";
    this.duration = options.duration || 0;
    this.delay = options.delay || 0;
    this.def = {
      timing: this.timing,
      duration: this.duration,
      delay: this.delay
    };
  }
  static extend(properties = {}) {
    const extendedAnimator = this;
    const newAnimator = function() {
      extendedAnimator.apply(this, arguments);
      util_default.extend(this, properties);
    };
    newAnimator.prototype = this.prototype;
    return newAnimator;
  }
};

// node_modules/onsenui/esm/elements/ons-list-item/animator.js
var ListItemAnimator = class extends BaseAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  showExpansion(listItem, callback) {
    callback();
  }
  hideExpansion(listItem, callback) {
    callback();
  }
};
var SlideListItemAnimator = class extends ListItemAnimator {
  showExpansion(listItem, callback) {
    this._animateExpansion(listItem, true, callback);
  }
  hideExpansion(listItem, callback) {
    this._animateExpansion(listItem, false, callback);
  }
  _animateExpansion(listItem, shouldOpen, callback) {
    const oldHeight = listItem.expandableContent.style.height;
    const oldDisplay = listItem.expandableContent.style.display;
    listItem.expandableContent.style.height = "auto";
    listItem.expandableContent.style.display = "block";
    const computedStyle = window.getComputedStyle(listItem.expandableContent);
    const expansionOpenTransition = [
      { height: 0, paddingTop: 0, paddingBottom: 0 },
      {
        height: computedStyle.height,
        paddingTop: computedStyle.paddingTop,
        paddingBottom: computedStyle.paddingBottom
      }
    ];
    const iconOpenTransition = [{ transform: "rotate(45deg)" }, { transform: "rotate(225deg)" }];
    listItem.expandableContent.style.height = oldHeight;
    animit_default(listItem.expandableContent, { duration: this.duration, property: "height padding-top padding-bottom" }).default(...shouldOpen ? expansionOpenTransition : expansionOpenTransition.reverse()).play(() => {
      listItem.expandableContent.style.display = oldDisplay;
      callback && callback();
    });
    if (listItem.expandChevron) {
      animit_default(listItem.expandChevron, { duration: this.duration, property: "transform" }).default(...shouldOpen ? iconOpenTransition : iconOpenTransition.reverse()).play();
    }
  }
};

// node_modules/onsenui/esm/elements/ons-list-item.js
var defaultClassName6 = "list-item";
var scheme6 = {
  ".list-item": "list-item--*",
  ".list-item__left": "list-item--*__left",
  ".list-item__center": "list-item--*__center",
  ".list-item__right": "list-item--*__right",
  ".list-item__label": "list-item--*__label",
  ".list-item__title": "list-item--*__title",
  ".list-item__subtitle": "list-item--*__subtitle",
  ".list-item__thumbnail": "list-item--*__thumbnail",
  ".list-item__icon": "list-item--*__icon"
};
var _animatorDict = {
  "default": SlideListItemAnimator,
  "none": ListItemAnimator
};
var ListItemElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the list item.[/en]
   *   [ja]各要素の表現を指定します。[/ja]
   */
  /**
   * @attribute lock-on-drag
   * @type {Boolean}
   * @description
   *   [en]Prevent vertical scrolling when the user drags horizontally.[/en]
   *   [ja]この属性があると、ユーザーがこの要素を横方向にドラッグしている時に、縦方向のスクロールが起きないようになります。[/ja]
   */
  /**
   * @property lockOnDrag
   * @type {Boolean}
   * @description
   *   [en]Prevent vertical scrolling when the user drags horizontally.[/en]
   *   [ja]この属性があると、ユーザーがこの要素を横方向にドラッグしている時に、縦方向のスクロールが起きないようになります。[/ja]
   */
  /**
   * @attribute tappable
   * @type {Boolean}
   * @description
   *   [en]Makes the element react to taps. `prevent-tap` attribute can be added to child elements like buttons or inputs to prevent this effect. `ons-*` elements are ignored by default.[/en]
   *   [ja][/ja]
   */
  /**
   * @property tappable
   * @type {Boolean}
   * @description
   *   [en]Makes the element react to taps. `prevent-tap` attribute can be added to child elements like buttons or inputs to prevent this effect. `ons-*` elements are ignored by default.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute tap-background-color
   * @type {Color}
   * @description
   *   [en] Changes the background color when tapped. For this to work, the attribute "tappable" needs to be set. The default color is "#d9d9d9". It will display as a ripple effect on Android.[/en]
   *   [ja][/ja]
   */
  /**
   * @property tapBackgroundColor
   * @type {Color}
   * @description
   *   [en] Changes the background color when tapped. For this to work, the attribute "tappable" needs to be set. The default color is "#d9d9d9". It will display as a ripple effect on Android.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute keep-tap-background-color
   * @type {Boolean}
   * @description
   *  [en] Prevent from clearing the background color on `"touchmove"`, `"touchcancel"`, `"touchend"`, `"touchleave"`, `"mouseup"`, and `"mouseout"`. For this to work, the attribute "tappable" needs to be set.[/en]
   *  [ja] この属性があると、`"touchmove"`, `"touchcancel"`, `"touchend"`, `"touchleave"`, `"mouseup"`, and `"mouseout"` イベント時に背景色がクリアされないようになります。[/ja]
   */
  /**
   * @property keepTapBackgroundColor
   * @type {Boolean}
   * @description
   *   [en] Prevent from clearing the background color on `"touchmove"`, `"touchcancel"`, `"touchend"`, `"touchleave"`, `"mouseup"`, and `"mouseout"`. For this to work, the attribute "tappable" needs to be set.[/en]
   *   [ja] この属性があると、`"touchmove"`, `"touchcancel"`, `"touchend"`, `"touchleave"`, `"mouseup"`, and `"mouseout"` イベント時に背景色がクリアされないようになります。[/ja]
   */
  /**
   * @attribute expandable
   * @type {Boolean}
   * @description
   *   [en]Makes the element able to be expanded to reveal extra content. For this to work, the expandable content must be defined in `div.expandable-content`.[/en]
   *   [ja][/ja]
   */
  /**
   * @property expandable
   * @initonly
   * @type {Boolean}
   * @description
   *   [en]Makes the element able to be expanded to reveal extra content. For this to work, the expandable content must be defined in `div.expandable-content`.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute expanded
   * @type {Boolean}
   * @description
   *   [en]For expandable list items, specifies whether the expandable content is expanded or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @property expanded
   * @type {Boolean}
   * @description
   *   [en]For expandable list items, specifies whether the expandable content is expanded or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @event expand
   * @description
   *   [en]For expandable list items, fires when the list item is expanded or contracted.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the expandable content. Can be either `"default"` or `"none"`.[/en]
   *  [ja][/ja]
   */
  /**
   * @property animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the expandable content. Can be either `"default"` or `"none"`.[/en]
   *  [ja][/ja]
   */
  constructor() {
    super();
    this._animatorFactory = this._updateAnimatorFactory();
    const re = /^ons-(?!col$|row$|if$)/i;
    this._shouldIgnoreTap = (e) => e.hasAttribute("prevent-tap") || re.test(e.tagName);
    this.show = this.showExpansion;
    this.hide = this.hideExpansion;
    contentReady(this, () => {
      this._compile();
    });
  }
  /**
   * Compiles the list item.
   *
   * Various elements are allowed in the body of a list item:
   *
   *  - div.left, div.right, and div.center are allowed as direct children
   *  - if div.center is not defined, anything that isn't div.left, div.right or div.expandable-content will be put in a div.center
   *  - if div.center is defined, anything that isn't div.left, div.right or div.expandable-content will be ignored
   *  - if list item has expandable attribute:
   *      - div.expandable-content is allowed as a direct child
   *      - div.top is allowed as direct child
   *      - if div.top is defined, anything that isn't div.expandable-content should be inside div.top - anything else will be ignored
   *      - if div.right is not defined, a div.right will be created with a drop-down chevron
   *
   * See the tests for examples.
   */
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName6);
    let top, expandableContent;
    let topContent = [];
    Array.from(this.childNodes).forEach((node) => {
      if (node.nodeType !== Node.ELEMENT_NODE) {
        topContent.push(node);
      } else if (node.classList.contains("top")) {
        top = node;
      } else if (node.classList.contains("expandable-content")) {
        expandableContent = node;
      } else {
        topContent.push(node);
      }
      if (node.nodeName !== "ONS-RIPPLE") {
        node.remove();
      }
    });
    topContent = top ? Array.from(top.childNodes) : topContent;
    let left, right, center;
    const centerContent = [];
    topContent.forEach((node) => {
      if (node.nodeType !== Node.ELEMENT_NODE) {
        centerContent.push(node);
      } else if (node.classList.contains("left")) {
        left = node;
      } else if (node.classList.contains("right")) {
        right = node;
      } else if (node.classList.contains("center")) {
        center = node;
      } else {
        centerContent.push(node);
      }
    });
    if (this.hasAttribute("expandable")) {
      this.classList.add("list-item--expandable");
      if (!top) {
        top = document.createElement("div");
        top.classList.add("top");
      }
      top.classList.add("list-item__top");
      this.appendChild(top);
      this._top = top;
      if (expandableContent) {
        expandableContent.classList.add("list-item__expandable-content");
        this.appendChild(expandableContent);
      }
      if (!right) {
        right = document.createElement("div");
        right.classList.add("list-item__right", "right");
        const chevron = document.createElement("span");
        chevron.classList.add("list-item__expand-chevron");
        right.appendChild(chevron);
      }
      if (this.expanded) {
        this.classList.add("list-item--expanded");
      }
    } else {
      top = this;
    }
    if (!center) {
      center = document.createElement("div");
      center.classList.add("center");
      centerContent.forEach((node) => center.appendChild(node));
    }
    center.classList.add("list-item__center");
    top.appendChild(center);
    if (left) {
      left.classList.add("list-item__left");
      top.appendChild(left);
    }
    if (right) {
      right.classList.add("list-item__right");
      top.appendChild(right);
    }
    util_default.updateRipple(this);
    ModifierUtil.initModifier(this, scheme6);
  }
  /**
   * @method showExpansion
   * @signature showExpansion()
   * @description
   *   [en]Show the expandable content if the element is expandable.[/en]
   *   [ja][/ja]
   */
  showExpansion() {
    this.expanded = true;
  }
  /**
   * @method hideExpansion
   * @signature hideExpansion()
   * @description
   *   [en]Hide the expandable content if the element expandable.[/en]
   *   [ja][/ja]
   */
  hideExpansion() {
    this.expanded = false;
  }
  toggleExpansion() {
    this.expanded = !this.expanded;
  }
  /**
   * @method clearTapBackgroundColor
   * @signature clearTapBackgroundColor()
   * @description
   *   [en]Clear backgroundColor changed on tap or click. This method is helpful when `keep-tap-background-color` is `true`. [/en]
   *   [ja]タップやクリックした時に効果が表示されるようになります。このメソッドは `keep-tap-background-color` が `true` のときに使用します。[/ja]
   */
  clearTapBackgroundColor() {
    this._clearTapBackgroundColor();
  }
  _animateExpansion() {
    const expandedAtStartup = this.expanded && this.classList.contains("list-item--expanded");
    if (!this.hasAttribute("expandable") || this._expanding || expandedAtStartup) {
      return;
    }
    this._expanding = true;
    const expandedCallback = () => {
      this._expanding = false;
      if (this.expanded) {
        this.classList.add("list-item--expanded");
      } else {
        this.classList.remove("list-item--expanded");
      }
    };
    const animator = this._animatorFactory.newAnimator();
    if (animator._animateExpansion) {
      animator._animateExpansion(this, this.expanded, expandedCallback);
    } else {
      expandedCallback();
    }
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict,
      baseClass: ListItemAnimator,
      baseClassName: "ListItemAnimator",
      defaultAnimation: this.getAttribute("animation") || "default"
    });
  }
  static get observedAttributes() {
    return ["modifier", "class", "ripple", "animation", "expanded"];
  }
  get expandableContent() {
    return this.querySelector(".list-item__expandable-content");
  }
  get expandChevron() {
    return this.querySelector(".list-item__expand-chevron");
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName6, scheme6);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme6);
        break;
      case "ripple":
        util_default.updateRipple(this);
        break;
      case "animation":
        this._animatorFactory = this._updateAnimatorFactory();
        break;
      case "expanded":
        contentReady(this, () => this._animateExpansion());
        break;
    }
  }
  connectedCallback() {
    contentReady(this, () => {
      this._setupListeners(true);
      this._originalBackgroundColor = this.style.backgroundColor;
      this.tapped = false;
    });
  }
  disconnectedCallback() {
    this._setupListeners(false);
  }
  _setupListeners(add) {
    const action = (add ? "add" : "remove") + "EventListener";
    util_default[action](this, "touchstart", this._onTouch, { passive: true });
    util_default[action](this, "touchmove", this._onRelease, { passive: true });
    this[action]("touchcancel", this._onRelease);
    this[action]("touchend", this._onRelease);
    this[action]("touchleave", this._onRelease);
    this[action]("drag", this._onDrag);
    this[action]("mousedown", this._onTouch);
    this[action]("mouseup", this._onRelease);
    this[action]("mouseout", this._onRelease);
    if (this._top) {
      this._top[action]("click", this._onClickTop.bind(this));
    }
  }
  _onClickTop() {
    if (!this._expanding) {
      this.toggleExpansion();
      this.dispatchEvent(new Event("expand"));
      this.dispatchEvent(new Event("expansion"));
    }
  }
  _onDrag(event) {
    const gesture = event.gesture;
    if (this.hasAttribute("lock-on-drag") && ["left", "right"].indexOf(gesture.direction) > -1) {
      gesture.preventDefault();
    }
  }
  _onTouch(e) {
    if (this.tapped || this !== e.target && (this._shouldIgnoreTap(e.target) || util_default.findParent(e.target, this._shouldIgnoreTap, (p) => p === this))) {
      return;
    }
    this.tapped = true;
    const touchStyle = { transition: "background-color 0.0s linear 0.02s, box-shadow 0.0s linear 0.02s" };
    if (this.hasAttribute("tappable")) {
      if (this.style.backgroundColor) {
        this._originalBackgroundColor = this.style.backgroundColor;
      }
      touchStyle.backgroundColor = this.getAttribute("tap-background-color") || "#d9d9d9";
      touchStyle.boxShadow = `0px -1px 0px 0px ${touchStyle.backgroundColor}`;
    }
    styler_default(this, touchStyle);
  }
  _onRelease() {
    this.tapped = false;
    if (!this.keepTapBackgroundColor) {
      this._clearTapBackgroundColor();
    }
    styler_default.clear(this, "transition boxShadow");
  }
  _clearTapBackgroundColor() {
    this.style.backgroundColor = this._originalBackgroundColor || "";
  }
};
util_default.defineBooleanProperties(ListItemElement, ["expanded", "expandable", "tappable", "lock-on-drag", "keep-tap-background-color"]);
util_default.defineStringProperties(ListItemElement, ["animation", "tap-background-color"]);
elements_default.ListItem = ListItemElement;
customElements.define("ons-list-item", ListItemElement);

// node_modules/vue-onsenui/esm/components/VOnsListItem.js
var name9 = "v-ons-list-item";
var script9 = {
  name: name9,
  mixins: [deriveEvents(name9), modifier],
  emits: ["update:expanded"],
  methods: {
    onExpand() {
      this.$emit("update:expanded", this.$el.expanded);
    }
  }
};
var _hoisted_19 = ["modifier"];
function render10(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-list-item", {
    modifier: _ctx.normalizedModifier,
    onExpand: _cache[0] || (_cache[0] = (...args) => $options.onExpand && $options.onExpand(...args))
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 40, _hoisted_19);
}
script9.render = render10;

// node_modules/onsenui/esm/elements/ons-list-title.js
var defaultClassName7 = "list-title";
var scheme7 = { "": "list-title--*" };
var ListTitleElement = class extends BaseElement {
  constructor() {
    super();
    this._compile();
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName7);
    ModifierUtil.initModifier(this, scheme7);
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName7, scheme7);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme7);
        break;
    }
  }
};
elements_default.ListTitle = ListTitleElement;
customElements.define("ons-list-title", ListTitleElement);

// node_modules/vue-onsenui/esm/components/VOnsListTitle.js
var name10 = "v-ons-list-title";
var script10 = {
  name: name10,
  mixins: [deriveEvents(name10), modifier]
};
var _hoisted_110 = ["modifier"];
function render11(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-list-title", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_110);
}
script10.render = render11;

// node_modules/onsenui/esm/elements/ons-list-header.js
var defaultClassName8 = "list-header";
var scheme8 = { "": "list-header--*" };
var ListHeaderElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the list header.[/en]
   *   [ja]ヘッダーの表現を指定します。[/ja]
   */
  constructor() {
    super();
    this._compile();
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName8);
    ModifierUtil.initModifier(this, scheme8);
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName8, scheme8);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme8);
        break;
    }
  }
};
elements_default.ListHeader = ListHeaderElement;
customElements.define("ons-list-header", ListHeaderElement);

// node_modules/vue-onsenui/esm/components/VOnsListHeader.js
var name11 = "v-ons-list-header";
var script11 = {
  name: name11,
  mixins: [deriveEvents(name11), modifier]
};
var _hoisted_111 = ["modifier"];
function render12(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-list-header", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_111);
}
script11.render = render12;

// node_modules/vue-onsenui/esm/components/VOnsRipple.js
var name12 = "v-ons-ripple";
var script12 = {
  name: name12,
  mixins: [deriveEvents(name12)]
};
function render13(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-ripple", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script12.render = render13;

// node_modules/onsenui/esm/elements/ons-row.js
var RowElement = class extends BaseElement {
};
elements_default.Row = RowElement;
customElements.define("ons-row", RowElement);

// node_modules/vue-onsenui/esm/components/VOnsRow.js
var name13 = "v-ons-row";
var script13 = {
  name: name13,
  mixins: [deriveEvents(name13)]
};
function render14(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-row", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script13.render = render14;

// node_modules/onsenui/esm/elements/ons-col.js
var ColElement = class extends BaseElement {
  constructor() {
    super();
    if (this.getAttribute("width")) {
      this._updateWidth();
    }
  }
  static get observedAttributes() {
    return ["width"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "width") {
      this._updateWidth();
    }
  }
  _updateWidth() {
    let width = this.getAttribute("width");
    if (!width) {
      styler_default.clear(this, "flex maxWidth");
    } else {
      width = width.trim().match(/^\d+$/) ? width + "%" : width;
      styler_default(this, {
        flex: "0 0 " + width,
        maxWidth: width
      });
    }
  }
};
elements_default.Col = ColElement;
customElements.define("ons-col", ColElement);

// node_modules/vue-onsenui/esm/components/VOnsCol.js
var name14 = "v-ons-col";
var script14 = {
  name: name14,
  mixins: [deriveEvents(name14)]
};
function render15(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-col", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script14.render = render15;

// node_modules/onsenui/esm/elements/ons-progress-bar.js
var scheme9 = {
  ".progress-bar": "progress-bar--*",
  ".progress-bar__primary": "progress-bar--*__primary",
  ".progress-bar__secondary": "progress-bar--*__secondary"
};
var template = util_default.createElement(`
  <div class="progress-bar">
    <div class="progress-bar__secondary"></div>
    <div class="progress-bar__primary"></div>
  </div>
`);
var INDET = "indeterminate";
var ProgressBarElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]Change the appearance of the progress indicator.[/en]
   *   [ja]プログレスインジケータの見た目を変更します。[/ja]
   */
  /**
   * @attribute value
   * @type {Number}
   * @description
   *   [en]Current progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  /**
   * @attribute secondary-value
   * @type {Number}
   * @description
   *   [en]Current secondary progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の２番目の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  /**
   * @attribute indeterminate
   * @description
   *   [en]If this attribute is set, an infinite looping animation will be shown.[/en]
   *   [ja]この属性が設定された場合、ループするアニメーションが表示されます。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
  }
  _compile() {
    if (!this._isCompiled()) {
      this._template = template.cloneNode(true);
    } else {
      this._template = util_default.findChild(this, ".progress-bar");
    }
    this._primary = util_default.findChild(this._template, ".progress-bar__primary");
    this._secondary = util_default.findChild(this._template, ".progress-bar__secondary");
    this._updateDeterminate();
    this._updateValue();
    this.appendChild(this._template);
    autostyle_default.prepare(this);
    ModifierUtil.initModifier(this, scheme9);
  }
  _isCompiled() {
    if (!util_default.findChild(this, ".progress-bar")) {
      return false;
    }
    const barElement = util_default.findChild(this, ".progress-bar");
    if (!util_default.findChild(barElement, ".progress-bar__secondary")) {
      return false;
    }
    if (!util_default.findChild(barElement, ".progress-bar__primary")) {
      return false;
    }
    return true;
  }
  static get observedAttributes() {
    return ["modifier", "value", "secondary-value", INDET];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "modifier") {
      ModifierUtil.onModifierChanged(last, current, this, scheme9);
      this.hasAttribute(INDET) && this._updateDeterminate();
    } else if (name44 === "value" || name44 === "secondary-value") {
      this._updateValue();
    } else if (name44 === INDET) {
      this._updateDeterminate();
    }
  }
  _updateDeterminate() {
    contentReady(this, () => util_default.toggleModifier(this, INDET, { force: this.hasAttribute(INDET) }));
  }
  _updateValue() {
    contentReady(this, () => {
      this._primary.style.width = this.hasAttribute("value") ? this.getAttribute("value") + "%" : "0%";
      this._secondary.style.width = this.hasAttribute("secondary-value") ? this.getAttribute("secondary-value") + "%" : "0%";
    });
  }
  /**
   * @property value
   * @type {Number}
   * @description
   *   [en]Current progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  set value(value) {
    if (typeof value !== "number" || value < 0 || value > 100) {
      util_default.throw("Invalid value");
    }
    this.setAttribute("value", Math.floor(value));
  }
  get value() {
    return parseInt(this.getAttribute("value") || "0");
  }
  /**
   * @property secondaryValue
   * @type {Number}
   * @description
   *   [en]Current secondary progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の２番目の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  set secondaryValue(value) {
    if (typeof value !== "number" || value < 0 || value > 100) {
      util_default.throw("Invalid value");
    }
    this.setAttribute("secondary-value", Math.floor(value));
  }
  get secondaryValue() {
    return parseInt(this.getAttribute("secondary-value") || "0");
  }
  /**
   * @property indeterminate
   * @type {Boolean}
   * @description
   *   [en]If this property is `true`, an infinite looping animation will be shown.[/en]
   *   [ja]この属性が設定された場合、ループするアニメーションが表示されます。[/ja]
   */
  set indeterminate(value) {
    if (value) {
      this.setAttribute(INDET, "");
    } else {
      this.removeAttribute(INDET);
    }
  }
  get indeterminate() {
    return this.hasAttribute(INDET);
  }
};
elements_default.ProgressBar = ProgressBarElement;
customElements.define("ons-progress-bar", ProgressBarElement);

// node_modules/vue-onsenui/esm/components/VOnsProgressBar.js
var name15 = "v-ons-progress-bar";
var script15 = {
  name: name15,
  mixins: [deriveEvents(name15), modifier]
};
var _hoisted_112 = ["modifier"];
function render16(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-progress-bar", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_112);
}
script15.render = render16;

// node_modules/onsenui/esm/elements/ons-progress-circular.js
var scheme10 = {
  ".progress-circular": "progress-circular--*",
  ".progress-circular__background": "progress-circular--*__background",
  ".progress-circular__primary": "progress-circular--*__primary",
  ".progress-circular__secondary": "progress-circular--*__secondary"
};
var template2 = util_default.createElement(`
  <svg class="progress-circular">
    <circle class="progress-circular__background" />
    <circle class="progress-circular__secondary" cx="50%" cy="50%" r="40%" />
    <circle class="progress-circular__primary" cx="50%" cy="50%" r="40%" />
  </svg>
`);
var INDET2 = "indeterminate";
var ProgressCircularElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]Change the appearance of the progress indicator.[/en]
   *   [ja]プログレスインジケータの見た目を変更します。[/ja]
   */
  /**
   * @attribute value
   * @type {Number}
   * @description
   *   [en]Current progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  /**
   * @attribute secondary-value
   * @type {Number}
   * @description
   *   [en]Current secondary progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の２番目の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  /**
   * @attribute indeterminate
   * @description
   *   [en]If this attribute is set, an infinite looping animation will be shown.[/en]
   *   [ja]この属性が設定された場合、ループするアニメーションが表示されます。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
  }
  static get observedAttributes() {
    return ["modifier", "value", "secondary-value", INDET2];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "modifier") {
      ModifierUtil.onModifierChanged(last, current, this, scheme10);
      this.hasAttribute(INDET2) && this._updateDeterminate();
    } else if (name44 === "value" || name44 === "secondary-value") {
      this._updateValue();
    } else if (name44 === INDET2) {
      this._updateDeterminate();
    }
  }
  _updateDeterminate() {
    contentReady(this, () => util_default.toggleModifier(this, INDET2, { force: this.hasAttribute(INDET2) }));
  }
  _updateValue() {
    if (this.hasAttribute("value")) {
      contentReady(this, () => {
        const per = Math.ceil(this.getAttribute("value") * 251.32 * 0.01);
        this._primary.style["stroke-dasharray"] = per + "%, 251.32%";
      });
    }
    if (this.hasAttribute("secondary-value")) {
      contentReady(this, () => {
        const per = Math.ceil(this.getAttribute("secondary-value") * 251.32 * 0.01);
        this._secondary.style.display = null;
        this._secondary.style["stroke-dasharray"] = per + "%, 251.32%";
      });
    } else {
      contentReady(this, () => {
        this._secondary.style.display = "none";
      });
    }
  }
  /**
   * @property value
   * @type {Number}
   * @description
   *   [en]Current progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  set value(value) {
    if (typeof value !== "number" || value < 0 || value > 100) {
      util_default.throw("Invalid value");
    }
    this.setAttribute("value", Math.floor(value));
  }
  get value() {
    return parseInt(this.getAttribute("value") || "0");
  }
  /**
   * @property secondaryValue
   * @type {Number}
   * @description
   *   [en]Current secondary progress. Should be a value between 0 and 100.[/en]
   *   [ja]現在の２番目の進行状況の値を指定します。0から100の間の値を指定して下さい。[/ja]
   */
  set secondaryValue(value) {
    if (typeof value !== "number" || value < 0 || value > 100) {
      util_default.throw("Invalid value");
    }
    this.setAttribute("secondary-value", Math.floor(value));
  }
  get secondaryValue() {
    return parseInt(this.getAttribute("secondary-value") || "0");
  }
  /**
   * @property indeterminate
   * @type {Boolean}
   * @description
   *   [en]If this property is `true`, an infinite looping animation will be shown.[/en]
   *   [ja]この属性が設定された場合、ループするアニメーションが表示されます。[/ja]
   */
  set indeterminate(value) {
    if (value) {
      this.setAttribute(INDET2, "");
    } else {
      this.removeAttribute(INDET2);
    }
  }
  get indeterminate() {
    return this.hasAttribute(INDET2);
  }
  _compile() {
    if (this._isCompiled()) {
      this._template = util_default.findChild(this, ".progress-circular");
    } else {
      this._template = template2.cloneNode(true);
    }
    this._primary = util_default.findChild(this._template, ".progress-circular__primary");
    this._secondary = util_default.findChild(this._template, ".progress-circular__secondary");
    this._updateDeterminate();
    this._updateValue();
    this.appendChild(this._template);
    autostyle_default.prepare(this);
    ModifierUtil.initModifier(this, scheme10);
  }
  _isCompiled() {
    if (!util_default.findChild(this, ".progress-circular")) {
      return false;
    }
    const svg = util_default.findChild(this, ".progress-circular");
    if (!util_default.findChild(svg, ".progress-circular__secondary")) {
      return false;
    }
    if (!util_default.findChild(svg, ".progress-circular__primary")) {
      return false;
    }
    return true;
  }
};
elements_default.ProgressCircular = ProgressCircularElement;
customElements.define("ons-progress-circular", ProgressCircularElement);

// node_modules/vue-onsenui/esm/components/VOnsProgressCircular.js
var name16 = "v-ons-progress-circular";
var script16 = {
  name: name16,
  mixins: [deriveEvents(name16), modifier]
};
var _hoisted_113 = ["modifier"];
function render17(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-progress-circular", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_113);
}
script16.render = render17;

// node_modules/onsenui/esm/elements/ons-carousel-item.js
var scheme11 = { "": "carousel-item--*" };
var CarouselItemElement = class extends BaseElement {
  constructor() {
    super();
    this.style.width = "100%";
    ModifierUtil.initModifier(this, scheme11);
  }
  static get observedAttributes() {
    return ["modifier"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "modifier") {
      return ModifierUtil.onModifierChanged(last, current, this, scheme11);
    }
  }
};
elements_default.CarouselItem = CarouselItemElement;
customElements.define("ons-carousel-item", CarouselItemElement);

// node_modules/vue-onsenui/esm/components/VOnsCarouselItem.js
var name17 = "v-ons-carousel-item";
var script17 = {
  name: name17,
  mixins: [deriveEvents(name17)]
};
function render18(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-carousel-item", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script17.render = render18;

// node_modules/onsenui/esm/elements/ons-splitter-mask.js
var SplitterMaskElement = class extends BaseElement {
  constructor() {
    super();
    this._boundOnClick = this._onClick.bind(this);
    contentReady(this, () => {
      if (this.parentNode._sides.every((side) => side.mode === "split")) {
        this.setAttribute("style", "display: none !important");
      }
    });
  }
  _onClick(event) {
    if (this.onClick instanceof Function) {
      this.onClick();
    } else if (util_default.match(this.parentNode, "ons-splitter")) {
      this.parentNode._sides.forEach((side) => side.close("left").catch(() => {
      }));
    }
    event.stopPropagation();
  }
  static get observedAttributes() {
    return [];
  }
  attributeChangedCallback(name44, last, current) {
  }
  connectedCallback() {
    this.addEventListener("click", this._boundOnClick);
  }
  disconnectedCallback() {
    this.removeEventListener("click", this._boundOnClick);
  }
};
elements_default.SplitterMask = SplitterMaskElement;
customElements.define("ons-splitter-mask", SplitterMaskElement);

// node_modules/vue-onsenui/esm/components/VOnsSplitterMask.js
var name18 = "v-ons-splitter-mask";
var script18 = {
  name: name18,
  mixins: [deriveEvents(name18)]
};
function render19(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-splitter-mask", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script18.render = render19;

// node_modules/onsenui/esm/elements/ons-splitter-content.js
var rewritables = {
  /**
   * @param {Element} element
   * @param {Function} callback
   */
  ready(element, callback) {
    setImmediate(callback);
  }
};
var SplitterContentElement = class extends BaseElement {
  /**
   * @attribute page
   * @type {String}
   * @description
   *   [en]
   *     The url of the content page. If this attribute is used the content will be loaded from a `<template>` tag or a remote file.
   *
   *     It is also possible to put `<ons-page>` element as a child of the element.
   *   [/en]
   *   [ja]ons-splitter-content要素に表示するページのURLを指定します。[/ja]
   */
  constructor() {
    super();
    this._page = null;
    this._pageLoader = defaultPageLoader;
    contentReady(this, () => {
      rewritables.ready(this, () => {
        const page = this._getPageTarget();
        if (page) {
          this.load(page);
        }
      });
    });
  }
  connectedCallback() {
    if (!util_default.match(this.parentNode, "ons-splitter")) {
      util_default.throw('"ons-splitter-content" must have "ons-splitter" as parent');
    }
  }
  _getPageTarget() {
    return this._page || this.getAttribute("page");
  }
  disconnectedCallback() {
  }
  static get observedAttributes() {
    return [];
  }
  attributeChangedCallback(name44, last, current) {
  }
  /**
   * @property page
   * @type {HTMLElement}
   * @description
   *   [en]The page to load in the splitter content.[/en]
   *   [ja]この要素内に表示するページを指定します。[/ja]
   */
  get page() {
    return this._page;
  }
  /**
   * @param {*} page
   */
  set page(page) {
    this._page = page;
  }
  get _content() {
    return this.children[0];
  }
  /**
   * @property pageLoader
   * @type {Function}
   * @description
   *   [en]Page element loaded in the splitter content.[/en]
   *   [ja]この要素内に表示するページを指定します。[/ja]
   */
  get pageLoader() {
    return this._pageLoader;
  }
  set pageLoader(loader) {
    if (!(loader instanceof PageLoader)) {
      util_default.throwPageLoader();
    }
    this._pageLoader = loader;
  }
  /**
   * @method load
   * @signature load(page, [options])
   * @param {String} page, [options]
   *   [en]Page URL. Can be either an HTML document or an `<template>` id.[/en]
   *   [ja]pageのURLか、`<template>`で宣言したテンプレートのid属性の値を指定します。[/ja]
   * @param {Object} [options]
   * @param {Function} [options.callback]
   * @description
   *   [en]Show the page specified in `page` in the content.[/en]
   *   [ja]指定したURLをメインページを読み込みます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the new `<ons-page>` element[/en]
   *   [ja]`<ons-page>`要素を解決するPromiseオブジェクトを返します。[/ja]
   */
  load(page, options = {}) {
    this._page = page;
    const callback = options.callback || function() {
    };
    return new Promise((resolve) => {
      let oldContent = this._content || null;
      this._pageLoader.load({ page, parent: this }, (pageElement) => {
        if (oldContent) {
          this._pageLoader.unload(oldContent);
          oldContent = null;
        }
        setImmediate(() => this._show());
        callback(pageElement);
        resolve(pageElement);
      });
    });
  }
  _show() {
    if (this._content) {
      this._content._show();
    }
  }
  _hide() {
    if (this._content) {
      this._content._hide();
    }
  }
  _destroy() {
    if (this._content) {
      this._pageLoader.unload(this._content);
    }
    this.remove();
  }
  static get rewritables() {
    return rewritables;
  }
};
elements_default.SplitterContent = SplitterContentElement;
customElements.define("ons-splitter-content", SplitterContentElement);

// node_modules/vue-onsenui/esm/components/VOnsSplitterContent.js
var name19 = "v-ons-splitter-content";
var script19 = {
  name: name19,
  mixins: [deriveEvents(name19)]
};
function render20(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-splitter-content", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script19.render = render20;

// node_modules/onsenui/esm/elements/ons-splitter/animator.js
var SplitterAnimator2 = class extends BaseAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .1, 1)", duration = 0.3, delay = 0 } = {}) {
    super({ timing, duration, delay });
  }
  updateOptions(options = {}) {
    util_default.extend(this, {
      timing: this.timing,
      duration: this.duration,
      delay: this.delay
    }, options);
  }
  /**
   * @param {Element} sideElement
   */
  activate(sideElement) {
    const splitter = sideElement.parentNode;
    contentReady(splitter, () => {
      this._side = sideElement;
      this._oppositeSide = splitter.right !== sideElement && splitter.right || splitter.left !== sideElement && splitter.left;
      this._content = splitter.content;
      this._mask = splitter.mask;
    });
  }
  deactivate() {
    this.clearTransition();
    this._mask && this.clearMask();
    this._content = this._side = this._oppositeSide = this._mask = null;
  }
  get minus() {
    return this._side.side === "right" ? "-" : "";
  }
  clearTransition() {
    "side mask content".split(/\s+/).forEach((e) => this["_" + e] && styler_default.clear(this["_" + e], "transform transition"));
  }
  clearMask() {
    if (!this._oppositeSide || this._oppositeSide.mode === "split" || !this._oppositeSide.isOpen) {
      this._mask.style.opacity = "";
      this._mask.style.display = "none";
    }
  }
  /**
   * @param {Number} distance
   */
  translate(distance) {
  }
  /**
   * @param {Function} done
   */
  open(done) {
    done();
  }
  /**
   * @param {Function} done
   */
  close(done) {
    done();
  }
};

// node_modules/onsenui/esm/elements/ons-splitter/overlay-animator.js
var OverlaySplitterAnimator = class extends SplitterAnimator2 {
  translate(distance) {
    this._mask.style.display = "block";
    animit_default(this._side).queue({
      transform: `translate3d(${this.minus + distance}px, 0, 0)`
    }).play();
  }
  /**
   * @param {Function} done
   */
  open(done) {
    animit_default.runAll(
      animit_default(this._side).wait(this.delay).queue({
        transform: `translate3d(${this.minus}100%, 0, 0)`
      }, this.def).queue((callback) => {
        callback();
        done && done();
      }),
      animit_default(this._mask).wait(this.delay).queue({
        display: "block"
      }).queue({
        opacity: "1"
      }, {
        duration: this.duration,
        timing: "linear"
      })
    );
  }
  /**
   * @param {Function} done
   */
  close(done) {
    animit_default.runAll(
      animit_default(this._side).wait(this.delay).queue({
        transform: "translate3d(0, 0, 0)"
      }, this.def).queue((callback) => {
        done && done();
        callback();
      }),
      animit_default(this._mask).wait(this.delay).queue({
        opacity: "0"
      }, {
        duration: this.duration,
        timing: "linear"
      }).queue({
        display: "none"
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-splitter/push-animator.js
var PushSplitterAnimator = class extends SplitterAnimator2 {
  _getSlidingElements() {
    const slidingElements = [this._side, this._content];
    if (this._oppositeSide && this._oppositeSide.mode === "split") {
      slidingElements.push(this._oppositeSide);
    }
    return slidingElements;
  }
  translate(distance) {
    if (!this._slidingElements) {
      this._slidingElements = this._getSlidingElements();
    }
    this._mask.style.display = "block";
    animit_default(this._slidingElements).queue({
      transform: `translate3d(${this.minus + distance}px, 0, 0)`
    }).play();
  }
  /**
   * @param {Function} done
   */
  open(done) {
    const max = this._side.offsetWidth;
    this._slidingElements = this._getSlidingElements();
    animit_default.runAll(
      animit_default(this._slidingElements).wait(this.delay).queue({
        transform: `translate3d(${this.minus + max}px, 0, 0)`
      }, this.def).queue((callback) => {
        this._slidingElements = null;
        callback();
        done && done();
      }),
      animit_default(this._mask).wait(this.delay).queue({
        display: "block"
      })
    );
  }
  /**
   * @param {Function} done
   */
  close(done) {
    this._slidingElements = this._getSlidingElements();
    animit_default.runAll(
      animit_default(this._slidingElements).wait(this.delay).queue({
        transform: "translate3d(0, 0, 0)"
      }, this.def).queue((callback) => {
        this._slidingElements = null;
        super.clearTransition();
        done && done();
        callback();
      }),
      animit_default(this._mask).wait(this.delay).queue({
        display: "none"
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-splitter/reveal-animator.js
var RevealSplitterAnimator = class extends SplitterAnimator2 {
  _getSlidingElements() {
    const slidingElements = [this._content, this._mask];
    if (this._oppositeSide && this._oppositeSide.mode === "split") {
      slidingElements.push(this._oppositeSide);
    }
    return slidingElements;
  }
  activate(sideElement) {
    super.activate(sideElement);
    if (sideElement.mode === "collapse") {
      this._setStyles(sideElement);
    }
  }
  deactivate() {
    this._side && this._unsetStyles(this._side);
    super.deactivate();
  }
  _setStyles(sideElement) {
    styler_default(sideElement, {
      left: sideElement.side === "right" ? "auto" : 0,
      right: sideElement.side === "right" ? 0 : "auto",
      zIndex: 0,
      backgroundColor: "black",
      transform: this._generateBehindPageStyle(0).container.transform,
      display: "none"
    });
    const splitter = sideElement.parentElement;
    contentReady(splitter, () => splitter.content && styler_default(splitter.content, { boxShadow: "0 0 12px 0 rgba(0, 0, 0, 0.2)" }));
  }
  _unsetStyles(sideElement) {
    styler_default.clear(sideElement, "left right zIndex backgroundColor display");
    if (sideElement._content) {
      sideElement._content.style.opacity = "";
    }
    if (!this._oppositeSide || this._oppositeSide.mode === "split") {
      sideElement.parentElement.content && styler_default.clear(sideElement.parentElement.content, "boxShadow");
    }
  }
  _generateBehindPageStyle(distance) {
    const max = this.maxWidth;
    let behindDistance = (distance - max) / max * 10;
    behindDistance = isNaN(behindDistance) ? 0 : Math.max(Math.min(behindDistance, 0), -10);
    const behindTransform = `translate3d(${(this.minus ? -1 : 1) * behindDistance}%, 0, 0)`;
    const opacity = 1 + behindDistance / 100;
    return {
      content: {
        opacity
      },
      container: {
        transform: behindTransform
      }
    };
  }
  translate(distance) {
    this._side.style.display = "";
    this._side.style.zIndex = 1;
    this.maxWidth = this.maxWidth || this._getMaxWidth();
    const menuStyle = this._generateBehindPageStyle(Math.min(distance, this.maxWidth));
    if (!this._slidingElements) {
      this._slidingElements = this._getSlidingElements();
    }
    this._mask.style.display = "block";
    animit_default.runAll(
      animit_default(this._slidingElements).queue({
        transform: `translate3d(${this.minus + distance}px, 0, 0)`
      }),
      animit_default(this._side._content).queue(menuStyle.content),
      animit_default(this._side).queue(menuStyle.container)
    );
  }
  /**
   * @param {Function} done
   */
  open(done) {
    this._side.style.display = "";
    this._side.style.zIndex = 1;
    this.maxWidth = this.maxWidth || this._getMaxWidth();
    const menuStyle = this._generateBehindPageStyle(this.maxWidth);
    this._slidingElements = this._getSlidingElements();
    setTimeout(() => {
      animit_default.runAll(
        animit_default(this._slidingElements).wait(this.delay).queue({
          transform: `translate3d(${this.minus + this.maxWidth}px, 0, 0)`
        }, this.def),
        animit_default(this._mask).wait(this.delay).queue({
          display: "block"
        }),
        animit_default(this._side._content).wait(this.delay).queue(menuStyle.content, this.def),
        animit_default(this._side).wait(this.delay).queue(menuStyle.container, this.def).queue((callback) => {
          this._slidingElements = null;
          callback();
          done && done();
        })
      );
    }, 1e3 / 60);
  }
  /**
   * @param {Function} done
   */
  close(done) {
    const menuStyle = this._generateBehindPageStyle(0);
    this._slidingElements = this._getSlidingElements();
    animit_default.runAll(
      animit_default(this._slidingElements).wait(this.delay).queue({
        transform: "translate3d(0, 0, 0)"
      }, this.def),
      animit_default(this._mask).wait(this.delay).queue({
        display: "none"
      }),
      animit_default(this._side._content).wait(this.delay).queue(menuStyle.content, this.def),
      animit_default(this._side).wait(this.delay).queue(menuStyle.container, this.def).queue((callback) => {
        this._slidingElements = null;
        this._side.style.zIndex = 0;
        this._side.style.display = "none";
        this._side._content.style.opacity = "";
        done && done();
        callback();
      })
    );
  }
  _getMaxWidth() {
    return this._side.offsetWidth;
  }
};

// node_modules/onsenui/esm/elements/ons-splitter.js
var _animatorDict2 = {
  default: OverlaySplitterAnimator,
  overlay: OverlaySplitterAnimator,
  push: PushSplitterAnimator,
  reveal: RevealSplitterAnimator
};
var SplitterElement = class extends BaseElement {
  _getSide(side) {
    const element = util_default.findChild(this, (e) => {
      return util_default.match(e, "ons-splitter-side") && e.getAttribute("side") === side;
    });
    return element;
  }
  /**
   * @property left
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]Left `<ons-splitter-side>` element.[/en]
   *   [ja][/ja]
   */
  get left() {
    return this._getSide("left");
  }
  /**
   * @property right
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]Right `<ons-splitter-side>` element.[/en]
   *   [ja][/ja]
   */
  get right() {
    return this._getSide("right");
  }
  /**
   * @property side
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]First `<ons-splitter-side>` element regardless the actual side.[/en]
   *   [ja][/ja]
   */
  get side() {
    return util_default.findChild(this, "ons-splitter-side");
  }
  get _sides() {
    return [this.left, this.right].filter((e) => e);
  }
  /**
   * @property content
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]The `<ons-splitter-content>` element.[/en]
   *   [ja][/ja]
   */
  get content() {
    return util_default.findChild(this, "ons-splitter-content");
  }
  get topPage() {
    return this.content._content;
  }
  get mask() {
    return util_default.findChild(this, "ons-splitter-mask");
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  get onDeviceBackButton() {
    return this._backButtonHandler;
  }
  set onDeviceBackButton(callback) {
    if (this._backButtonHandler) {
      this._backButtonHandler.destroy();
    }
    this._backButtonHandler = device_back_button_dispatcher_default.createHandler(this, callback);
  }
  _onDeviceBackButton(event) {
    this._sides.some((s) => s.isOpen ? s.close() : false) || event.callParentHandler();
  }
  _onModeChange(e) {
    if (e.target.parentNode) {
      contentReady(this, () => {
        this._layout();
      });
    }
  }
  _layout() {
    this._sides.forEach((side) => {
      if (this.content) {
        this.content.style[side.side] = side.mode === "split" ? side.style.width : 0;
      }
    });
  }
  constructor() {
    super();
    this._onModeChange = this._onModeChange.bind(this);
    contentReady(this, () => {
      !this.mask && this.appendChild(document.createElement("ons-splitter-mask"));
      this._layout();
    });
  }
  connectedCallback() {
    this.onDeviceBackButton = this._onDeviceBackButton.bind(this);
    this.addEventListener("modechange", this._onModeChange, false);
  }
  disconnectedCallback() {
    this._backButtonHandler.destroy();
    this._backButtonHandler = null;
    this.removeEventListener("modechange", this._onModeChange, false);
  }
  attributeChangedCallback(name44, last, current) {
  }
  _show() {
    util_default.propagateAction(this, "_show");
  }
  _hide() {
    util_default.propagateAction(this, "_hide");
  }
  _destroy() {
    util_default.propagateAction(this, "_destroy");
    this.remove();
  }
  static registerAnimator(name44, Animator) {
    if (!(Animator instanceof SplitterAnimator)) {
      util_default.throwAnimator("Splitter");
    }
    _animatorDict2[name44] = Animator;
  }
  static get SplitterAnimator() {
    return SplitterAnimator;
  }
  static get animators() {
    return _animatorDict2;
  }
};
elements_default.Splitter = SplitterElement;
customElements.define("ons-splitter", SplitterElement);

// node_modules/vue-onsenui/esm/components/VOnsSplitter.js
var name20 = "v-ons-splitter";
var script20 = {
  name: name20,
  mixins: [deriveEvents(name20), selfProvider, deriveDBB]
};
function render21(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-splitter", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script20.render = render21;

// node_modules/onsenui/esm/elements/base/base-input.js
var INPUT_ATTRIBUTES = [
  "autocapitalize",
  "autocomplete",
  "autocorrect",
  "autofocus",
  "disabled",
  "inputmode",
  "max",
  "maxlength",
  "min",
  "minlength",
  "name",
  "pattern",
  "placeholder",
  "readonly",
  "required",
  "size",
  "spellcheck",
  "step",
  "validator",
  "value"
];
var BaseInputElement = class _BaseInputElement extends BaseElement {
  _update() {
  }
  // Optionally implemented
  get _scheme() {
    util_default.throwMember();
  }
  get _template() {
    util_default.throwMember();
  }
  get type() {
    util_default.throwMember();
  }
  constructor() {
    super();
    if (this.constructor === _BaseInputElement) {
      util_default.throwAbstract();
    }
    contentReady(this, () => this._compile());
    this._boundDelegateEvent = this._delegateEvent.bind(this);
  }
  _compile() {
    autostyle_default.prepare(this);
    this._defaultClassName && this.classList.add(this._defaultClassName);
    if (this.children.length !== 0) {
      return;
    }
    this.appendChild(util_default.createFragment(this._template));
    this._setInputId();
    this._updateBoundAttributes();
    ModifierUtil.initModifier(this, this._scheme);
  }
  _updateBoundAttributes() {
    INPUT_ATTRIBUTES.forEach((attr) => {
      if (this.hasAttribute(attr)) {
        this._input.setAttribute(attr, this.getAttribute(attr));
      } else {
        this._input.removeAttribute(attr);
      }
    });
    this._update();
  }
  _delegateEvent(event) {
    const e = new CustomEvent(event.type, {
      bubbles: false,
      cancelable: true
    });
    return this.dispatchEvent(e);
  }
  _setInputId() {
    if (this.hasAttribute("input-id")) {
      this._input.id = this.getAttribute("input-id");
    }
  }
  get _defaultClassName() {
    return "";
  }
  get _input() {
    return this.querySelector("input");
  }
  get value() {
    return this._input === null ? this.getAttribute("value") : this._input.value;
  }
  set value(val) {
    contentReady(this, () => {
      if (val instanceof Date) {
        val = val.toISOString().substring(0, 10);
      }
      this._input.value = val;
      this._update();
    });
  }
  connectedCallback() {
    contentReady(this, () => {
      this._input.addEventListener("focus", this._boundDelegateEvent);
      this._input.addEventListener("blur", this._boundDelegateEvent);
    });
  }
  disconnectedCallback() {
    contentReady(this, () => {
      this._input.removeEventListener("focus", this._boundDelegateEvent);
      this._input.removeEventListener("blur", this._boundDelegateEvent);
    });
  }
  static get observedAttributes() {
    return ["modifier", "input-id", "class", ...INPUT_ATTRIBUTES];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "modifier":
        contentReady(this, () => ModifierUtil.onModifierChanged(last, current, this, this._scheme));
        break;
      case "input-id":
        contentReady(this, () => this._setInputId());
        break;
      case "class":
        util_default.restoreClass(this, this._defaultClassName, this._scheme);
        break;
    }
    if (INPUT_ATTRIBUTES.indexOf(name44) >= 0) {
      contentReady(this, () => this._updateBoundAttributes());
    }
  }
  blur() {
    this._input.blur();
  }
  focus() {
    this._input.focus();
  }
};
util_default.defineBooleanProperties(BaseInputElement, ["disabled"]);

// node_modules/onsenui/esm/elements/base/base-checkbox.js
var BaseCheckboxElement = class _BaseCheckboxElement extends BaseInputElement {
  constructor() {
    super();
    if (this.constructor === _BaseCheckboxElement) {
      util.throwAbstract();
    }
    contentReady(this, () => {
      this.attributeChangedCallback("checked", null, this.getAttribute("checked"));
    });
  }
  /* Inherited props */
  get _template() {
    return `
      <input type="${this.type}" class="${this._defaultClassName}__input">
      <span class="${this._defaultClassName}__checkmark"></span>
    `;
  }
  /* Own props */
  get _helper() {
    return this.querySelector("span");
  }
  get checked() {
    return this._input.checked;
  }
  set checked(val) {
    contentReady(this, () => {
      this._input.checked = val;
    });
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "checked"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "checked":
        this.checked = current !== null;
        break;
      default:
        super.attributeChangedCallback(name44, last, current);
    }
  }
};

// node_modules/onsenui/esm/elements/ons-switch.js
var scheme12 = {
  "": "switch--*",
  ".switch__input": "switch--*__input",
  ".switch__handle": "switch--*__handle",
  ".switch__toggle": "switch--*__toggle"
};
var locations = {
  ios: [1, 21],
  material: [0, 16]
};
var SwitchElement = class extends BaseCheckboxElement {
  constructor() {
    super();
    contentReady(this, () => {
      this.attributeChangedCallback("modifier", null, this.getAttribute("modifier"));
    });
    this._onChange = this._onChange.bind(this);
    this._onRelease = this._onRelease.bind(this);
    this._lastTimeStamp = 0;
  }
  get _scheme() {
    return scheme12;
  }
  get _defaultClassName() {
    return "switch";
  }
  get _template() {
    return `
      <input type="${this.type}" class="${this._defaultClassName}__input">
      <div class="${this._defaultClassName}__toggle">
        <div class="${this._defaultClassName}__handle">
          <div class="${this._defaultClassName}__touch"></div>
        </div>
      </div>
    `;
  }
  get type() {
    return "checkbox";
  }
  /* Own props */
  _getPosition(e) {
    const l = this._locations;
    return Math.min(l[1], Math.max(l[0], this._startX + e.gesture.deltaX));
  }
  _emitChangeEvent() {
    util_default.triggerElementEvent(this, "change", {
      value: this.checked,
      switch: this,
      isInteractive: true
    });
  }
  _onChange(event) {
    if (event && event.stopPropagation) {
      event.stopPropagation();
    }
    this._emitChangeEvent();
  }
  _onClick(ev) {
    if (ev.target.classList.contains(`${this.defaultElementClass}__touch`) || ev.timeStamp - this._lastTimeStamp < 50) {
      ev.preventDefault();
    }
    this._lastTimeStamp = ev.timeStamp;
  }
  _onHold(e) {
    if (!this.disabled) {
      ModifierUtil.addModifier(this, "active");
      document.addEventListener("release", this._onRelease);
    }
  }
  _onDragStart(e) {
    if (this.disabled || ["left", "right"].indexOf(e.gesture.direction) === -1) {
      ModifierUtil.removeModifier(this, "active");
      return;
    }
    e.consumed = true;
    ModifierUtil.addModifier(this, "active");
    this._startX = this._locations[this.checked ? 1 : 0];
    this.addEventListener("drag", this._onDrag);
    document.addEventListener("release", this._onRelease);
  }
  _onDrag(e) {
    e.stopPropagation();
    this._handle.style.left = this._getPosition(e) + "px";
  }
  _onRelease(e) {
    const l = this._locations;
    const position = this._getPosition(e);
    const previousValue = this.checked;
    this.checked = position >= (l[0] + l[1]) / 2;
    if (this.checked !== previousValue) {
      this._emitChangeEvent();
    }
    this.removeEventListener("drag", this._onDrag);
    document.removeEventListener("release", this._onRelease);
    this._handle.style.left = "";
    ModifierUtil.removeModifier(this, "active");
  }
  click(ev = {}) {
    if (!this.disabled) {
      this.checked = !this.checked;
      this._emitChangeEvent();
      this._lastTimeStamp = ev.timeStamp || 0;
    }
  }
  get _handle() {
    return this.querySelector(`.${this._defaultClassName}__handle`);
  }
  get checkbox() {
    return this._input;
  }
  connectedCallback() {
    contentReady(this, () => {
      this._input.addEventListener("change", this._onChange);
    });
    this.addEventListener("dragstart", this._onDragStart);
    this.addEventListener("hold", this._onHold);
    this.addEventListener("tap", this.click);
    this.addEventListener("click", this._onClick);
    this._gestureDetector = new gesture_detector_default(this, { dragMinDistance: 1, holdTimeout: 251, passive: true });
  }
  disconnectedCallback() {
    contentReady(this, () => {
      this._input.removeEventListener("change", this._onChange);
    });
    this.removeEventListener("dragstart", this._onDragStart);
    this.removeEventListener("hold", this._onHold);
    this.removeEventListener("tap", this.click);
    this.removeEventListener("click", this._onClick);
    if (this._gestureDetector) {
      this._gestureDetector.dispose();
    }
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "modifier"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "modifier") {
      const md = (current || "").indexOf("material") !== -1;
      this._locations = locations[md ? "material" : "ios"];
    }
    super.attributeChangedCallback(name44, last, current);
  }
  /**
   * @event change
   * @description
   *   [en]Fired when the switch is toggled.[/en]
   *   [ja]ON/OFFが変わった時に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Object} event.switch
   *   [en]Switch object.[/en]
   *   [ja]イベントが発火したSwitchオブジェクトを返します。[/ja]
   * @param {Boolean} event.value
   *   [en]Current value.[/en]
   *   [ja]現在の値を返します。[/ja]
   * @param {Boolean} event.isInteractive
   *   [en]True if the change was triggered by the user clicking on the switch.[/en]
   *   [ja]タップやクリックなどのユーザの操作によって変わった場合にはtrueを返します。[/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the switch.[/en]
   *  [ja]スイッチの表現を指定します。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Whether the switch is be disabled.[/en]
   *   [ja]スイッチを無効の状態にする場合に指定します。[/ja]
   */
  /**
   * @attribute checked
   * @description
   *   [en]Whether the switch is checked.[/en]
   *   [ja]スイッチがONの状態にするときに指定します。[/ja]
   */
  /**
   * @attribute input-id
   * @type {String}
   * @description
   *   [en]Specify the `id` attribute of the inner `<input>` element. This is useful when using `<label for="...">` elements.[/en]
   *   [ja][/ja]
   */
  /**
   * @property checked
   * @type {Boolean}
   * @description
   *   [en]This value is `true` if the switch is checked.[/en]
   *   [ja]スイッチがONの場合に`true`。[/ja]
   */
  /**
   * @property value
   * @type {String}
   * @description
   *   [en]The current value of the input.[/en]
   *   [ja][/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property checkbox
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]The underlying checkbox element.[/en]
   *   [ja]コンポーネント内部のcheckbox要素になります。[/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the switch.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the switch.[/en]
   *   [ja][/ja]
   */
};
elements_default.Switch = SwitchElement;
customElements.define("ons-switch", SwitchElement);

// node_modules/vue-onsenui/esm/components/VOnsSwitch.js
var name21 = "v-ons-switch";
var script21 = {
  name: name21,
  mixins: [deriveEvents(name21), modelCheckbox, modifier]
};
var _hoisted_114 = ["modifier"];
function render22(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-switch", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_114);
}
script21.render = render22;

// node_modules/onsenui/esm/elements/ons-checkbox.js
var scheme13 = {
  ".checkbox": "checkbox--*",
  ".checkbox__input": "checkbox--*__input",
  ".checkbox__checkmark": "checkbox--*__checkmark"
};
var CheckboxElement = class extends BaseCheckboxElement {
  get _scheme() {
    return scheme13;
  }
  get _defaultClassName() {
    return "checkbox";
  }
  get type() {
    return "checkbox";
  }
  /**
   * @attribute input-id
   * @type {String}
   * @description
   *  [en]Specify the "id" attribute of the inner `<input>` element. This is useful when using `<label for="...">` elements.[/en]
   *  [ja][/ja]
   */
  /**
   * @property value
   * @type {String}
   * @description
   *   [en]The current value of the checkbox.[/en]
   *   [ja][/ja]
   */
  /**
   * @property checked
   * @type {Boolean}
   * @description
   *   [en]Whether the checkbox is checked or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the checkbox is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the checkbox.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the checkbox.[/en]
   *   [ja][/ja]
   */
};
elements_default.Checkbox = CheckboxElement;
customElements.define("ons-checkbox", CheckboxElement);

// node_modules/vue-onsenui/esm/components/VOnsCheckbox.js
var name22 = "v-ons-checkbox";
var script22 = {
  name: name22,
  mixins: [deriveEvents(name22), modelCheckbox, modifier]
};
var _hoisted_115 = ["modifier"];
function render23(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-checkbox", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_115);
}
script22.render = render23;

// node_modules/onsenui/esm/elements/ons-input.js
var scheme14 = {
  ".text-input": "text-input--*",
  ".text-input__label": "text-input--*__label"
};
var InputElement = class extends BaseInputElement {
  constructor() {
    super();
    this._boundOnInput = this._update.bind(this);
    this._boundOnFocusin = this._update.bind(this);
  }
  /* Inherited props */
  _update() {
    this._updateLabel();
    this._updateLabelClass();
  }
  get _scheme() {
    return scheme14;
  }
  get _template() {
    return `
      <input type="${this.type}" class="text-input">
      <span class="text-input__label"></span>
    `;
  }
  get type() {
    const type = this.getAttribute("type");
    return ["checkbox", "radio"].indexOf(type) < 0 && type || "text";
  }
  set type(value) {
    this.setAttribute("type", value);
  }
  /* Own props */
  _updateLabel() {
    const label = this.getAttribute("placeholder") || "";
    if (typeof this._helper.textContent !== "undefined") {
      this._helper.textContent = label;
    } else {
      this._helper.innerText = label;
    }
  }
  _updateLabelClass() {
    if (this.value === "") {
      this._helper.classList.remove("text-input--material__label--active");
    } else {
      this._helper.classList.add("text-input--material__label--active");
    }
  }
  get _helper() {
    return this.querySelector("span");
  }
  connectedCallback() {
    super.connectedCallback();
    contentReady(this, () => {
      this._input.addEventListener("input", this._boundOnInput);
      this._input.addEventListener("focusin", this._boundOnFocusin);
    });
    const type = this.getAttribute("type");
    if (["checkbox", "radio"].indexOf(type) >= 0) {
      util_default.warn(`Warn: <ons-input type="${type}"> is deprecated since v2.4.0. Use <ons-${type}> instead.`);
    }
  }
  disconnectedCallback() {
    super.disconnectedCallback();
    contentReady(this, () => {
      this._input.removeEventListener("input", this._boundOnInput);
      this._input.removeEventListener("focusin", this._boundOnFocusin);
    });
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "type"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "type":
        contentReady(this, () => this._input.setAttribute("type", this.type));
        break;
      default:
        super.attributeChangedCallback(name44, last, current);
    }
  }
  /**
   * @attribute placeholder
   * @type {String}
   * @description
   *   [en]Placeholder text. In Material Design, this placeholder will be a floating label.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute float
   * @description
   *  [en]If this attribute is present, the placeholder will be animated in Material Design.[/en]
   *  [ja]この属性が設定された時、ラベルはアニメーションするようになります。[/ja]
   */
  /**
   * @property float
   * @type {Boolean}
   * @description
   *  [en]If this property is present, the placeholder will be animated in Material Design.[/en]
   *  [ja]この属性が設定された時、ラベルはアニメーションするようになります。[/ja]
   */
  /**
   * @attribute type
   * @type {String}
   * @description
   *  [en]
   *    Specify the input type. This is the same as the "type" attribute for normal inputs. It expects strict text types such as `text`, `password`, etc. For checkbox, radio button, select or range, please have a look at the corresponding elements.
   *
   *    Please take a look at [MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#attr-type) for an exhaustive list of possible values. Depending on the platform and browser version some of these might not work.
   *  [/en]
   *  [ja][/ja]
   */
  /**
   * @attribute input-id
   * @type {String}
   * @description
   *  [en]Specify the "id" attribute of the inner `<input>` element. This is useful when using `<label for="...">` elements.[/en]
   *  [ja][/ja]
   */
  /**
   * @property value
   * @type {String}
   * @description
   *   [en]The current value of the input.[/en]
   *   [ja][/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the input is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the input.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the input.[/en]
   *   [ja][/ja]
   */
};
util_default.defineBooleanProperties(InputElement, ["float"]);
elements_default.Input = InputElement;
customElements.define("ons-input", InputElement);

// node_modules/vue-onsenui/esm/components/VOnsInput.js
var name23 = "v-ons-input";
var script23 = {
  name: name23,
  mixins: [deriveEvents(name23), modelInput, modifier]
};
var _hoisted_116 = ["modifier"];
function render24(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-input", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_116);
}
script23.render = render24;

// node_modules/onsenui/esm/elements/ons-search-input.js
var scheme15 = {
  ".search-input": "search-input--*"
};
var SearchInputElement = class extends BaseInputElement {
  get _scheme() {
    return scheme15;
  }
  get _template() {
    return `
      <input type="${this.type}" class="search-input">
    `;
  }
  get type() {
    return "search";
  }
  /**
   * @attribute input-id
   * @type {String}
   * @description
   *  [en]Specify the "id" attribute of the inner `<input>` element. This is useful when using `<label for="...">` elements.[/en]
   *  [ja][/ja]
   */
  /**
   * @property value
   * @type {String}
   * @description
   *   [en]The current value of the input.[/en]
   *   [ja][/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the input is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the input.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the input.[/en]
   *   [ja][/ja]
   */
};
elements_default.SearchInput = SearchInputElement;
customElements.define("ons-search-input", SearchInputElement);

// node_modules/vue-onsenui/esm/components/VOnsSearchInput.js
var name24 = "v-ons-search-input";
var script24 = {
  name: name24,
  mixins: [deriveEvents(name24), modelInput, modifier]
};
var _hoisted_117 = ["modifier"];
function render25(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-search-input", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_117);
}
script24.render = render25;

// node_modules/onsenui/esm/elements/ons-range.js
var scheme16 = {
  "": "range--*",
  ".range__input": "range--*__input",
  ".range__focus-ring": "range--*__focus-ring"
};
var activeClassToken = "range__input--active";
var RangeElement = class extends BaseInputElement {
  constructor() {
    super();
    this._onMouseDown = this._onMouseDown.bind(this);
    this._onMouseUp = this._onMouseUp.bind(this);
    this._onTouchStart = this._onTouchStart.bind(this);
    this._onTouchEnd = this._onTouchEnd.bind(this);
    this._onInput = this._update.bind(this);
    this._onDragstart = this._onDragstart.bind(this);
    this._onDragend = this._onDragend.bind(this);
  }
  _compile() {
    super._compile();
    this._updateDisabled(this.hasAttribute("disabled"));
  }
  /* Inherited props */
  _update() {
    const input = this._input;
    const focusRing = this._focusRing;
    input.style.backgroundSize = `${100 * this._ratio}% 2px`;
    focusRing.value = this.value;
    if (input.min === "" && input.value === "0" || input.min === input.value) {
      input.setAttribute("_zero", "");
    } else {
      input.removeAttribute("_zero");
    }
    ["min", "max"].forEach((attr) => focusRing[attr] = input[attr]);
  }
  get _scheme() {
    return scheme16;
  }
  get _template() {
    return `
      <input type="${this.type}" class="${this._defaultClassName}__input">
      <input type="range" class="range__focus-ring" tabIndex="-1">
    `;
  }
  get _defaultClassName() {
    return "range";
  }
  get type() {
    return "range";
  }
  /* Own props */
  _onMouseDown(e) {
    this._input.classList.add(activeClassToken);
    setImmediate(() => this._input.focus());
  }
  _onTouchStart(e) {
    this._onMouseDown();
  }
  _onMouseUp(e) {
    this._input.classList.remove(activeClassToken);
  }
  _onTouchEnd(e) {
    this._onMouseUp(e);
  }
  _onDragstart(e) {
    e.consumed = true;
    e.gesture.stopPropagation();
    this._input.classList.add(activeClassToken);
    this.addEventListener("drag", this._onDrag);
  }
  _onDrag(e) {
    e.stopPropagation();
  }
  _onDragend(e) {
    this._input.classList.remove(activeClassToken);
    this.removeEventListener("drag", this._onDrag);
  }
  get _focusRing() {
    return this.children[1];
  }
  get _ratio() {
    const min = this._input.min === "" ? 0 : parseInt(this._input.min);
    const max = this._input.max === "" ? 100 : parseInt(this._input.max);
    return (this.value - min) / (max - min);
  }
  static get observedAttributes() {
    return ["disabled", ...BaseInputElement.observedAttributes];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "disabled") {
      this._updateDisabled(current);
    }
    super.attributeChangedCallback(name44, last, current);
  }
  /**
   * @param {boolean} disabled
   */
  _updateDisabled(disabled) {
    if (disabled) {
      this.classList.add("range--disabled");
    } else {
      this.classList.remove("range--disabled");
    }
  }
  connectedCallback() {
    this._setupListeners(true);
  }
  disconnectedCallback() {
    this._setupListeners(false);
  }
  _setupListeners(add) {
    const action = (add ? "add" : "remove") + "EventListener";
    util_default[action](this, "touchstart", this._onTouchStart, { passive: true });
    this[action]("mousedown", this._onMouseDown);
    this[action]("mouseup", this._onMouseUp);
    this[action]("touchend", this._onTouchEnd);
    this[action]("dragstart", this._onDragstart);
    this[action]("dragend", this._onDragend);
    this[action]("input", this._onInput);
  }
  /**
   * @attribute disabled
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property value
   * @type {Number}
   * @description
   *   [en]Current value.[/en]
   *   [ja][/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the range.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the range.[/en]
   *   [ja][/ja]
   */
};
elements_default.Range = RangeElement;
customElements.define("ons-range", RangeElement);

// node_modules/vue-onsenui/esm/components/VOnsRange.js
var name25 = "v-ons-range";
var script25 = {
  name: name25,
  mixins: [deriveEvents(name25), modelInputNumber, modifier]
};
var _hoisted_118 = ["modifier"];
function render26(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-range", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_118);
}
script25.render = render26;

// node_modules/onsenui/esm/elements/ons-radio.js
var scheme17 = {
  ".radio-button": "radio-button--*",
  ".radio-button__input": "radio-button--*__input",
  ".radio-button__checkmark": "radio-button--*__checkmark"
};
var RadioElement = class extends BaseCheckboxElement {
  get _scheme() {
    return scheme17;
  }
  get _defaultClassName() {
    return "radio-button";
  }
  get type() {
    return "radio";
  }
  /**
   * @attribute input-id
   * @type {String}
   * @description
   *  [en]Specify the "id" attribute of the inner `<input>` element. This is useful when using `<label for="...">` elements.[/en]
   *  [ja][/ja]
   */
  /**
   * @property value
   * @type {String}
   * @description
   *   [en]The current value of the radio button.[/en]
   *   [ja][/ja]
   */
  /**
   * @property checked
   * @type {Boolean}
   * @description
   *   [en]Whether the radio button is checked or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the radio button is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @method focus
   * @signature focus()
   * @description
   *   [en]Focuses the radio button.[/en]
   *   [ja][/ja]
   */
  /**
   * @method blur
   * @signature blur()
   * @description
   *   [en]Removes focus from the radio button.[/en]
   *   [ja][/ja]
   */
};
elements_default.Radio = RadioElement;
customElements.define("ons-radio", RadioElement);

// node_modules/vue-onsenui/esm/components/VOnsRadio.js
var name26 = "v-ons-radio";
var script26 = {
  name: name26,
  mixins: [deriveEvents(name26), modelRadio, modifier]
};
var _hoisted_119 = ["modifier"];
function render27(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-radio", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_119);
}
script26.render = render27;

// node_modules/onsenui/esm/elements/ons-fab.js
var defaultClassName9 = "fab";
var scheme18 = {
  "": "fab--*",
  ".fab__icon": "fab--*__icon"
};
var FabElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the button.[/en]
   *  [ja]ボタンの表現を指定します。[/ja]
   */
  /**
   * @attribute ripple
   * @description
   *  [en]If this attribute is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  /**
   * @property ripple
   * @type {Boolean}
   * @description
   *  [en]If this property is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute position
   * @type {String}
   * @description
   *  [en]The position of the button. Should be a string like `"bottom right"` or `"top left"`. If this attribute is not defined it will be displayed as an inline element.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if button should be disabled.[/en]
   *   [ja]ボタンを無効化する場合は指定します。[/ja]
   */
  constructor() {
    super();
    this._hide();
    this.classList.add(defaultClassName9);
    contentReady(this, () => {
      this._compile();
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    if (!util_default.findChild(this, ".fab__icon")) {
      const content = document.createElement("span");
      content.classList.add("fab__icon");
      util_default.arrayFrom(this.childNodes).forEach((element) => {
        if (!element.tagName || element.tagName.toLowerCase() !== "ons-ripple") {
          content.appendChild(element);
        }
      });
      this.appendChild(content);
    }
    this._updateRipple();
    ModifierUtil.initModifier(this, scheme18);
    this._updatePosition();
  }
  connectedCallback() {
    setImmediate(() => this._show());
  }
  static get observedAttributes() {
    return ["modifier", "ripple", "position", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName9, scheme18);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme18);
        break;
      case "ripple":
        this._updateRipple();
        break;
      case "position":
        this._updatePosition();
        break;
    }
  }
  _show() {
    if (!this._manuallyHidden) {
      this._toggle(true);
    }
  }
  _hide() {
    setImmediate(() => this._toggle(false));
  }
  _updateRipple() {
    util_default.updateRipple(this);
  }
  _updatePosition() {
    const position = this.getAttribute("position");
    this.classList.remove(
      "fab--top__left",
      "fab--bottom__right",
      "fab--bottom__left",
      "fab--top__right",
      "fab--top__center",
      "fab--bottom__center"
    );
    switch (position) {
      case "top right":
      case "right top":
        this.classList.add("fab--top__right");
        break;
      case "top left":
      case "left top":
        this.classList.add("fab--top__left");
        break;
      case "bottom right":
      case "right bottom":
        this.classList.add("fab--bottom__right");
        break;
      case "bottom left":
      case "left bottom":
        this.classList.add("fab--bottom__left");
        break;
      case "center top":
      case "top center":
        this.classList.add("fab--top__center");
        break;
      case "center bottom":
      case "bottom center":
        this.classList.add("fab--bottom__center");
        break;
      default:
        break;
    }
  }
  /**
   * @method show
   * @signature show()
   * @description
   *  [en]Show the floating action button.[/en]
   *  [ja][/ja]
   */
  show() {
    this.toggle(true);
  }
  /**
   * @method hide
   * @signature hide()
   * @description
   *  [en]Hide the floating action button.[/en]
   *  [ja][/ja]
   */
  hide() {
    this.toggle(false);
  }
  /**
   * @method toggle
   * @signature toggle()
   * @description
   *   [en]Toggle the visibility of the button.[/en]
   *   [ja][/ja]
   */
  toggle(action = !this.visible) {
    this._manuallyHidden = !action;
    this._toggle(action);
  }
  _toggle(action = !this.visible) {
    const isBottom = (this.getAttribute("position") || "").indexOf("bottom") >= 0;
    const translate = isBottom ? `translate3d(0px, -${util_default.globals.fabOffset || 0}px, 0px)` : "";
    styler_default(this, { transform: `${translate} scale(${Number(action)})` });
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property visible
   * @readonly
   * @type {Boolean}
   * @description
   *   [en]Whether the element is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  get visible() {
    return this.style.transform.indexOf("scale(0)") === -1 && this.style.display !== "none";
  }
};
util_default.defineBooleanProperties(FabElement, ["disabled", "ripple"]);
elements_default.Fab = FabElement;
customElements.define("ons-fab", FabElement);

// node_modules/vue-onsenui/esm/components/VOnsFab.js
var name27 = "v-ons-fab";
var script27 = {
  name: name27,
  mixins: [deriveEvents(name27), hidable, modifier]
};
var _hoisted_120 = ["modifier"];
function render28(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-fab", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_120);
}
script27.render = render28;

// node_modules/onsenui/esm/elements/ons-speed-dial-item.js
var defaultClassName10 = "fab fab--mini speed-dial__item";
var scheme19 = {
  "": "fab--* speed-dial__item--*"
};
var SpeedDialItemElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the component.[/en]
   *   [ja]このコンポーネントの表現を指定します。[/ja]
   */
  /**
   * @attribute ripple
   * @description
   *  [en]If this attribute is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  /**
   * @property ripple
   * @type {Boolean}
   * @description
   *  [en]If this property is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  constructor() {
    super();
    this._compile();
    this._boundOnClick = this._onClick.bind(this);
  }
  static get observedAttributes() {
    return ["modifier", "ripple", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName10, scheme19);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme19);
        util_default.addModifier(this, "mini");
        break;
      case "ripple":
        this._updateRipple();
    }
  }
  connectedCallback() {
    this.addEventListener("click", this._boundOnClick, false);
  }
  disconnectedCallback() {
    this.removeEventListener("click", this._boundOnClick, false);
  }
  _updateRipple() {
    util_default.updateRipple(this);
  }
  _onClick(e) {
    e.stopPropagation();
  }
  _compile() {
    autostyle_default.prepare(this);
    defaultClassName10.split(/\s+/).forEach((token) => this.classList.add(token));
    util_default.addModifier(this, "mini");
    this._updateRipple();
    ModifierUtil.initModifier(this, scheme19);
  }
};
util_default.defineBooleanProperties(SpeedDialItemElement, ["ripple"]);
elements_default.SpeedDialItem = SpeedDialItemElement;
customElements.define("ons-speed-dial-item", SpeedDialItemElement);

// node_modules/vue-onsenui/esm/components/VOnsSpeedDialItem.js
var name28 = "v-ons-speed-dial-item";
var script28 = {
  name: name28,
  mixins: [deriveEvents(name28), modifier]
};
var _hoisted_121 = ["modifier"];
function render29(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-speed-dial-item", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_121);
}
script28.render = render29;

// node_modules/onsenui/esm/elements/ons-dialog/animator.js
var DialogAnimator = class extends BaseAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  show(dialog2, done) {
    done();
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  hide(dialog2, done) {
    done();
  }
};
var AndroidDialogAnimator = class extends DialogAnimator {
  constructor({ timing = "ease-in-out", delay = 0, duration = 0.3 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 0 }, { opacity: 1 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -60%, 0)", opacity: 0 },
        { transform: "translate3d(-50%, -50%, 0)", opacity: 1 }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 1 }, { opacity: 0 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0)", opacity: 1 },
        { transform: "translate3d(-50%, -60%, 0)", opacity: 0 }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
};
var IOSDialogAnimator = class extends DialogAnimator {
  constructor({ timing = "ease-in-out", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
    this.bodyHeight = document.body.clientHeight;
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 0 }, { opacity: 1 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: `translate3d(-50%, ${this.bodyHeight / 2 - 1}px, 0)` },
        { transform: "translate3d(-50%, -50%, 0)" }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 1 }, { opacity: 0 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0)" },
        { transform: `translate3d(-50%, ${this.bodyHeight / 2 - 1}px, 0)` }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
};
var SlideDialogAnimator = class extends DialogAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .4, 1)", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
    this.bodyHeight = document.body.clientHeight;
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 0 }, { opacity: 1 }),
      animit_default(dialog2._dialog, this.def).default(
        // FIXME: This should avoid Forced Synchronous Layout. Otherwise, fade animation of mask will be broken.
        { transform: `translate3d(-50%, ${-(this.bodyHeight / 2) + 1 - dialog2._dialog.clientHeight}px, 0)` },
        { transform: "translate3d(-50%, -50%, 0)" }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 1 }, { opacity: 0 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0)" },
        // FIXME: This should avoid Forced Synchronous Layout. Otherwise, fade animation of mask will be broken.
        { transform: `translate3d(-50%, ${-(this.bodyHeight / 2) + 1 - dialog2._dialog.clientHeight}px, 0)` }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/base/base-dialog.js
var BaseDialogElement = class _BaseDialogElement extends BaseElement {
  get _scheme() {
    util_default.throwMember();
  }
  _updateAnimatorFactory() {
    util_default.throwMember();
  }
  _toggleStyle(shouldShow) {
    this.style.display = shouldShow ? "block" : "none";
  }
  constructor() {
    super();
    if (this.constructor === _BaseDialogElement) {
      util_default.throwAbstract();
    }
    this._visible = false;
    this._doorLock = new DoorLock();
    this._cancel = this._cancel.bind(this);
    this._selfCamelName = util_default.camelize(this.tagName.slice(4));
    this._defaultDBB = (e) => this.cancelable ? this._cancel() : e.callParentHandler();
    this._animatorFactory = this._updateAnimatorFactory();
  }
  get onDeviceBackButton() {
    return this._backButtonHandler;
  }
  set onDeviceBackButton(callback) {
    if (this._backButtonHandler) {
      this._backButtonHandler.destroy();
    }
    this._backButtonCallback = callback;
    this._backButtonHandler = device_back_button_dispatcher_default.createHandler(this, callback);
  }
  _cancel() {
    if (this.cancelable && !this._running) {
      this._running = true;
      this.hide().then(
        () => {
          this._running = false;
          util_default.triggerElementEvent(this, "dialogcancel");
          util_default.triggerElementEvent(this, "dialog-cancel");
        },
        () => this._running = false
      );
    }
  }
  show(...args) {
    return this._setVisible(true, ...args).then((dialog2) => {
      this.visible = true;
      return dialog2;
    });
  }
  hide(...args) {
    return this._setVisible(false, ...args).then((dialog2) => {
      this.visible = false;
      return dialog2;
    });
  }
  toggle(...args) {
    return this._setVisible(!this.visible, ...args).then((dialog2) => {
      this.visible = this._visible;
      return dialog2;
    });
  }
  _setVisible(shouldShow, options = {}) {
    const action = shouldShow ? "show" : "hide";
    options = { ...options };
    options.animationOptions = util_default.extend(
      options.animationOptions || {},
      this.animationOptions
    );
    let canceled = false;
    util_default.triggerElementEvent(this, `pre${action}`, {
      // preshow prehide
      [this._selfCamelName]: this,
      cancel: () => canceled = true
    });
    if (canceled) {
      return Promise.reject(`Canceled in pre${action} event.`);
    }
    return new Promise((resolve) => {
      this._doorLock.waitUnlock(() => {
        const unlock = this._doorLock.lock();
        const animator = this._animatorFactory.newAnimator(options);
        shouldShow && this._toggleStyle(true, options);
        this._visible = shouldShow;
        util_default.iosPageScrollFix(shouldShow);
        contentReady(this, () => {
          animator[action](this, () => {
            !shouldShow && this._toggleStyle(false, options);
            unlock();
            util_default.propagateAction(this, "_" + action);
            util_default.triggerElementEvent(this, "post" + action, { [this._selfCamelName]: this });
            if (options.callback instanceof Function) {
              options.callback(this);
            }
            resolve(this);
          });
        });
      });
    });
  }
  get maskColor() {
    return this.getAttribute("mask-color");
  }
  set maskColor(value) {
    if (value === null || value === void 0) {
      this.removeAttribute("mask-color");
    } else {
      this.setAttribute("mask-color", value);
    }
  }
  get animationOptions() {
    return AnimatorFactory.parseAnimationOptionsString(this.getAttribute("animation-options"));
  }
  set animationOptions(value) {
    if (value === void 0 || value === null) {
      this.removeAttribute("animation-options");
    } else {
      this.setAttribute("animation-options", JSON.stringify(value));
    }
  }
  _updateMask() {
    contentReady(this, () => {
      if (this._mask) {
        this._mask.style.backgroundColor = this.maskColor;
      }
    });
  }
  _updateAnimation() {
    this._animatorFactory = this._updateAnimatorFactory();
  }
  connectedCallback() {
    if (typeof this._backButtonCallback === "function") {
      this.onDeviceBackButton = this._backButtonCallback;
    } else if (typeof this._defaultDBB === "function") {
      this.onDeviceBackButton = this._defaultDBB.bind(this);
    }
    contentReady(this, () => {
      if (this._mask) {
        this._mask.addEventListener("click", this._cancel, false);
      }
    });
  }
  disconnectedCallback() {
    if (this._backButtonHandler) {
      this._backButtonHandler.destroy();
      this._backButtonHandler = null;
    }
    if (this._mask) {
      this._mask.removeEventListener("click", this._cancel, false);
    }
  }
  static get observedAttributes() {
    return ["modifier", "animation", "mask-color", "visible"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, this._scheme);
        break;
      case "animation":
        this._updateAnimation();
        break;
      case "mask-color":
        this._updateMask();
        break;
      case "visible":
        if (this.visible !== this._visible) {
          this._updateMask();
          this._updateAnimation();
          contentReady(this, () => {
            this._setVisible(this.visible);
          });
        }
        break;
    }
  }
  static get events() {
    return ["preshow", "postshow", "prehide", "posthide", "dialogcancel", "dialog-cancel"];
  }
};
util_default.defineBooleanProperties(BaseDialogElement, ["visible", "disabled", "cancelable"]);

// node_modules/onsenui/esm/elements/ons-dialog.js
var scheme20 = {
  ".dialog": "dialog--*",
  ".dialog-container": "dialog-container--*",
  ".dialog-mask": "dialog-mask--*"
};
var _animatorDict3 = {
  "default": function() {
    return platform_default.isAndroid() ? AndroidDialogAnimator : IOSDialogAnimator;
  },
  "slide": SlideDialogAnimator,
  "none": DialogAnimator
};
var DialogElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   * [en]Fired just before the dialog is displayed.[/en]
   * [ja]ダイアログが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.dialog
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the dialog from being shown.[/en]
   *   [ja]この関数を実行すると、ダイアログの表示がキャンセルされます。[/ja]
   */
  /**
   * @event postshow
   * @description
   * [en]Fired just after the dialog is displayed.[/en]
   * [ja]ダイアログが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.dialog
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   * [en]Fired just before the dialog is hidden.[/en]
   * [ja]ダイアログが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.dialog
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the dialog from being hidden.[/en]
   *   [ja]この関数を実行すると、ダイアログの非表示がキャンセルされます。[/ja]
   */
  /**
   * @event posthide
   * @description
   * [en]Fired just after the dialog is hidden.[/en]
   * [ja]ダイアログが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.dialog
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event dialogcancel
   * @description
   * [en]Fired when the dialog is canceled.[/en]
   * [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the dialog.[/en]
   *  [ja]ダイアログの表現を指定します。[/ja]
   */
  /**
   * @attribute cancelable
   * @description
   *  [en]If this attribute is set the dialog can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute disabled
   * @description
   *  [en]If this attribute is set the dialog is disabled.[/en]
   *  [ja]この属性がある時、ダイアログはdisabled状態になります。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the dialog. Can be either `"none"` or `"default"`.[/en]
   *  [ja]ダイアログを表示する際のアニメーション名を指定します。"none"もしくは"default"を指定できます。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute mask-color
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is `"rgba(0, 0, 0, 0.2)"`.[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
  }
  get _scheme() {
    return scheme20;
  }
  get _mask() {
    return util_default.findChild(this, ".dialog-mask");
  }
  get _dialog() {
    return util_default.findChild(this, ".dialog");
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict3,
      baseClass: DialogAnimator,
      baseClassName: "DialogAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    this.style.display = "none";
    this.style.zIndex = 10001;
    if (!this._dialog) {
      const dialog2 = document.createElement("div");
      dialog2.classList.add("dialog");
      const container = document.createElement("div");
      container.classList.add("dialog-container");
      while (this.firstChild) {
        container.appendChild(this.firstChild);
      }
      dialog2.appendChild(container);
      this.appendChild(dialog2);
    }
    if (!this._mask) {
      const mask = document.createElement("div");
      mask.classList.add("dialog-mask");
      this.insertBefore(mask, this.firstChild);
    }
    this._dialog.style.zIndex = 20001;
    this._mask.style.zIndex = 2e4;
    this.setAttribute("status-bar-fill", "");
    ModifierUtil.initModifier(this, this._scheme);
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  /**
   * @method show
   * @signature show([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"slide"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "slide"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}` [/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the dialog has been revealed.[/en]
   *   [ja]ダイアログが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *  [en]Show the dialog.[/en]
   *  [ja]ダイアログを開きます。[/ja]
   * @return {Promise} Resolves to the displayed element.
   */
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"slide"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "slide"のいずれかを指定できます。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`[/ja]
   * @param {Function} [options.callback]
   *   [en]This functions is called after the dialog has been hidden.[/en]
   *   [ja]ダイアログが隠れた後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Hide the dialog.[/en]
   *   [ja]ダイアログを閉じます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja][/ja]
   */
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property cancelable
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is cancelable or not. A cancelable dialog can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *   [ja][/ja]
   */
  /**
   * @property maskColor
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is "rgba(0, 0, 0, 0.2)".[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @param {String} name
   * @param {DialogAnimator} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof DialogAnimator)) {
      util_default.throwAnimator("Dialog");
    }
    _animatorDict3[name44] = Animator;
  }
  static get animators() {
    return _animatorDict3;
  }
  static get DialogAnimator() {
    return DialogAnimator;
  }
};
elements_default.Dialog = DialogElement;
customElements.define("ons-dialog", DialogElement);

// node_modules/vue-onsenui/esm/components/VOnsDialog.js
var name29 = "v-ons-dialog";
var script29 = {
  name: name29,
  mixins: [deriveEvents(name29), hasOptions, dialog, deriveDBB, portal, modifier]
};
var _hoisted_122 = ["modifier"];
function render30(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-dialog", mergeProps({ modifier: _ctx.normalizedModifier }, _ctx.orderedProps), [
    renderSlot(_ctx.$slots, "default")
  ], 16, _hoisted_122);
}
script29.render = render30;

// node_modules/onsenui/esm/ons/iphonex-patch.js
var iPhoneXPatch = {};
iPhoneXPatch.isIPhoneXPortraitPatchActive = () => {
  return document.documentElement.getAttribute("onsflag-iphonex-portrait") != null && window.innerWidth < window.innerHeight;
};
iPhoneXPatch.isIPhoneXLandscapePatchActive = () => {
  return document.documentElement.getAttribute("onsflag-iphonex-landscape") != null && window.innerWidth >= window.innerHeight;
};
iPhoneXPatch.getSafeAreaLengths = () => {
  let safeAreaLengths;
  if (iPhoneXPatch.isIPhoneXPortraitPatchActive()) {
    safeAreaLengths = {
      top: 44,
      right: 0,
      bottom: 34,
      left: 0
    };
  } else if (iPhoneXPatch.isIPhoneXLandscapePatchActive()) {
    safeAreaLengths = {
      top: 0,
      right: 44,
      bottom: 21,
      left: 44
    };
  } else {
    safeAreaLengths = {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0
    };
  }
  return safeAreaLengths;
};
iPhoneXPatch.getSafeAreaDOMRect = () => {
  let safeAreaRect;
  if (iPhoneXPatch.isIPhoneXPortraitPatchActive()) {
    safeAreaRect = {
      x: 0,
      y: 44,
      /* 0 + 44 (top safe area) */
      width: window.innerWidth,
      height: window.innerHeight - 78
      /* height - 44 (top safe area) - 34 (bottom safe area) */
    };
  } else if (iPhoneXPatch.isIPhoneXLandscapePatchActive()) {
    safeAreaRect = {
      x: 44,
      /* 0 + 44 (left safe area) */
      y: 0,
      width: window.innerWidth - 88,
      /* width - 44 (left safe area) - 34 (right safe area) */
      height: window.innerHeight - 21
      /* height - 21 (bottom safe area) */
    };
  } else {
    safeAreaRect = {
      x: 0,
      y: 0,
      width: window.innerWidth,
      height: window.innerHeight
    };
  }
  return {
    ...safeAreaRect,
    left: safeAreaRect.x,
    top: safeAreaRect.y,
    right: safeAreaRect.x + safeAreaRect.width,
    bottom: safeAreaRect.y + safeAreaRect.height
  };
};
var iphonex_patch_default = iPhoneXPatch;

// node_modules/onsenui/esm/elements/ons-action-sheet/animator.js
var ActionSheetAnimator = class extends BaseAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  show(dialog2, done) {
    done();
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  hide(dialog2, done) {
    done();
  }
};
var MDActionSheetAnimator = class extends ActionSheetAnimator {
  constructor({ timing = "ease", delay = 0, duration = 0.4 } = {}) {
    super({ timing, delay, duration });
    this.maskTiming = "linear";
    this.maskDuration = 0.2;
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    animit_default.runAll(
      animit_default(dialog2._mask).queue({ opacity: 0 }).wait(this.delay).queue({ opacity: 1 }, {
        duration: this.maskDuration,
        timing: this.maskTiming
      }),
      animit_default(dialog2._sheet, this.def).default(
        { transform: `translate3d(0, 80%, 0)`, opacity: 0 },
        { transform: "translate3d(0, 0, 0)", opacity: 1 }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    animit_default.runAll(
      animit_default(dialog2._mask).queue({ opacity: 1 }).wait(this.delay).queue({ opacity: 0 }, {
        duration: this.maskDuration,
        timing: this.maskTiming
      }),
      animit_default(dialog2._sheet, this.def).default(
        { transform: "translate3d(0, 0, 0)", opacity: 1 },
        { transform: `translate3d(0, 80%, 0)`, opacity: 0 }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
};
var IOSActionSheetAnimator = class extends ActionSheetAnimator {
  constructor({ timing = "ease", delay = 0, duration = 0.3 } = {}) {
    super({ timing, delay, duration });
    this.maskTiming = "linear";
    this.maskDuration = 0.2;
    if (iphonex_patch_default.isIPhoneXPortraitPatchActive()) {
      this.liftAmount = "calc(100% + 48px)";
    } else if (iphonex_patch_default.isIPhoneXLandscapePatchActive()) {
      this.liftAmount = "calc(100% + 33px)";
    } else {
      this.liftAmount = document.body.clientHeight / 2 - 1 + "px";
    }
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    animit_default.runAll(
      animit_default(dialog2._mask).queue({ opacity: 0 }).wait(this.delay).queue({ opacity: 1 }, {
        duration: this.maskDuration,
        timing: this.maskTiming
      }),
      animit_default(dialog2._sheet, this.def).default(
        { transform: `translate3d(0, ${this.liftAmount}, 0)` },
        { transform: "translate3d(0, 0, 0)" }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    animit_default.runAll(
      animit_default(dialog2._mask).queue({ opacity: 1 }).wait(this.delay).queue({ opacity: 0 }, {
        duration: this.maskDuration,
        timing: this.maskTiming
      }),
      animit_default(dialog2._sheet, this.def).default(
        { transform: "translate3d(0, 0, 0)" },
        { transform: `translate3d(0, ${this.liftAmount}, 0)` }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-action-sheet.js
var scheme21 = {
  ".action-sheet": "action-sheet--*",
  ".action-sheet-mask": "action-sheet-mask--*",
  ".action-sheet-title": "action-sheet-title--*"
};
var _animatorDict4 = {
  "default": function() {
    return platform_default.isAndroid() ? MDActionSheetAnimator : IOSActionSheetAnimator;
  },
  "none": ActionSheetAnimator
};
var ActionSheetElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   * [en]Fired just before the action sheet is displayed.[/en]
   * [ja]ダイアログが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.actionSheet
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the action sheet from being shown.[/en]
   *   [ja]この関数を実行すると、ダイアログの表示がキャンセルされます。[/ja]
   */
  /**
   * @event postshow
   * @description
   * [en]Fired just after the action sheet is displayed.[/en]
   * [ja]ダイアログが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.actionSheet
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   * [en]Fired just before the action sheet is hidden.[/en]
   * [ja]ダイアログが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.actionSheet
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the action sheet from being hidden.[/en]
   *   [ja]この関数を実行すると、ダイアログの非表示がキャンセルされます。[/ja]
   */
  /**
   * @event posthide
   * @description
   * [en]Fired just after the action sheet is hidden.[/en]
   * [ja]ダイアログが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.actionSheet
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event dialogcancel
   * @description
   * [en]Fired when the action sheet is canceled.[/en]
   * [ja][/ja]
   */
  /**
   * @attribute title
   * @type {String}
   * @description
   *  [en]Optional title of the action sheet. A new element will be created containing this string.[/en]
   *  [ja]アクションシートのタイトルを指定します。ここで指定した文字列を含む新しい要素が作成されます。[/ja]
   */
  /**
   * @property title
   * @type {String}
   * @description
   *  [en]Optional title of the action sheet. A new element will be created containing this string.[/en]
   *  [ja]アクションシートのタイトルを指定します。ここで指定した文字列を含む新しい要素が作成されます。[/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the action sheet.[/en]
   *  [ja]ダイアログの表現を指定します。[/ja]
   */
  /**
   * @attribute cancelable
   * @description
   *  [en]If this attribute is set the action sheet can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *  [ja]この属性が設定されると、アクションシートの背景やAndroidデバイスのバックボタンを推すことでアクションシートが閉じるようになります。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *  [en]If this attribute is set the action sheet is disabled.[/en]
   *  [ja]この属性がある時、ダイアログはdisabled状態になります。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the action sheet. Can be either `"none"` or `"default"`.[/en]
   *  [ja]ダイアログを表示する際のアニメーション名を指定します。"none"もしくは"default"を指定できます。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute mask-color
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is `"rgba(0, 0, 0, 0.2)"`.[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the action sheet is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
  }
  get _scheme() {
    return scheme21;
  }
  get _mask() {
    return util_default.findChild(this, ".action-sheet-mask");
  }
  get _sheet() {
    return util_default.findChild(this, ".action-sheet");
  }
  get _title() {
    return this.querySelector(".action-sheet-title");
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict4,
      baseClass: ActionSheetAnimator,
      baseClassName: "ActionSheetAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    this.style.display = "none";
    this.style.zIndex = 10001;
    if (!this._sheet) {
      const sheet = document.createElement("div");
      sheet.classList.add("action-sheet");
      while (this.firstChild) {
        sheet.appendChild(this.firstChild);
      }
      this.appendChild(sheet);
    }
    if (!this._title) {
      const title = document.createElement("div");
      title.classList.add("action-sheet-title");
      if (this.title) {
        title.innerHTML = this.title;
      } else {
        title.hidden = true;
      }
      this._sheet.insertBefore(title, this._sheet.firstChild);
    }
    if (!this._mask) {
      const mask = document.createElement("div");
      mask.classList.add("action-sheet-mask");
      this.insertBefore(mask, this.firstChild);
    }
    this._sheet.style.zIndex = 20001;
    this._mask.style.zIndex = 2e4;
    ModifierUtil.initModifier(this, this._scheme);
  }
  _updateTitle() {
    if (this._title) {
      if (!this.title) {
        this._title.hidden = true;
      } else {
        this._title.innerHTML = this.title;
        this._title.hidden = false;
      }
    }
  }
  get title() {
    return this.getAttribute("title");
  }
  set title(value) {
    if (value === null || value === void 0) {
      this.removeAttribute("title");
    } else {
      this.setAttribute("title", value);
    }
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  /**
   * @property maskColor
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is "rgba(0, 0, 0, 0.2)".[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @method show
   * @signature show([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"slide"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "slide"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}` [/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the action sheet has been revealed.[/en]
   *   [ja]ダイアログが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *  [en]Show the action sheet.[/en]
   *  [ja]ダイアログを開きます。[/ja]
   * @return {Promise} Resolves to the displayed element.
   */
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"slide"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "slide"のいずれかを指定できます。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`[/ja]
   * @param {Function} [options.callback]
   *   [en]This functions is called after the action sheet has been hidden.[/en]
   *   [ja]ダイアログが隠れた後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Hide the action sheet.[/en]
   *   [ja]ダイアログを閉じます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja]隠れた要素を解決します。[/ja]
   */
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the action sheet is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the action sheet is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property cancelable
   * @type {Boolean}
   * @description
   *   [en]Whether the action sheet is cancelable or not. A cancelable action sheet can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *   [ja]アクションシートがキャンセル可能かどうかを設定します。キャンセル可能なアクションシートは、背景をタップしたりAndroidデバイスのバックボタンを推すことで閉じるようになります。[/ja]
   */
  static get observedAttributes() {
    return [...super.observedAttributes, "title"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "title") {
      this._updateTitle();
    } else {
      super.attributeChangedCallback(name44, last, current);
    }
  }
  /**
   * @param {String} name
   * @param {ActionSheetAnimator} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof ActionSheetAnimator)) {
      util_default.throwAnimator("ActionSheet");
    }
    _animatorDict4[name44] = Animator;
  }
  static get animators() {
    return _animatorDict4;
  }
  static get ActionSheetAnimator() {
    return ActionSheetAnimator;
  }
};
elements_default.ActionSheet = ActionSheetElement;
customElements.define("ons-action-sheet", ActionSheetElement);

// node_modules/vue-onsenui/esm/components/VOnsActionSheet.js
var name30 = "v-ons-action-sheet";
var script30 = {
  name: name30,
  mixins: [deriveEvents(name30), hasOptions, dialog, deriveDBB, portal, modifier]
};
var _hoisted_123 = ["modifier"];
function render31(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-action-sheet", mergeProps({ modifier: _ctx.normalizedModifier }, _ctx.orderedProps), [
    renderSlot(_ctx.$slots, "default")
  ], 16, _hoisted_123);
}
script30.render = render31;

// node_modules/onsenui/esm/elements/ons-action-sheet-button.js
var ActionSheetButtonElement = class extends BaseButtonElement {
  /**
   * @attribute icon
   * @type {String}
   * @description
   *  [en]Creates an `ons-icon` component with this string. Only visible on Android. Check [See also](#seealso) section for more information.[/en]
   *  [ja]`ons-icon`コンポーネントを悪性します。Androidでのみ表示されます。[/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the action sheet button.[/en]
   *   [ja]アクションシートボタンの見た目を設定します。[/ja]
   */
  get _scheme() {
    return {
      "": "action-sheet-button--*",
      ".action-sheet-icon": "action-sheet-icon--*"
    };
  }
  get _defaultClassName() {
    return "action-sheet-button";
  }
  get _rippleOpt() {
    return void 0;
  }
};
elements_default.ActionSheetButton = ActionSheetButtonElement;
customElements.define("ons-action-sheet-button", ActionSheetButtonElement);

// node_modules/vue-onsenui/esm/components/VOnsActionSheetButton.js
var name31 = "v-ons-action-sheet-button";
var script31 = {
  name: name31,
  mixins: [deriveEvents(name31), modifier]
};
var _hoisted_124 = ["modifier"];
function render32(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-action-sheet-button", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_124);
}
script31.render = render32;

// node_modules/onsenui/esm/elements/ons-modal/animator.js
var ModalAnimator = class extends BaseAnimator {
  /**
   * @param {Object} options
   * @param {String} options.timing
   * @param {Number} options.duration
   * @param {Number} options.delay
   */
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  show(modal, callback) {
    callback();
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  hide(modal, callback) {
    callback();
  }
};

// node_modules/onsenui/esm/elements/ons-modal/fade-animator.js
var FadeModalAnimator = class extends ModalAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.3 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  show(modal, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(modal, this.def).default({ opacity: 0 }, { opacity: 1 }).queue((done) => {
      callback();
      done();
    }).play();
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  hide(modal, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(modal, this.def).default({ opacity: 1 }, { opacity: 0 }).queue((done) => {
      callback();
      done();
    }).play();
  }
};

// node_modules/onsenui/esm/elements/ons-modal/lift-animator.js
var LiftModalAnimator = class extends ModalAnimator {
  constructor({ timing = "cubic-bezier( .1, .7, .1, 1)", delay = 0, duration = 0.4 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  show(modal, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(modal, this.def).default(
      { transform: "translate3d(0, 100%, 0)" },
      { transform: "translate3d(0, 0, 0)" }
    ).queue((done) => {
      callback();
      done();
    }).play();
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  hide(modal, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(modal, this.def).default(
      { transform: "translate3d(0, 0, 0)" },
      { transform: "translate3d(0, 100%, 0)" }
    ).queue((done) => {
      callback();
      done();
    }).play();
  }
};

// node_modules/onsenui/esm/elements/ons-modal.js
var scheme22 = {
  "": "modal--*",
  "modal__content": "modal--*__content"
};
var defaultClassName11 = "modal";
var _animatorDict5 = {
  "default": ModalAnimator,
  "fade": FadeModalAnimator,
  "lift": LiftModalAnimator,
  "none": ModalAnimator
};
var ModalElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   * [en]Fired just before the modal is displayed.[/en]
   * [ja]モーダルが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.modal
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the modal from being shown.[/en]
   *   [ja]この関数を実行すると、ダイアログの表示がキャンセルされます。[/ja]
   */
  /**
   * @event postshow
   * @description
   * [en]Fired just after the modal is displayed.[/en]
   * [ja]モーダルが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.modal
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   * [en]Fired just before the modal is hidden.[/en]
   * [ja]モーダルが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.modal
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute this function to stop the modal from being hidden.[/en]
   *   [ja]この関数を実行すると、ダイアログの非表示がキャンセルされます。[/ja]
   */
  /**
   * @event posthide
   * @description
   * [en]Fired just after the modal is hidden.[/en]
   * [ja]モーダルが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.modal
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the modal. Can be either `"none"`, `"fade"` or `"lift"`.[/en]
   *  [ja]モーダルを表示する際のアニメーション名を指定します。"none"もしくは"fade","lift"を指定できます。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. <code>{duration: 0.2, delay: 1, timing: 'ease-in'}</code>[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the modal is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    this._defaultDBB = () => void 0;
    contentReady(this, () => this._compile());
  }
  get _scheme() {
    return scheme22;
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict5,
      baseClass: ModalAnimator,
      baseClassName: "ModalAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  _compile() {
    this.style.display = "none";
    this.style.zIndex = 10001;
    this.classList.add(defaultClassName11);
    if (!util_default.findChild(this, ".modal__content")) {
      const content = document.createElement("div");
      content.classList.add("modal__content");
      while (this.childNodes[0]) {
        const node = this.childNodes[0];
        this.removeChild(node);
        content.insertBefore(node, null);
      }
      this.appendChild(content);
    }
    ModifierUtil.initModifier(this, this._scheme);
  }
  _toggleStyle(shouldShow) {
    this.style.display = shouldShow ? "table" : "none";
  }
  connectedCallback() {
    super.connectedCallback();
  }
  disconnectedCallback() {
    super.disconnectedCallback();
  }
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the element is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @method show
   * @signature show([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"fade"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "fade"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the modal has been revealed.[/en]
   *   [ja]モーダルが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Show modal.[/en]
   *   [ja]モーダルを表示します。[/ja]
   * @return {Promise}
   *   [en]Resolves to the displayed element[/en]
   *   [ja][/ja]
   */
  /**
   * @method toggle
   * @signature toggle([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"fade"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "fade"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the modal has been revealed.[/en]
   *   [ja]モーダルが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Toggle modal visibility.[/en]
   *   [ja]モーダルの表示を切り替えます。[/ja]
   */
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"none"` and `"fade"`.[/en]
   *   [ja]アニメーション名を指定します。"none", "fade"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the modal has been revealed.[/en]
   *   [ja]モーダルが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Hide modal.[/en]
   *   [ja]モーダルを非表示にします。[/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja][/ja]
   */
  static get observedAttributes() {
    return [...super.observedAttributes, "class"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "class") {
      util_default.restoreClass(this, defaultClassName11, scheme22);
    } else {
      super.attributeChangedCallback(name44, last, current);
    }
  }
  /**
   * @param {String} name
   * @param {Function} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof ModalAnimator)) {
      util_default.throwAnimator("Modal");
    }
    _animatorDict5[name44] = Animator;
  }
  static get animators() {
    return _animatorDict5;
  }
  static get ModalAnimator() {
    return ModalAnimator;
  }
};
elements_default.Modal = ModalElement;
customElements.define("ons-modal", ModalElement);

// node_modules/vue-onsenui/esm/components/VOnsModal.js
var name32 = "v-ons-modal";
var script32 = {
  name: name32,
  mixins: [deriveEvents(name32), hidable, hasOptions, deriveDBB, portal, modifier]
};
var _hoisted_125 = ["modifier"];
function render33(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-modal", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_125);
}
script32.render = render33;

// node_modules/onsenui/esm/elements/ons-toast/animator.js
var ToastAnimator = class extends BaseAnimator {
  /**
   * @param {Object} options
   * @param {String} options.timing
   * @param {Number} options.duration
   * @param {Number} options.delay
   */
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  show(modal, callback) {
    callback();
  }
  /**
   * @param {HTMLElement} modal
   * @param {Function} callback
   */
  hide(modal, callback) {
    callback();
  }
};

// node_modules/onsenui/esm/elements/ons-toast/fade-animator.js
var FadeToastAnimator = class extends ToastAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.3 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  show(toast, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(toast, this.def).default({ opacity: 0 }, { opacity: 1 }).queue((done) => {
      callback();
      done();
    }).play();
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  hide(toast, callback) {
    callback = callback ? callback : function() {
    };
    animit_default(toast, this.def).default({ opacity: 1 }, { opacity: 0 }).queue((done) => {
      callback();
      done();
    }).play();
  }
};

// node_modules/onsenui/esm/elements/ons-toast/ascend-animator.js
var AscendToastAnimator = class extends ToastAnimator {
  constructor({ timing = "ease", delay = 0, duration = 0.25 } = {}) {
    super({ timing, delay, duration });
    this.messageDelay = this.duration * 0.4 + this.delay;
    if (platform_default.isAndroid()) {
      this.ascension = 48;
    } else {
      if (iphonex_patch_default.isIPhoneXPortraitPatchActive()) {
        this.ascension = 98;
      } else if (iphonex_patch_default.isIPhoneXLandscapePatchActive()) {
        this.ascension = 85;
      } else {
        this.ascension = 64;
      }
    }
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  show(toast, callback) {
    toast = toast._toast;
    util_default.globals.fabOffset = this.ascension;
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: `translate3d(0, ${this.ascension}px, 0)` },
        { transform: "translate3d(0, 0, 0)" }
      ).queue((done) => {
        callback && callback();
        done();
      }),
      animit_default(this._getFabs()).wait(this.delay).queue({ transform: `translate3d(0, -${this.ascension}px, 0) scale(1)` }, this.def),
      animit_default(util_default.arrayFrom(toast.children), this.def).default({ opacity: 0 }, { opacity: 1 })
    );
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  hide(toast, callback) {
    toast = toast._toast;
    util_default.globals.fabOffset = 0;
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: "translate3d(0, 0, 0)" },
        { transform: `translate3d(0, ${this.ascension}px, 0)` }
      ).queue((done) => {
        callback && callback();
        done();
      }),
      animit_default(this._getFabs(), this.def).wait(this.delay).queue({ transform: `translate3d(0, 0, 0) scale(1)` }, this.def),
      animit_default(util_default.arrayFrom(toast.children), this.def).default({ opacity: 1 }, { opacity: 0 })
    );
  }
  _getFabs() {
    return util_default.arrayFrom(document.querySelectorAll("ons-fab[position~=bottom], ons-speed-dial[position~=bottom]")).filter((fab) => fab.visible);
  }
};

// node_modules/onsenui/esm/elements/ons-toast/lift-animator.js
var LiftToastAnimator = class extends ToastAnimator {
  constructor({ timing = "ease", delay = 0, duration = 0.35 } = {}) {
    super({ timing, delay, duration });
    this.bodyHeight = document.body.clientHeight;
    if (iphonex_patch_default.isIPhoneXPortraitPatchActive()) {
      this.liftAmount = "calc(100% + 34px)";
    } else if (iphonex_patch_default.isIPhoneXLandscapePatchActive()) {
      this.liftAmount = "calc(100% + 21px)";
    } else {
      this.liftAmount = "100%";
    }
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  show(toast, callback) {
    toast = toast._toast;
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: `translate3d(0, ${this.liftAmount}, 0)`, opacity: 0 },
        { transform: "translate3d(0, 0, 0)", opacity: 1 }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  hide(toast, callback) {
    toast = toast._toast;
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: "translate3d(0, 0, 0)", opacity: 1 },
        { transform: `translate3d(0, ${this.liftAmount}, 0)`, opacity: 0 }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
  _updatePosition(toast) {
    if (parseInt(toast.style.top, 10) === 0) {
      toast.style.top = toast.style.bottom = "";
    }
  }
};

// node_modules/onsenui/esm/elements/ons-toast/fall-animator.js
var FallToastAnimator = class extends ToastAnimator {
  constructor({ timing = "ease", delay = 0, duration = 0.35 } = {}) {
    super({ timing, delay, duration });
    if (iphonex_patch_default.isIPhoneXPortraitPatchActive()) {
      this.fallAmount = "calc(-100% - 44px)";
    } else {
      this.fallAmount = "-100%";
    }
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  show(toast, callback) {
    toast = toast._toast;
    this._updatePosition(toast);
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: `translate3d(0, ${this.fallAmount}, 0)`, opacity: 0 },
        { transform: "translate3d(0, 0, 0)", opacity: 1 }
      ).queue((done) => {
        callback && callback();
        done();
      })
    );
  }
  /**
   * @param {HTMLElement} toast
   * @param {Function} callback
   */
  hide(toast, callback) {
    toast = toast._toast;
    this._updatePosition(toast);
    animit_default.runAll(
      animit_default(toast, this.def).default(
        { transform: "translate3d(0, 0, 0)", opacity: 1 },
        { transform: `translate3d(0, ${this.fallAmount}, 0)`, opacity: 0 }
      ).queue((done) => {
        this._updatePosition(toast, true);
        callback && callback();
        done();
      })
    );
  }
  _updatePosition(toast, cleanUp) {
    let correctTop;
    if (iphonex_patch_default.isIPhoneXPortraitPatchActive()) {
      correctTop = "44px";
    } else {
      correctTop = "0";
    }
    if (toast.style.top !== correctTop) {
      toast.style.top = correctTop;
      toast.style.bottom = "initial";
    }
  }
};

// node_modules/onsenui/esm/elements/ons-toast.js
var scheme23 = {
  ".toast": "toast--*",
  ".toast__message": "toast--*__message",
  ".toast__button": "toast--*__button"
};
var defaultClassName12 = "toast";
var _animatorDict6 = {
  "default": platform_default.isAndroid() ? AscendToastAnimator : LiftToastAnimator,
  "fade": FadeToastAnimator,
  "ascend": AscendToastAnimator,
  "lift": LiftToastAnimator,
  "fall": FallToastAnimator,
  "none": ToastAnimator
};
var ToastElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   *   [en]Fired just before the toast is displayed.[/en]
   *   [ja]ダイアログが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.toast
   *   [en]Toast object.[/en]
   *   [ja]ダイアログのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute to stop the toast from showing.[/en]
   *   [ja]この関数を実行すると、ダイアログの表示を止めます。[/ja]
   */
  /**
   * @event postshow
   * @description
   *   [en]Fired just after the toast is displayed.[/en]
   *   [ja]ダイアログが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.toast
   *   [en]Toast object.[/en]
   *   [ja]ダイアログのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   *   [en]Fired just before the toast is hidden.[/en]
   *   [ja]ダイアログが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.toast
   *   [en]Toast object.[/en]
   *   [ja]ダイアログのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute to stop the toast from hiding.[/en]
   *   [ja]この関数を実行すると、ダイアログが閉じようとするのを止めます。[/ja]
   */
  /**
   * @event posthide
   * @description
   * [en]Fired just after the toast is hidden.[/en]
   * [ja]ダイアログが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.toast
   *   [en]Toast object.[/en]
   *   [ja]ダイアログのオブジェクト。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the toast. Can be either `"default"`, `"ascend"` (Android), `"lift"` (iOS), `"fall"`, `"fade"` or `"none"`.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. <code>{duration: 0.2, delay: 1, timing: 'ease-in'}</code>[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the toast is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    this._defaultDBB = null;
    contentReady(this, () => this._compile());
  }
  get _scheme() {
    return scheme23;
  }
  get _toast() {
    return util_default.findChild(this, `.${defaultClassName12}`);
  }
  _updateAnimatorFactory() {
    this._toast && (this._toast.style.top = this._toast.style.bottom = "");
    return new AnimatorFactory({
      animators: _animatorDict6,
      baseClass: ToastAnimator,
      baseClassName: "ToastAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  _compile() {
    autostyle_default.prepare(this);
    this.style.display = "none";
    this.style.zIndex = 1e4;
    const messageClassName = "toast__message";
    const buttonClassName = "toast__button";
    let toast = util_default.findChild(this, `.${defaultClassName12}`);
    if (!toast) {
      toast = document.createElement("div");
      toast.classList.add(defaultClassName12);
      while (this.childNodes[0]) {
        toast.appendChild(this.childNodes[0]);
      }
    }
    let button = util_default.findChild(toast, `.${buttonClassName}`);
    if (!button) {
      button = util_default.findChild(toast, (e) => util_default.match(e, ".button") || util_default.match(e, "button"));
      if (button) {
        button.classList.remove("button");
        button.classList.add(buttonClassName);
        toast.appendChild(button);
      }
    }
    if (!util_default.findChild(toast, `.${messageClassName}`)) {
      let message = util_default.findChild(toast, ".message");
      if (!message) {
        message = document.createElement("div");
        for (let i = toast.childNodes.length - 1; i >= 0; i--) {
          if (toast.childNodes[i] !== button) {
            message.insertBefore(toast.childNodes[i], message.firstChild);
          }
        }
      }
      message.classList.add(messageClassName);
      toast.insertBefore(message, toast.firstChild);
    }
    if (toast.parentNode !== this) {
      this.appendChild(toast);
    }
    ModifierUtil.initModifier(this, this._scheme);
  }
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the element is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @method show
   * @signature show([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"default"`, `"ascend"` (Android), `"lift"` (iOS), `"fall"`, `"fade"` or `"none"`.[/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @description
   *   [en]Show the element.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the displayed element[/en]
   *   [ja][/ja]
   */
  /**
   * @method toggle
   * @signature toggle([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"default"`, `"ascend"` (Android), `"lift"` (iOS), `"fall"`, `"fade"` or `"none"`.[/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @description
   *   [en]Toggle toast visibility.[/en]
   *   [ja][/ja]
   */
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"default"`, `"ascend"` (Android), `"lift"` (iOS), `"fall"`, `"fade"` or `"none"`.[/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @description
   *   [en]Hide toast.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja][/ja]
   */
  /**
   * @param {String} name
   * @param {Function} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof ToastAnimator)) {
      util_default.throw('"Animator" param must inherit OnsToastElement.ToastAnimator');
    }
    _animatorDict6[name44] = Animator;
  }
  static get animators() {
    return _animatorDict6;
  }
  static get ToastAnimator() {
    return ToastAnimator;
  }
};
elements_default.Toast = ToastElement;
customElements.define("ons-toast", ToastElement);

// node_modules/vue-onsenui/esm/components/VOnsToast.js
var name33 = "v-ons-toast";
var script33 = {
  name: name33,
  mixins: [deriveEvents(name33), hasOptions, deriveDBB, portal, modifier]
};
var _hoisted_126 = ["modifier"];
function render34(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-toast", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_126);
}
script33.render = render34;

// node_modules/onsenui/esm/elements/ons-popover/animator.js
var PopoverAnimator = class extends BaseAnimator {
  /**
   * @param {Object} options
   * @param {String} options.timing
   * @param {Number} options.duration
   * @param {Number} options.delay
   */
  constructor({ timing = "cubic-bezier(.1, .7, .4, 1)", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  show(popover, callback) {
    callback();
  }
  hide(popover, callback) {
    callback();
  }
  _animate(element, { from, to, options, callback, restore = false, animation }) {
    options = util_default.extend({}, this.options, options);
    if (animation) {
      from = animation.from;
      to = animation.to;
    }
    animation = animit_default(element);
    if (restore) {
      animation = animation.saveStyle();
    }
    animation = animation.queue(from).wait(this.delay).queue({
      css: to,
      duration: this.duration,
      timing: this.timing
    });
    if (restore) {
      animation = animation.restoreStyle();
    }
    if (callback) {
      animation = animation.queue((done) => {
        callback();
        done();
      });
    }
    return animation;
  }
  _animateAll(element, animations) {
    Object.keys(animations).forEach((key) => this._animate(element[key], animations[key]).play());
  }
};
var fade = {
  out: {
    from: { opacity: 1 },
    to: { opacity: 0 }
  },
  in: {
    from: { opacity: 0 },
    to: { opacity: 1 }
  }
};
var MDFadePopoverAnimator = class extends PopoverAnimator {
  show(popover, callback) {
    this._animateAll(popover, {
      _mask: fade.in,
      _popover: { animation: fade.in, restore: true, callback }
    });
  }
  hide(popover, callback) {
    this._animateAll(popover, {
      _mask: fade.out,
      _popover: { animation: fade.out, restore: true, callback }
    });
  }
};
var IOSFadePopoverAnimator = class extends MDFadePopoverAnimator {
  show(popover, callback) {
    this._animateAll(popover, {
      _mask: fade.in,
      _popover: {
        from: {
          transform: "scale3d(1.3, 1.3, 1.0)",
          opacity: 0
        },
        to: {
          transform: "scale3d(1.0, 1.0,  1.0)",
          opacity: 1
        },
        restore: true,
        callback
      }
    });
  }
};

// node_modules/onsenui/esm/elements/ons-popover.js
var scheme24 = {
  ".popover": "popover--*",
  ".popover-mask": "popover-mask--*",
  ".popover__content": "popover--*__content",
  ".popover__arrow": "popover--*__arrow"
};
var _animatorDict7 = {
  "default": function() {
    return platform_default.isAndroid() ? MDFadePopoverAnimator : IOSFadePopoverAnimator;
  },
  "none": PopoverAnimator,
  "fade-ios": IOSFadePopoverAnimator,
  "fade-md": MDFadePopoverAnimator
};
var positions = {
  up: "bottom",
  left: "right",
  down: "top",
  right: "left"
};
var directions = Object.keys(positions);
var PopoverElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   *   [en]Fired just before the popover is displayed.[/en]
   *   [ja]ポップオーバーが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.popover
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call this function to stop the popover from being shown.[/en]
   *   [ja]この関数を呼び出すと、ポップオーバーの表示がキャンセルされます。[/ja]
   */
  /**
   * @event postshow
   * @description
   *   [en]Fired just after the popover is displayed.[/en]
   *   [ja]ポップオーバーが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.popover
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   *   [en]Fired just before the popover is hidden.[/en]
   *   [ja]ポップオーバーが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.popover
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call this function to stop the popover from being hidden.[/en]
   *   [ja]この関数を呼び出すと、ポップオーバーが隠れる処理をキャンセルします。[/ja]
   */
  /**
   * @event posthide
   * @description
   *   [en]Fired just after the popover is hidden.[/en]
   *   [ja]ポップオーバーが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.popover
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event dialogcancel
   * @description
   * [en]Fired when the popover is canceled.[/en]
   * [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the popover.[/en]
   *  [ja]ポップオーバーの表現を指定します。[/ja]
   */
  /**
   * @attribute direction
   * @type {String}
   * @description
   *  [en]
   *    A space separated list of directions. If more than one direction is specified,
   *    it will be chosen automatically. Valid directions are `"up"`, `"down"`, `"left"` and `"right"`.
   *  [/en]
   *  [ja]
   *    ポップオーバーを表示する方向を空白区切りで複数指定できます。
   *    指定できる方向は、"up", "down", "left", "right"の4つです。空白区切りで複数指定することもできます。
   *    複数指定された場合、対象とする要素に合わせて指定した値から自動的に選択されます。
   *  [/ja]
   */
  /**
   * @attribute cancelable
   * @description
   *   [en]If this attribute is set the popover can be closed by tapping the background or by pressing the back button.[/en]
   *   [ja]この属性があると、ポップオーバーが表示された時に、背景やバックボタンをタップした時にをポップオーバー閉じます。[/ja]
   */
  /**
   * @attribute cover-target
   * @description
   *   [en]If set the popover will cover the target on the screen.[/en]
   *   [ja][/ja]
   */
  /**
   * @property coverTarget
   * @description
   *   [en]If set the popover will cover the target on the screen.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute target
   * @type {String}
   * @description
   *   [en]Specifies the ID of the default element for the popover.[/en]
   *   [ja]ポップオーバーの対象とするデフォルト要素のIDを指定します。[/ja]
   */
  /**
   * @property target
   * @type {String}
   * @description
   *   [en]Specifies the ID of the default element for the popover.[/en]
   *   [ja]ポップオーバーの対象とするデフォルト要素のIDを指定します。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @description
   *   [en]The animation used when showing an hiding the popover. Can be either `"none"`, `"default"`, `"fade-ios"` or `"fade-md"`.[/en]
   *   [ja]ポップオーバーを表示する際のアニメーション名を指定します。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. {duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute mask-color
   * @type {Color}
   * @description
   *   [en]Color of the background mask. Default is `"rgba(0, 0, 0, 0.2)"`.[/en]
   *   [ja]背景のマスクの色を指定します。デフォルトは"rgba(0, 0, 0, 0.2)"です。[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the popover is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    this._boundOnChange = this._onChange.bind(this);
    contentReady(this, () => {
      this._compile();
      this.style.display = "none";
    });
  }
  get _scheme() {
    return scheme24;
  }
  get _mask() {
    return util_default.findChild(this, ".popover-mask");
  }
  get _popover() {
    return util_default.findChild(this, ".popover");
  }
  get _content() {
    return util_default.findChild(this._popover, ".popover__content");
  }
  get _arrow() {
    return util_default.findChild(this._popover, ".popover__arrow");
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict7,
      baseClass: PopoverAnimator,
      baseClassName: "PopoverAnimator",
      defaultAnimation: this.getAttribute("animation") || "default"
    });
  }
  _toggleStyle(shouldShow, options = {}) {
    if (shouldShow) {
      this.style.display = "block";
      let target = options.target;
      if (!target && this.target) {
        target = document.getElementById(this.target);
      }
      this._currentTarget = target;
      this._positionPopover(target);
    } else {
      this.style.display = "none";
      this._clearStyles();
    }
  }
  _positionPopover(target) {
    const { _radius: radius, _content: contentElement, _margin: margin } = this;
    const safeAreaLengths = iphonex_patch_default.getSafeAreaLengths();
    const safeAreaRect = iphonex_patch_default.getSafeAreaDOMRect();
    const targetRect = target.getBoundingClientRect();
    const isMD = util_default.hasModifier(this, "material");
    const cover = isMD && this.hasAttribute("cover-target");
    const parent = util_default.findParent(this, "ons-page") || document.body;
    const parentDimensions = parent.getBoundingClientRect();
    const maxPositions = {
      top: Math.max(parentDimensions.top, safeAreaRect.top),
      left: Math.max(parentDimensions.left, safeAreaRect.left),
      bottom: Math.min(parentDimensions.bottom, safeAreaRect.bottom),
      right: Math.min(parentDimensions.right, safeAreaRect.right)
    };
    const targetDistance = {
      top: targetRect.top - (maxPositions.top + margin),
      left: targetRect.left - (maxPositions.left + margin),
      bottom: maxPositions.bottom - margin - targetRect.bottom,
      right: maxPositions.right - margin - targetRect.right
    };
    const targetCenterDistanceFrom = {
      top: targetRect.top + Math.round(targetRect.height / 2) - (maxPositions.top + margin),
      left: targetRect.left + Math.round(targetRect.width / 2) - (maxPositions.left + margin),
      bottom: maxPositions.bottom - margin - targetRect.bottom + Math.round(targetRect.height / 2),
      right: maxPositions.right - margin - targetRect.right + Math.round(targetRect.width / 2)
    };
    const { vertical, primary: primaryDirection, secondary: secondaryDirection } = this._calculateDirections(targetDistance);
    this._currentDirection = primaryDirection;
    util_default.addModifier(this, primaryDirection);
    const sizeName = vertical ? "width" : "height";
    const contentSize = ((style) => ({
      width: parseInt(style.getPropertyValue("width"), 10),
      height: parseInt(style.getPropertyValue("height"), 10)
    }))(window.getComputedStyle(contentElement));
    const targetAndArrowLength = cover ? 0 : (vertical ? targetRect.height : targetRect.width) + (isMD ? 0 : 14);
    const primaryOffset = Math.max(
      safeAreaLengths[primaryDirection] + margin,
      safeAreaLengths[primaryDirection] + margin + targetDistance[primaryDirection] + targetAndArrowLength
    );
    const secondaryOffset = Math.max(
      safeAreaLengths[secondaryDirection] + margin,
      safeAreaLengths[secondaryDirection] + margin + targetCenterDistanceFrom[secondaryDirection] - contentSize[sizeName] / 2
    );
    this._popover.style[primaryDirection] = primaryOffset + "px";
    this._popover.style[secondaryDirection] = secondaryOffset + "px";
    this._arrow.style[secondaryDirection] = Math.max(
      radius,
      safeAreaLengths[secondaryDirection] + margin + targetCenterDistanceFrom[secondaryDirection] - secondaryOffset
    ) + "px";
  }
  _calculateDirections(distance) {
    const options = (this.getAttribute("direction") || "up down left right").split(/\s+/).map((e) => positions[e]);
    const primary = options.sort((a, b) => distance[a] - distance[b])[0];
    const vertical = "top" == primary || "bottom" == primary;
    let secondary;
    if (vertical) {
      secondary = distance.left < distance.right ? "left" : "right";
    } else {
      secondary = distance.top < distance.bottom ? "top" : "bottom";
    }
    return { vertical, primary, secondary };
  }
  _clearStyles() {
    this._currentDirection = null;
    ["top", "bottom", "left", "right"].forEach((e) => {
      this._arrow.style[e] = this._content.style[e] = this._popover.style[e] = "";
      util_default.removeModifier(this, e);
    });
  }
  _onChange() {
    setImmediate(() => {
      if (this._currentTarget) {
        this._positionPopover(this._currentTarget);
      }
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    if (this._popover && this._mask) {
      return;
    }
    const hasDefaultContainer = this._popover && this._content;
    if (hasDefaultContainer) {
      if (!this._mask) {
        const mask = document.createElement("div");
        mask.classList.add("popover-mask");
        this.insertBefore(mask, this.firstChild);
      }
      if (!this._arrow) {
        const arrow = document.createElement("div");
        arrow.classList.add("popover__arrow");
        this._popover.appendChild(arrow);
      }
    } else {
      const template3 = util_default.createFragment(`
        <div class="popover-mask"></div>
        <div class="popover">
          <div class="popover__content"></div>
          <div class="popover__arrow"></div>
        </div>
      `);
      const content = template3.querySelector(".popover__content");
      while (this.childNodes[0]) {
        content.appendChild(this.childNodes[0]);
      }
      this.appendChild(template3);
    }
    if (this.hasAttribute("style")) {
      this._popover.setAttribute("style", this.getAttribute("style"));
      this.removeAttribute("style");
    }
    ModifierUtil.initModifier(this, this._scheme);
  }
  /**
   * @method show
   * @signature show(target, [options])
   * @param {String|Event|HTMLElement} target
   *   [en]Target element. Can be either a CSS selector, an event object or a DOM element. It can be also provided as 'options.target' instead. [/en]
   *   [ja]ポップオーバーのターゲットとなる要素を指定します。CSSセレクタかeventオブジェクトかDOM要素のいずれかを渡せます。[/ja]
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name.  Use one of `"fade-ios"`, `"fade-md"`, `"none"` and `"default"`.[/en]
   *   [ja]アニメーション名を指定します。"fade-ios", "fade-md", "none", "default"のいずれかを指定できます。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]This function is called after the popover has been revealed.[/en]
   *   [ja]ポップオーバーが表示され終わった後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Open the popover and point it at a target. The target can be either an event, a CSS selector or a DOM element..[/en]
   *   [ja]対象とする要素にポップオーバーを表示します。target引数には、$eventオブジェクトやDOMエレメントやCSSセレクタを渡すことが出来ます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the displayed element[/en]
   *   [ja][/ja]
   */
  show(target, options = {}) {
    if (target && typeof target === "object" && !(target instanceof Event) && !(target instanceof HTMLElement)) {
      options = { ...target };
    } else {
      options = { ...options, target };
    }
    if (typeof options.target === "string") {
      options.target = document.querySelector(options.target);
    } else if (options.target instanceof Event) {
      options.target = options.target.target;
    }
    if (!options.target && this.target) {
      options.target = document.getElementById(this.target);
    }
    if (!(options.target instanceof HTMLElement)) {
      util_default.throw("Invalid target type or undefined");
    }
    return super.show(options);
  }
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name.  Use one of `"fade-ios"`, `"fade-md"`, `"none"` and `"default"`.[/en]
   *   [ja]アニメーション名を指定します。"fade-ios", "fade-md", "none", "default"のいずれかを指定できます。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]This functions is called after the popover has been hidden.[/en]
   *   [ja]ポップオーバーが隠れた後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Close the popover.[/en]
   *   [ja]ポップオーバーを閉じます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja][/ja]
   */
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the element is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @property cancelable
   * @type {Boolean}
   * @description
   *   [en]
   *     A boolean value that specifies whether the popover is cancelable or not.
   *
   *     When the popover is cancelable it can be closed by tapping the background or by pressing the back button on Android devices.
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  /**
   * @property maskColor
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is "rgba(0, 0, 0, 0.2)".[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  connectedCallback() {
    super.connectedCallback();
    window.addEventListener("resize", this._boundOnChange, false);
    this._margin = this._margin || parseInt(window.getComputedStyle(this).getPropertyValue("top"));
    this._margin = this._margin || 6;
    contentReady(this, () => {
      this._radius = parseInt(window.getComputedStyle(this._content).getPropertyValue("border-top-left-radius"));
    });
  }
  disconnectedCallback() {
    super.disconnectedCallback();
    window.removeEventListener("resize", this._boundOnChange, false);
  }
  static get observedAttributes() {
    return [...super.observedAttributes, "direction"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "direction") {
      return this._boundOnChange();
    } else if (name44 === "modifier") {
      this._currentDirection && util_default.addModifier(this, this._currentDirection);
    }
    super.attributeChangedCallback(name44, last, current);
  }
  /**
   * @param {String} name
   * @param {PopoverAnimator} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof PopoverAnimator)) {
      util_default.throwAnimator("Popover");
    }
    _animatorDict7[name44] = Animator;
  }
  static get animators() {
    return _animatorDict7;
  }
  static get PopoverAnimator() {
    return PopoverAnimator;
  }
};
util_default.defineBooleanProperties(PopoverElement, ["cover-target"]);
util_default.defineStringProperties(PopoverElement, ["target"]);
elements_default.Popover = PopoverElement;
customElements.define("ons-popover", PopoverElement);

// node_modules/vue-onsenui/esm/components/VOnsPopover.js
var name34 = "v-ons-popover";
var script34 = {
  name: name34,
  mixins: [hidable, hasOptions, dialog, deriveEvents(name34), deriveDBB, portal],
  props: {
    target: {
      validator(value) {
        return value.__isVue || typeof value === "string" || value instanceof Event || value instanceof HTMLElement;
      }
    }
  },
  computed: {
    normalizedTarget() {
      return this.target.$el || this.target;
    },
    normalizedOptions() {
      if (this.target) {
        return {
          target: this.normalizedTarget,
          ...this.options
        };
      }
      return this.options;
    }
  }
};
function render35(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-popover", normalizeProps(guardReactiveProps(_ctx.orderedProps)), [
    renderSlot(_ctx.$slots, "default")
  ], 16);
}
script34.render = render35;

// node_modules/onsenui/esm/elements/ons-alert-dialog/animator.js
var AlertDialogAnimator = class extends BaseAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  show(dialog2, done) {
    done();
  }
  /**
   * @param {HTMLElement} dialog
   * @param {Function} done
   */
  hide(dialog2, done) {
    done();
  }
};
var AndroidAlertDialogAnimator = class extends AlertDialogAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .4, 1)", duration = 0.2, delay = 0 } = {}) {
    super({ duration, timing, delay });
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 0 }, { opacity: 1 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0) scale3d(.9, .9, 1)", opacity: 0 },
        { transform: "translate3d(-50%, -50%, 0) scale3d(1, 1, 1)", opacity: 1 }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 1 }, { opacity: 0 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0) scale3d(1, 1, 1)", opacity: 1 },
        { transform: "translate3d(-50%, -50%, 0) scale3d(.9, .9, 1)", opacity: 0 }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
};
var IOSAlertDialogAnimator = class extends AlertDialogAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .4, 1)", duration = 0.2, delay = 0 } = {}) {
    super({ duration, timing, delay });
  }
  /*
   * @param {Object} dialog
   * @param {Function} callback
   */
  show(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 0 }, { opacity: 1 }),
      animit_default(dialog2._dialog, this.def).default(
        { transform: "translate3d(-50%, -50%, 0) scale3d(1.3, 1.3, 1)", opacity: 0 },
        { transform: "translate3d(-50%, -50%, 0) scale3d(1, 1, 1)", opacity: 1 }
      ).queue((done) => {
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} dialog
   * @param {Function} callback
   */
  hide(dialog2, callback) {
    callback = callback ? callback : function() {
    };
    animit_default.runAll(
      animit_default(dialog2._mask, this.def).default({ opacity: 1 }, { opacity: 0 }),
      animit_default(dialog2._dialog, this.def).default({ opacity: 1 }, { opacity: 0 }).queue((done) => {
        callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-alert-dialog.js
var scheme25 = {
  ".alert-dialog": "alert-dialog--*",
  ".alert-dialog-container": "alert-dialog-container--*",
  ".alert-dialog-title": "alert-dialog-title--*",
  ".alert-dialog-content": "alert-dialog-content--*",
  ".alert-dialog-footer": "alert-dialog-footer--*",
  ".alert-dialog-footer--rowfooter": "alert-dialog-footer--rowfooter--*",
  ".alert-dialog-button--rowfooter": "alert-dialog-button--rowfooter--*",
  ".alert-dialog-button--primal": "alert-dialog-button--primal--*",
  ".alert-dialog-button": "alert-dialog-button--*",
  "ons-alert-dialog-button": "alert-dialog-button--*",
  ".alert-dialog-mask": "alert-dialog-mask--*",
  ".text-input": "text-input--*"
};
var _animatorDict8 = {
  "none": AlertDialogAnimator,
  "default": function() {
    return platform_default.isAndroid() ? AndroidAlertDialogAnimator : IOSAlertDialogAnimator;
  },
  "fade": function() {
    return platform_default.isAndroid() ? AndroidAlertDialogAnimator : IOSAlertDialogAnimator;
  }
};
var AlertDialogElement = class extends BaseDialogElement {
  /**
   * @event preshow
   * @description
   *   [en]Fired just before the alert dialog is displayed.[/en]
   *   [ja]アラートダイアログが表示される直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.alertDialog
   *   [en]Alert dialog object.[/en]
   *   [ja]アラートダイアログのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute to stop the dialog from showing.[/en]
   *   [ja]この関数を実行すると、アラートダイアログの表示を止めます。[/ja]
   */
  /**
   * @event postshow
   * @description
   *   [en]Fired just after the alert dialog is displayed.[/en]
   *   [ja]アラートダイアログが表示された直後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.alertDialog
   *   [en]Alert dialog object.[/en]
   *   [ja]アラートダイアログのオブジェクト。[/ja]
   */
  /**
   * @event prehide
   * @description
   *   [en]Fired just before the alert dialog is hidden.[/en]
   *   [ja]アラートダイアログが隠れる直前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.alertDialog
   *   [en]Alert dialog object.[/en]
   *   [ja]アラートダイアログのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Execute to stop the dialog from hiding.[/en]
   *   [ja]この関数を実行すると、アラートダイアログが閉じようとするのを止めます。[/ja]
   */
  /**
   * @event posthide
   * @description
   * [en]Fired just after the alert dialog is hidden.[/en]
   * [ja]アラートダイアログが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.alertDialog
   *   [en]Alert dialog object.[/en]
   *   [ja]アラートダイアログのオブジェクト。[/ja]
   */
  /**
   * @event dialogcancel
   * @description
   * [en]Fired when the dialog is canceled.[/en]
   * [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the dialog.[/en]
   *  [ja]ダイアログの見た目を指定します。[/ja]
   */
  /**
   * @attribute cancelable
   * @description
   *  [en]If this attribute is set the dialog can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *  [ja]この属性を設定すると、ダイアログの背景をタップしたりAndroidデバイスのバックボタンを押すとダイアログが閉じるようになります。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *  [en]If this attribute is set the dialog is disabled.[/en]
   *  [ja]この属性がある時、アラートダイアログはdisabled状態になります。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *  [en]The animation used when showing and hiding the dialog. Can be either `"none"` or `"default"`.[/en]
   *  [ja]ダイアログを表示する際のアニメーション名を指定します。デフォルトでは"none"か"default"が指定できます。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute mask-color
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is "rgba(0, 0, 0, 0.2)".[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @attribute visible
   * @type {Boolean}
   * @description
   *   [en]Whether the alert dialog is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
  }
  get _scheme() {
    return scheme25;
  }
  /**
   * @return {Element}
   */
  get _mask() {
    return util_default.findChild(this, ".alert-dialog-mask");
  }
  /**
   * @return {Element}
   */
  get _dialog() {
    return util_default.findChild(this, ".alert-dialog");
  }
  /**
   * @return {Element}
   */
  get _titleElement() {
    return util_default.findChild(this._dialog.children[0], ".alert-dialog-title");
  }
  /**
   * @return {Element}
   */
  get _contentElement() {
    return util_default.findChild(this._dialog.children[0], ".alert-dialog-content");
  }
  _updateAnimatorFactory() {
    return new AnimatorFactory({
      animators: _animatorDict8,
      baseClass: AlertDialogAnimator,
      baseClassName: "AlertDialogAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    this.style.display = "none";
    this.style.zIndex = 10001;
    const content = document.createDocumentFragment();
    if (!this._mask && !this._dialog) {
      while (this.firstChild) {
        content.appendChild(this.firstChild);
      }
    }
    if (!this._mask) {
      const mask = document.createElement("div");
      mask.classList.add("alert-dialog-mask");
      this.insertBefore(mask, this.children[0]);
    }
    if (!this._dialog) {
      const dialog2 = document.createElement("div");
      dialog2.classList.add("alert-dialog");
      this.insertBefore(dialog2, null);
    }
    if (!util_default.findChild(this._dialog, ".alert-dialog-container")) {
      const container = document.createElement("div");
      container.classList.add("alert-dialog-container");
      this._dialog.appendChild(container);
    }
    this._dialog.children[0].appendChild(content);
    this._dialog.style.zIndex = 20001;
    this._mask.style.zIndex = 2e4;
    ModifierUtil.initModifier(this, this._scheme);
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property cancelable
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is cancelable or not. A cancelable dialog can be closed by tapping the background or by pressing the back button on Android devices.[/en]
   *   [ja]そのダイアログがキャンセル可能かどうかを表します。キャンセル可能なダイアログは、背景をタップするかAndroidデバイスのバックボタンを押すことで閉じることが出来るようになります。[/ja]
   */
  /**
   * @property maskColor
   * @type {String}
   * @default rgba(0, 0, 0, 0.2)
   * @description
   *  [en]Color of the background mask. Default is "rgba(0, 0, 0, 0.2)".[/en]
   *  [ja]背景のマスクの色を指定します。"rgba(0, 0, 0, 0.2)"がデフォルト値です。[/ja]
   */
  /**
   * @method show
   * @signature show([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクトです。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"fade"` and `"none"`.[/en]
   *   [ja]アニメーション名を指定します。指定できるのは、"fade", "none"のいずれかです。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g.  `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. <code>{duration: 0.2, delay: 0.4, timing: 'ease-in'}</code> [/ja]
   * @param {Function} [options.callback]
   *   [en]Function to execute after the dialog has been revealed.[/en]
   *   [ja]ダイアログが表示され終わった時に呼び出されるコールバックを指定します。[/ja]
   * @description
   *   [en]Show the alert dialog.[/en]
   *   [ja]ダイアログを表示します。[/ja]
   * @return {Promise}
   *   [en]A `Promise` object that resolves to the displayed element.[/en]
   *   [ja]表示される要素を解決する`Promise`オブジェクトを返します。[/ja]
   */
  /**
   * @method hide
   * @signature hide([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]Animation name. Available animations are `"fade"` and `"none"`.[/en]
   *   [ja]アニメーション名を指定します。"fade", "none"のいずれかを指定します。[/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g.  <code>{duration: 0.2, delay: 0.4, timing: 'ease-in'}</code>[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. <code>{duration: 0.2, delay: 0.4, timing: 'ease-in'}</code> [/ja]
   * @param {Function} [options.callback]
   *   [en]Function to execute after the dialog has been hidden.[/en]
   *   [ja]このダイアログが閉じた時に呼び出されるコールバックを指定します。[/ja]
   * @description
   *   [en]Hide the alert dialog.[/en]
   *   [ja]ダイアログを閉じます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the hidden element[/en]
   *   [ja]隠れた要素を解決する`Promise`オブジェクトを返します。[/ja]
   */
  /**
   * @property visible
   * @type {Boolean}
   * @description
   *   [en]Whether the dialog is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @param {String} name
   * @param {DialogAnimator} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof AlertDialogAnimator)) {
      util_default.throwAnimator("AlertDialog");
    }
    _animatorDict8[name44] = Animator;
  }
  static get animators() {
    return _animatorDict8;
  }
  static get AlertDialogAnimator() {
    return AlertDialogAnimator;
  }
};
elements_default.AlertDialog = AlertDialogElement;
customElements.define("ons-alert-dialog", AlertDialogElement);

// node_modules/vue-onsenui/esm/components/VOnsAlertDialog.js
var name35 = "v-ons-alert-dialog";
var script35 = {
  name: name35,
  mixins: [hasOptions, dialog, deriveEvents(name35), deriveDBB, portal, modifier],
  props: {
    title: {
      type: String
    },
    footer: {
      type: Object,
      validator(value) {
        return Object.keys(value).every((key) => value[key] instanceof Function);
      }
    }
  }
};
var _hoisted_127 = ["modifier"];
var _hoisted_2 = { class: "alert-dialog-title" };
var _hoisted_3 = { class: "alert-dialog-content" };
var _hoisted_4 = { class: "alert-dialog-footer" };
var _hoisted_5 = ["onClick"];
function render36(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-alert-dialog", mergeProps({ modifier: _ctx.normalizedModifier }, _ctx.orderedProps), [
    createBaseVNode("div", _hoisted_2, [
      renderSlot(_ctx.$slots, "title", {}, () => [
        createTextVNode(toDisplayString($props.title), 1)
      ])
    ]),
    createBaseVNode("div", _hoisted_3, [
      renderSlot(_ctx.$slots, "default")
    ]),
    createBaseVNode("div", _hoisted_4, [
      renderSlot(_ctx.$slots, "footer", {}, () => [
        (openBlock(true), createElementBlock(Fragment, null, renderList($props.footer, (handler, key) => {
          return openBlock(), createElementBlock("ons-alert-dialog-button", {
            key,
            onClick: handler
          }, toDisplayString(key), 9, _hoisted_5);
        }), 128))
      ])
    ])
  ], 16, _hoisted_127);
}
script35.render = render36;

// node_modules/onsenui/esm/elements/ons-speed-dial.js
var defaultClassName13 = "speed-dial";
var scheme26 = {
  "": "speed-dial--*"
};
var SpeedDialElement = class extends BaseElement {
  /**
   * @event open
   * @description
   *   [en]Fired when the menu items are shown.[/en]
   *   [ja][/ja]
   */
  /**
   * @event close
   * @description
   *   [en]Fired when the menu items are hidden.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the component.[/en]
   *   [ja]このコンポーネントの表現を指定します。[/ja]
   */
  /**
   * @attribute ripple
   * @description
   *  [en]If this attribute is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  /**
   * @property ripple
   * @type {Boolean}
   * @description
   *  [en]If this property is defined, the button will have a ripple effect when tapped.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute position
   * @type {String}
   * @description
   *   [en]
   *     Specify the vertical and horizontal position of the component.
   *     I.e. to display it in the top right corner specify "right top".
   *     Choose from "right", "left", "top" and "bottom".
   *   [/en]
   *   [ja]
   *     この要素を表示する左右と上下の位置を指定します。
   *     例えば、右上に表示する場合には"right top"を指定します。
   *     左右と上下の位置の指定には、rightとleft、topとbottomがそれぞれ指定できます。
   *   [/ja]
   */
  /**
   * @attribute direction
   * @type {String}
   * @description
   *   [en]Specify the direction the items are displayed. Possible values are "up", "down", "left" and "right".[/en]
   *   [ja]
   *     要素が表示する方向を指定します。up, down, left, rightが指定できます。
   *   [/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if button should be disabled.[/en]
   *   [ja]無効化する場合に指定します。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => {
      this._compile();
    });
    this._boundOnClick = this._onClick.bind(this);
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "click");
    this._connectOnClick = onConnected;
    this._disconnectOnClick = onDisconnected;
  }
  _compile() {
    this.classList.add(defaultClassName13);
    autostyle_default.prepare(this);
    this._updateRipple();
    ModifierUtil.initModifier(this, scheme26);
    if (this.hasAttribute("direction")) {
      this._updateDirection(this.getAttribute("direction"));
    } else {
      this._updateDirection("up");
    }
    this._updatePosition();
  }
  static get observedAttributes() {
    return ["class", "modifier", "ripple", "direction", "position", "open"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName13, scheme26);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme26);
        break;
      case "ripple":
        contentReady(this, () => this._updateRipple());
        break;
      case "direction":
        contentReady(this, () => this._updateDirection(current));
        break;
      case "position":
        contentReady(this, () => this._updatePosition());
        break;
      case "open":
        if (!this._ignoreOpenSideEffect) {
          contentReady(this, () => this._updateOpen(last));
        }
        break;
    }
  }
  connectedCallback() {
    this.addEventListener("click", this._boundOnClick, false);
    this._connectOnClick();
  }
  disconnectedCallback() {
    this.removeEventListener("click", this._boundOnClick, false);
    this._disconnectOnClick();
  }
  get items() {
    return util_default.arrayFrom(this.querySelectorAll("ons-speed-dial-item"));
  }
  get _fab() {
    return util_default.findChild(this, "ons-fab");
  }
  _onClick(event) {
    setTimeout(() => {
      if (!event.defaultPrevented && !this.disabled && this.visible) {
        return this.toggleItems();
      }
    });
  }
  _show() {
    if (!this.inline) {
      return this.show();
    }
    return Promise.resolve();
  }
  _hide() {
    return new Promise((resolve) => {
      if (!this.inline) {
        setImmediate(() => this.hide().then(resolve));
      } else {
        resolve();
      }
    });
  }
  _updateRipple() {
    if (this._fab) {
      this.hasAttribute("ripple") ? this._fab.setAttribute("ripple", "") : this._fab.removeAttribute("ripple");
    }
  }
  _updateDirection(direction) {
    const children = this.items;
    for (let i = 0; i < children.length; i++) {
      styler_default(children[i], {
        transitionDelay: 25 * i + "ms",
        bottom: "auto",
        right: "auto",
        top: "auto",
        left: "auto"
      });
    }
    switch (direction) {
      case "up":
        for (let i = 0; i < children.length; i++) {
          children[i].style.bottom = 72 + 56 * i + "px";
          children[i].style.right = "8px";
        }
        break;
      case "down":
        for (let i = 0; i < children.length; i++) {
          children[i].style.top = 72 + 56 * i + "px";
          children[i].style.left = "8px";
        }
        break;
      case "left":
        for (let i = 0; i < children.length; i++) {
          children[i].style.top = "8px";
          children[i].style.right = 72 + 56 * i + "px";
        }
        break;
      case "right":
        for (let i = 0; i < children.length; i++) {
          children[i].style.top = "8px";
          children[i].style.left = 72 + 56 * i + "px";
        }
        break;
      default:
        util_default.throw("Argument must be one of up, down, left or right.");
    }
  }
  _updatePosition() {
    const position = this.getAttribute("position");
    this.classList.remove(
      "fab--top__left",
      "fab--bottom__right",
      "fab--bottom__left",
      "fab--top__right",
      "fab--top__center",
      "fab--bottom__center"
    );
    switch (position) {
      case "top right":
      case "right top":
        this.classList.add("fab--top__right");
        break;
      case "top left":
      case "left top":
        this.classList.add("fab--top__left");
        break;
      case "bottom right":
      case "right bottom":
        this.classList.add("fab--bottom__right");
        break;
      case "bottom left":
      case "left bottom":
        this.classList.add("fab--bottom__left");
        break;
      case "center top":
      case "top center":
        this.classList.add("fab--top__center");
        break;
      case "center bottom":
      case "bottom center":
        this.classList.add("fab--bottom__center");
        break;
      default:
        break;
    }
  }
  _getTranslate() {
    const isBottom = (this.getAttribute("position") || "").indexOf("bottom") >= 0;
    const translate = isBottom ? `translate3d(0px, -${util_default.globals.fabOffset || 0}px, 0px) ` : "";
    return translate;
  }
  /**
   * @method show
   * @signature show()
   * @description
   *   [en]Show the speed dial.[/en]
   *   [ja]Speed dialを表示します。[/ja]
   */
  show() {
    this._fab.show();
    styler_default(this, { transform: this._getTranslate });
    return Promise.resolve();
  }
  /**
   * @method hide
   * @signature hide()
   * @description
   *   [en]Hide the speed dial.[/en]
   *   [ja]Speed dialを非表示にします。[/ja]
   */
  hide() {
    return this.hideItems().then(() => this._fab.hide());
  }
  /**
   * @method showItems
   * @signature showItems()
   * @description
   *   [en]Show the speed dial items.[/en]
   *   [ja]Speed dialの子要素を表示します。[/ja]
   */
  showItems() {
    const last = this.open;
    this._ignoreOpenSideEffect = true;
    this.open = true;
    this._ignoreOpenSideEffect = false;
    return this._updateOpen(last);
  }
  /**
   * @method hideItems
   * @signature hideItems()
   * @description
   *   [en]Hide the speed dial items.[/en]
   *   [ja]Speed dialの子要素を非表示にします。[/ja]
   */
  hideItems() {
    const last = this.open;
    this._ignoreOpenSideEffect = true;
    this.open = false;
    this._ignoreOpenSideEffect = false;
    return this._updateOpen(last);
  }
  _updateOpen(last) {
    if (this.open) {
      if (this.hasAttribute("direction")) {
        this._updateDirection(this.getAttribute("direction"));
      } else {
        this._updateDirection("up");
      }
    }
    let totalDelay = 0;
    if (last !== this.open) {
      const children = this.items;
      for (let i = 0; i < children.length; i++) {
        const delay = 25 * (this.open ? i : children.length - i);
        totalDelay += delay;
        styler_default(children[i], {
          transform: `scale(${this.open ? 1 : 0})`,
          transitionDelay: delay + "ms"
        });
      }
      totalDelay += 50;
      util_default.triggerElementEvent(this, this.open ? "open" : "close");
    }
    const deferred = util_default.defer();
    setTimeout(deferred.resolve, totalDelay);
    return deferred.promise;
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  set disabled(value) {
    if (value) {
      this.hideItems();
    }
    util_default.arrayFrom(this.children).forEach((e) => {
      util_default.match(e, ".fab") && util_default.toggleAttribute(e, "disabled", value);
    });
    util_default.toggleAttribute(this, "disabled", value);
  }
  get disabled() {
    return this.hasAttribute("disabled");
  }
  /**
   * @property inline
   * @readonly
   * @type {Boolean}
   * @description
   *   [en]Whether the element is inline or not.[/en]
   *   [ja]インライン要素の場合に`true`。[/ja]
   */
  get inline() {
    return this.hasAttribute("inline");
  }
  /**
   * @property visible
   * @readonly
   * @type {Boolean}
   * @description
   *   [en]Whether the element is visible or not.[/en]
   *   [ja]要素が見える場合に`true`。[/ja]
   */
  get visible() {
    return this._fab && this._fab.visible && this.style.display !== "none";
  }
  /**
   * @method isOpen
   * @signature isOpen()
   * @description
   *   [en]Returns whether the menu is open or not.[/en]
   *   [ja][/ja]
   */
  isOpen() {
    return this.open;
  }
  /**
   * @attribute open
   * @type {Boolean}
   * @description
   *   [en]Returns whether the menu is open or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @property open
   * @type {Boolean}
   * @description
   *   [en]Returns whether the menu is open or not.[/en]
   *   [ja][/ja]
   */
  /**
   * @method toggle
   * @signature toggle()
   * @description
   *   [en]Toggle visibility.[/en]
   *   [ja]Speed dialの表示非表示を切り替えます。[/ja]
   */
  toggle() {
    return this.visible ? this.hide() : this.show();
  }
  /**
   * @method toggleItems
   * @signature toggleItems()
   * @description
   *   [en]Toggle item visibility.[/en]
   *   [ja]Speed dialの子要素の表示非表示を切り替えます。[/ja]
   */
  toggleItems() {
    return this.open ? this.hideItems() : this.showItems();
  }
  static get events() {
    return ["open", "close"];
  }
};
util_default.defineBooleanProperties(SpeedDialElement, ["open", "ripple"]);
elements_default.SpeedDial = SpeedDialElement;
customElements.define("ons-speed-dial", SpeedDialElement);

// node_modules/vue-onsenui/esm/components/VOnsSpeedDial.js
var name36 = "v-ons-speed-dial";
var script36 = {
  name: name36,
  mixins: [deriveEvents(name36), hidable],
  emits: ["update:open"],
  props: {
    open: {
      type: Boolean,
      default: void 0
    }
  },
  methods: {
    _shouldUpdate() {
      return this.open !== void 0 && this.open !== this.$el.isOpen();
    },
    _updateToggle() {
      this._shouldUpdate() && this.$el[this.open ? "showItems" : "hideItems"].call(this.$el);
    }
  },
  watch: {
    open() {
      this._updateToggle();
    }
  },
  mounted() {
    this._updateOpenHandler = () => this._shouldUpdate() && this.$emit("update:open", this.$el.isOpen());
    ["open", "close"].forEach((event) => this.$el.addEventListener(event, this._updateOpenHandler));
    this._updateToggle();
  },
  beforeDestroy() {
    ["open", "close"].forEach((event) => this.$el.removeEventListener(event, this._updateOpenHandler));
  }
};
function render37(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-speed-dial", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script36.render = render37;

// node_modules/onsenui/esm/ons/internal/swiper.js
var directionMap = {
  vertical: {
    axis: "Y",
    size: "Height",
    dir: ["up", "down"],
    t3d: ["0px, ", "px, 0px"]
  },
  horizontal: {
    axis: "X",
    size: "Width",
    dir: ["left", "right"],
    t3d: ["", "px, 0px, 0px"]
  }
};
var Swiper = class {
  constructor(params) {
    const FALSE = () => false;
    `getInitialIndex getBubbleWidth isVertical isOverScrollable isCentered
    isAutoScrollable refreshHook preChangeHook postChangeHook overScrollHook`.split(/\s+/).forEach((key) => this[key] = params[key] || FALSE);
    this.getElement = params.getElement;
    this.scrollHook = params.scrollHook;
    this.itemSize = params.itemSize || "100%";
    this.getAutoScrollRatio = (...args) => {
      let ratio = params.getAutoScrollRatio && params.getAutoScrollRatio(...args);
      ratio = typeof ratio === "number" && ratio === ratio ? ratio : 0.5;
      if (ratio < 0 || ratio > 1) {
        util_default.throw("Invalid auto-scroll-ratio " + ratio + ". Must be between 0 and 1");
      }
      return ratio;
    };
    this.shouldBlock = util_default.globals.actualMobileOS === "other";
    this.onDragStart = this.onDragStart.bind(this);
    this.onDrag = this.onDrag.bind(this);
    this.onDragEnd = this.onDragEnd.bind(this);
    this.onResize = this.onResize.bind(this);
    this._shouldFixScroll = util_default.globals.actualMobileOS === "ios";
  }
  init({ swipeable, autoRefresh } = {}) {
    this.initialized = true;
    this.target = this.getElement().children[0];
    this.blocker = this.getElement().children[1];
    if (!this.target || !this.blocker) {
      util_default.throw('Expected "target" and "blocker" elements to exist before initializing Swiper');
    }
    if (!this.shouldBlock) {
      this.blocker.style.display = "none";
    }
    this.getElement().classList.add("ons-swiper");
    this.target.classList.add("ons-swiper-target");
    this.blocker.classList.add("ons-swiper-blocker");
    this._gestureDetector = new gesture_detector_default(
      this.getElement(),
      { dragMinDistance: 1, dragLockToAxis: true, passive: !this._shouldFixScroll }
    );
    this._mutationObserver = new MutationObserver(() => this.refresh());
    this.updateSwipeable(swipeable);
    this.updateAutoRefresh(autoRefresh);
    this._scroll = this._offset = this._lastActiveIndex = 0;
    this._updateLayout();
    this._setupInitialIndex();
    setImmediate(() => this.initialized && this._setupInitialIndex());
    if (window !== window.parent || this.offsetHeight === 0) {
      window.requestAnimationFrame(() => this.initialized && this.onResize());
    }
  }
  dispose() {
    this.initialized = false;
    this.updateSwipeable(false);
    this.updateAutoRefresh(false);
    this._gestureDetector && this._gestureDetector.dispose();
    this.target = this.blocker = this._gestureDetector = this._mutationObserver = null;
    this.setupResize(false);
  }
  onResize() {
    const i = this._scroll / this.itemNumSize;
    this._reset();
    this.setActiveIndex(i);
    this.refresh();
  }
  get itemCount() {
    return this.target.children.length;
  }
  get itemNumSize() {
    if (typeof this._itemNumSize !== "number" || this._itemNumSize !== this._itemNumSize) {
      this._itemNumSize = this._calculateItemSize();
    }
    return this._itemNumSize;
  }
  get maxScroll() {
    const max = this.itemCount * this.itemNumSize - this.targetSize;
    return Math.ceil(max < 0 ? 0 : max);
  }
  _calculateItemSize() {
    const matches = this.itemSize.match(/^(\d+)(px|%)/);
    if (!matches) {
      util_default.throw(`Invalid state: swiper's size unit must be '%' or 'px'`);
    }
    const value = parseInt(matches[1], 10);
    return matches[2] === "%" ? Math.round(value / 100 * this.targetSize) : value;
  }
  _setupInitialIndex() {
    this._reset();
    this._lastActiveIndex = Math.max(Math.min(Number(this.getInitialIndex()), this.itemCount), 0);
    this._scroll = this._offset + this.itemNumSize * this._lastActiveIndex;
    this._scrollTo(this._scroll);
  }
  _setSwiping(toggle) {
    this.target.classList.toggle("swiping", toggle);
  }
  setActiveIndex(index, options = {}) {
    this._setSwiping(true);
    index = Math.max(0, Math.min(index, this.itemCount - 1));
    const scroll = Math.max(0, Math.min(this.maxScroll, this._offset + this.itemNumSize * index));
    return this._changeTo(scroll, options);
  }
  getActiveIndex(scroll = this._scroll) {
    scroll -= this._offset;
    const count = this.itemCount, size = this.itemNumSize;
    if (this.itemNumSize === 0 || !util_default.isInteger(scroll)) {
      return this._lastActiveIndex;
    }
    if (scroll <= 0) {
      return 0;
    }
    for (let i = 0; i < count; i++) {
      if (size * i <= scroll && size * (i + 1) > scroll) {
        return i;
      }
    }
    return count - 1;
  }
  setupResize(add) {
    window[(add ? "add" : "remove") + "EventListener"]("resize", this.onResize, true);
  }
  show() {
    this.setupResize(true);
    this.onResize();
    setTimeout(() => this.target && this.target.classList.add("active"), 1e3 / 60);
  }
  hide() {
    this.setupResize(false);
    this.target.classList.remove("active");
  }
  updateSwipeable(shouldUpdate) {
    if (this._gestureDetector) {
      const action = shouldUpdate ? "on" : "off";
      this._gestureDetector[action]("drag", this.onDrag);
      this._gestureDetector[action]("dragstart", this.onDragStart);
      this._gestureDetector[action]("dragend", this.onDragEnd);
    }
  }
  updateAutoRefresh(shouldWatch) {
    if (this._mutationObserver) {
      shouldWatch ? this._mutationObserver.observe(this.target, { childList: true }) : this._mutationObserver.disconnect();
    }
  }
  updateItemSize(newSize) {
    this.itemSize = newSize || "100%";
    this.refresh();
  }
  toggleBlocker(block) {
    this.blocker.style.pointerEvents = block ? "auto" : "none";
  }
  _canConsumeGesture(gesture) {
    const d = gesture.direction;
    const isFirst = this._scroll === 0 && !this.isOverScrollable();
    const isLast = this._scroll === this.maxScroll && !this.isOverScrollable();
    return this.isVertical() ? d === "down" && !isFirst || d === "up" && !isLast : d === "right" && !isFirst || d === "left" && !isLast;
  }
  onDragStart(event) {
    this._ignoreDrag = event.consumed || !util_default.isValidGesture(event);
    if (!this._ignoreDrag) {
      const consume = event.consume;
      event.consume = () => {
        consume && consume();
        this._ignoreDrag = true;
      };
      if (this._canConsumeGesture(event.gesture)) {
        const startX = event.gesture.center && event.gesture.center.clientX || 0, distFromEdge = this.getBubbleWidth() || 0, start = () => {
          consume && consume();
          event.consumed = true;
          this._started = true;
          this.shouldBlock && this.toggleBlocker(true);
          this._setSwiping(true);
          util_default.iosPreventScroll(this._gestureDetector);
        };
        startX < distFromEdge || startX > this.targetSize - distFromEdge ? setImmediate(() => !this._ignoreDrag && start()) : start();
      }
    }
  }
  onDrag(event) {
    if (!event.gesture || this._ignoreDrag || !this._started) {
      return;
    }
    this._continued = true;
    event.stopPropagation();
    this._scrollTo(this._scroll - this._getDelta(event), { throttle: true });
  }
  onDragEnd(event) {
    this._started = false;
    if (!event.gesture || this._ignoreDrag || !this._continued) {
      this._ignoreDrag = true;
      return;
    }
    this._continued = false;
    event.stopPropagation();
    const scroll = this._scroll - this._getDelta(event);
    const normalizedScroll = this._normalizeScroll(scroll);
    scroll === normalizedScroll ? this._startMomentumScroll(scroll, event) : this._killOverScroll(normalizedScroll);
    this.shouldBlock && this.toggleBlocker(false);
  }
  _startMomentumScroll(scroll, event) {
    const velocity = this._getVelocity(event), matchesDirection = event.gesture.interimDirection === this.dM.dir[this._getDelta(event) < 0 ? 0 : 1];
    const nextScroll = this._getAutoScroll(scroll, velocity, matchesDirection);
    let duration = Math.abs(nextScroll - scroll) / (velocity + 0.01) / 1e3;
    duration = Math.min(0.25, Math.max(0.1, duration));
    this._changeTo(nextScroll, { swipe: true, animationOptions: { duration, timing: "cubic-bezier(.4, .7, .5, 1)" } });
  }
  _killOverScroll(scroll) {
    this._scroll = scroll;
    const direction = this.dM.dir[Number(scroll > 0)];
    const killOverScroll = () => this._changeTo(scroll, { animationOptions: { duration: 0.4, timing: "cubic-bezier(.1, .4, .1, 1)" } });
    this.overScrollHook({ direction, killOverScroll }) || killOverScroll();
  }
  _changeTo(scroll, options = {}) {
    const e = { activeIndex: this.getActiveIndex(scroll), lastActiveIndex: this._lastActiveIndex, swipe: options.swipe || false };
    const change = e.activeIndex !== e.lastActiveIndex;
    const canceled = change ? this.preChangeHook(e) : false;
    this._scroll = canceled ? this._offset + e.lastActiveIndex * this.itemNumSize : scroll;
    this._lastActiveIndex = canceled ? e.lastActiveIndex : e.activeIndex;
    return this._scrollTo(this._scroll, options).then(() => {
      if (scroll === this._scroll && !canceled) {
        this._setSwiping(false);
        change && this.postChangeHook(e);
      } else if (options.reject) {
        this._setSwiping(false);
        return Promise.reject("Canceled");
      }
    });
  }
  _scrollTo(scroll, options = {}) {
    if (options.throttle) {
      const ratio = 0.35;
      if (scroll < 0) {
        scroll = this.isOverScrollable() ? Math.round(scroll * ratio) : 0;
      } else {
        const maxScroll = this.maxScroll;
        if (maxScroll < scroll) {
          scroll = this.isOverScrollable() ? maxScroll + Math.round((scroll - maxScroll) * ratio) : maxScroll;
        }
      }
    }
    const opt = options.animation === "none" ? {} : options.animationOptions;
    this.scrollHook && this.itemNumSize > 0 && this.scrollHook((scroll / this.itemNumSize).toFixed(2), options.animationOptions || {});
    return new Promise(
      (resolve) => animit_default(this.target).queue({ transform: this._getTransform(scroll) }, opt).play(resolve)
    );
  }
  _getAutoScroll(scroll, velocity, matchesDirection) {
    const max = this.maxScroll, offset = this._offset, size = this.itemNumSize;
    if (!this.isAutoScrollable()) {
      return Math.max(0, Math.min(max, scroll));
    }
    let arr = [];
    for (let s = offset; s < max; s += size) {
      arr.push(s);
    }
    arr.push(max);
    arr = arr.sort((left, right) => Math.abs(left - scroll) - Math.abs(right - scroll)).filter((item, pos) => !pos || item !== arr[pos - 1]);
    let result = arr[0];
    const lastScroll = this._lastActiveIndex * size + offset;
    const scrollRatio = Math.abs(scroll - lastScroll) / size;
    if (scrollRatio <= this.getAutoScrollRatio(matchesDirection, velocity, size)) {
      result = lastScroll;
    } else {
      if (scrollRatio < 1 && arr[0] === lastScroll && arr.length > 1) {
        result = arr[1];
      }
    }
    return Math.max(0, Math.min(max, result));
  }
  _reset() {
    this._targetSize = this._itemNumSize = void 0;
  }
  _normalizeScroll(scroll) {
    return Math.max(Math.min(scroll, this.maxScroll), 0);
  }
  refresh() {
    this._reset();
    this._updateLayout();
    if (util_default.isInteger(this._scroll)) {
      const scroll = this._normalizeScroll(this._scroll);
      scroll !== this._scroll ? this._killOverScroll(scroll) : this._changeTo(scroll);
    } else {
      this._setupInitialIndex();
    }
    this.refreshHook();
  }
  get targetSize() {
    if (!this._targetSize) {
      this._targetSize = this.target[`offset${this.dM.size}`];
    }
    return this._targetSize;
  }
  _getDelta(event) {
    return event.gesture[`delta${this.dM.axis}`];
  }
  _getVelocity(event) {
    return event.gesture[`velocity${this.dM.axis}`];
  }
  _getTransform(scroll) {
    return `translate3d(${this.dM.t3d[0]}${-scroll}${this.dM.t3d[1]})`;
  }
  _updateLayout() {
    this.dM = directionMap[this.isVertical() ? "vertical" : "horizontal"];
    this.target.classList.toggle("ons-swiper-target--vertical", this.isVertical());
    for (let c = this.target.children[0]; c; c = c.nextElementSibling) {
      c.style[this.dM.size.toLowerCase()] = this.itemSize;
    }
    if (this.isCentered()) {
      this._offset = (this.targetSize - this.itemNumSize) / -2 || 0;
    }
  }
};

// node_modules/onsenui/esm/elements/ons-carousel.js
var CarouselElement = class extends BaseElement {
  /**
   * @event postchange
   * @description
   *   [en]Fired just after the current carousel item has changed.[/en]
   *   [ja]現在表示しているカルーセルの要素が変わった時に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.carousel
   *   [en]Carousel object.[/en]
   *   [ja]イベントが発火したCarouselオブジェクトです。[/ja]
   * @param {Number} event.activeIndex
   *   [en]Current active index.[/en]
   *   [ja]現在アクティブになっている要素のインデックス。[/ja]
   * @param {Number} event.lastActiveIndex
   *   [en]Previous active index.[/en]
   *   [ja]以前アクティブだった要素のインデックス。[/ja]
   */
  /**
   * @event prechange
   * @description
   *   [en]Fired just before the current carousel item changes.[/en]
   *   [ja][/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.carousel
   *   [en]Carousel object.[/en]
   *   [ja]イベントが発火したCarouselオブジェクトです。[/ja]
   * @param {Number} event.activeIndex
   *   [en]Current active index.[/en]
   *   [ja]現在アクティブになっている要素のインデックス。[/ja]
   * @param {Number} event.lastActiveIndex
   *   [en]Previous active index.[/en]
   *   [ja]以前アクティブだった要素のインデックス。[/ja]
   */
  /**
   * @event refresh
   * @description
   *   [en]Fired when the carousel has been refreshed.[/en]
   *   [ja]カルーセルが更新された時に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.carousel
   *   [en]Carousel object.[/en]
   *   [ja]イベントが発火したCarouselオブジェクトです。[/ja]
   */
  /**
   * @event overscroll
   * @description
   *   [en]Fired when the carousel has been overscrolled.[/en]
   *   [ja]カルーセルがオーバースクロールした時に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.carousel
   *   [en]Fired when the carousel has been refreshed.[/en]
   *   [ja]カルーセルが更新された時に発火します。[/ja]
   * @param {Number} event.activeIndex
   *   [en]Current active index.[/en]
   *   [ja]現在アクティブになっている要素のインデックス。[/ja]
   * @param {String} event.direction
   *   [en]Can be one of either "up", "down", "left" or "right".[/en]
   *   [ja]オーバースクロールされた方向が得られます。"up", "down", "left", "right"のいずれかの方向が渡されます。[/ja]
   * @param {Function} event.waitToReturn
   *   [en]Takes a <code>Promise</code> object as an argument. The carousel will not scroll back until the promise has been resolved or rejected.[/en]
   *   [ja]この関数はPromiseオブジェクトを引数として受け取ります。渡したPromiseオブジェクトがresolveされるかrejectされるまで、カルーセルはスクロールバックしません。[/ja]
   */
  /**
   * @event swipe
   * @description
   *   [en]Fires when the carousel swipes.[/en]
   *   [ja][/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Number} event.index
   *   [en]Current index.[/en]
   *   [ja]現在アクティブになっているons-carouselのインデックスを返します。[/ja]
   * @param {Object} event.options
   *   [en]Animation options object.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute direction
   * @type {String}
   * @description
   *   [en]The direction of the carousel. Can be either "horizontal" or "vertical". Default is "horizontal".[/en]
   *   [ja]カルーセルの方向を指定します。"horizontal"か"vertical"を指定できます。"horizontal"がデフォルト値です。[/ja]
   */
  /**
   * @attribute fullscreen
   * @description
   *   [en]If this attribute is set the carousel will cover the whole screen.[/en]
   *   [ja]この属性があると、absoluteポジションを使ってカルーセルが自動的に画面いっぱいに広がります。[/ja]
   */
  /**
   * @property fullscreen
   * @type {Boolean}
   * @description
   *   [en]If this property is set the carousel will cover the whole screen.[/en]
   *   [ja]この属性があると、absoluteポジションを使ってカルーセルが自動的に画面いっぱいに広がります。[/ja]
   */
  /**
   * @attribute overscrollable
   * @description
   *   [en]If this attribute is set the carousel will be scrollable over the edge. It will bounce back when released.[/en]
   *   [ja]この属性がある時、タッチやドラッグで端までスクロールした時に、バウンドするような効果が当たります。[/ja]
   */
  /**
   * @attribute centered
   * @description
   *   [en]If this attribute is set the carousel then the selected item will be in the center of the carousel instead of the beginning. Useful only when the items are smaller than the carousel. [/en]
   *   [ja]この属性がある時、選んでいるons-carousel-itemはカルーセルの真ん中へ行きます。項目がカルーセルよりも小さい場合にのみ、これは便利です。[/ja]
   */
  /**
   * @attribute item-width
   * @type {String}
   * @description
   *    [en]ons-carousel-item's width. Only works when the direction is set to "horizontal".[/en]
   *    [ja]ons-carousel-itemの幅を指定します。この属性は、direction属性に"horizontal"を指定した時のみ有効になります。[/ja]
   */
  /**
   * @attribute item-height
   * @type {String}
   * @description
   *   [en]ons-carousel-item's height. Only works when the direction is set to "vertical".[/en]
   *   [ja]ons-carousel-itemの高さを指定します。この属性は、direction属性に"vertical"を指定した時のみ有効になります。[/ja]
   */
  /**
   * @attribute auto-scroll
   * @description
   *   [en]If this attribute is set the carousel will be automatically scrolled to the closest item border when released.[/en]
   *   [ja]この属性がある時、一番近いcarousel-itemの境界まで自動的にスクロールするようになります。[/ja]
   */
  /**
   * @attribute auto-scroll-ratio
   * @type {Number}
   * @description
   *    [en]A number between 0.0 and 1.0 that specifies how much the user must drag the carousel in order for it to auto scroll to the next item.[/en]
   *    [ja]0.0から1.0までの値を指定します。カルーセルの要素をどれぐらいの割合までドラッグすると次の要素に自動的にスクロールするかを指定します。[/ja]
   */
  /**
   * @attribute swipeable
   * @description
   *   [en]If this attribute is set the carousel can be scrolled by drag or swipe.[/en]
   *   [ja]この属性がある時、カルーセルをスワイプやドラッグで移動できるようになります。[/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]If this attribute is set the carousel is disabled.[/en]
   *   [ja]この属性がある時、dragやtouchやswipeを受け付けなくなります。[/ja]
   */
  /**
   * @attribute initial-index
   * @initonly
   * @default 0
   * @type {Number}
   * @description
   *   [en]Specify the index of the ons-carousel-item to show initially. Default is 0. If active-index is set, initial-index is ignored.[/en]
   *   [ja]最初に表示するons-carousel-itemを0始まりのインデックスで指定します。デフォルト値は 0 です。[/ja]
   */
  /**
   * @attribute auto-refresh
   * @description
   *   [en]When this attribute is set the carousel will automatically refresh when the number of child nodes change.[/en]
   *   [ja]この属性がある時、子要素の数が変わるとカルーセルは自動的に更新されるようになります。[/ja]
   */
  /**
   * @property autoRefresh
   * @type {Boolean}
   * @description
   *   [en]When this property is set the carousel will automatically refresh when the number of child nodes change.[/en]
   *   [ja]この属性がある時、子要素の数が変わるとカルーセルは自動的に更新されるようになります。[/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @description
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  constructor() {
    super();
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "swipe");
    this._connectOnSwipe = onConnected;
    this._disconnectOnSwipe = onDisconnected;
    contentReady(this, () => this._compile());
  }
  _compile() {
    const target = this.children[0] && this.children[0].tagName !== "ONS-CAROUSEL-ITEM" && this.children[0] || document.createElement("div");
    if (!target.parentNode) {
      while (this.firstChild) {
        target.appendChild(this.firstChild);
      }
      this.appendChild(target);
    }
    !this.children[1] && this.appendChild(document.createElement("div"));
    this.appendChild = this.appendChild.bind(target);
    this.insertBefore = this.insertBefore.bind(target);
  }
  connectedCallback() {
    if (!this._swiper) {
      this._swiper = new Swiper({
        getElement: () => this,
        getInitialIndex: () => this.getAttribute("active-index") || this.getAttribute("initial-index"),
        getAutoScrollRatio: () => this.autoScrollRatio,
        isVertical: () => this.vertical,
        isOverScrollable: () => this.overscrollable,
        isCentered: () => this.centered,
        isAutoScrollable: () => this.autoScroll,
        itemSize: this.itemSize,
        overScrollHook: this._onOverScroll.bind(this),
        preChangeHook: this._onPreChange.bind(this),
        postChangeHook: this._onPostChange.bind(this),
        refreshHook: this._onRefresh.bind(this),
        scrollHook: (index, options) => util_default.triggerElementEvent(this, "swipe", { index, options })
      });
      contentReady(this, () => this._swiper.init({
        swipeable: this.hasAttribute("swipeable"),
        autoRefresh: this.hasAttribute("auto-refresh")
      }));
    }
    this._connectOnSwipe();
  }
  disconnectedCallback() {
    if (this._swiper && this._swiper.initialized) {
      this._swiper.dispose();
      this._swiper = null;
    }
    this._disconnectOnSwipe();
  }
  static get observedAttributes() {
    return ["swipeable", "auto-refresh", "direction", "item-height", "item-width", "active-index"];
  }
  attributeChangedCallback(name44, last, current) {
    if (!this._swiper) {
      return;
    }
    switch (name44) {
      case "swipeable":
        this._swiper.updateSwipeable(this.hasAttribute("swipeable"));
        break;
      case "auto-refresh":
        this._swiper.updateAutoRefresh(this.hasAttribute("auto-refresh"));
        break;
      case "item-height":
        this.vertical && this._swiper.updateItemSize(this.itemSize);
        break;
      case "item-width":
        this.vertical || this._swiper.updateItemSize(this.itemSize);
        break;
      case "direction":
        this._swiper.refresh();
        break;
      case "active-index":
        if (this.getActiveIndex() !== this.activeIndex) {
          this.setActiveIndex(this.activeIndex);
        }
        break;
    }
  }
  _show() {
    this._swiper.show();
  }
  _hide() {
    this._swiper.hide();
  }
  _onOverScroll({ direction, killOverScroll }) {
    let waitForAction = false;
    util_default.triggerElementEvent(this, "overscroll", {
      carousel: this,
      activeIndex: this.getActiveIndex(),
      direction,
      waitToReturn: (promise) => {
        waitForAction = true;
        promise.then(killOverScroll);
      }
    });
    return waitForAction;
  }
  _onPreChange({ activeIndex, lastActiveIndex }) {
    util_default.triggerElementEvent(this, "prechange", { carousel: this, activeIndex, lastActiveIndex });
  }
  _onPostChange({ activeIndex, lastActiveIndex }) {
    this.activeIndex = activeIndex;
    util_default.triggerElementEvent(this, "postchange", { carousel: this, activeIndex, lastActiveIndex });
  }
  _onRefresh() {
    util_default.triggerElementEvent(this, "refresh", { carousel: this });
  }
  /**
   * @method setActiveIndex
   * @signature setActiveIndex(index, [options])
   * @param {Number} index
   *   [en]The index that the carousel should be set to.[/en]
   *   [ja]carousel要素のインデックスを指定します。[/ja]
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja][/ja]
   * @param {Function} [options.callback]
   *   [en]A function that will be called after the animation is finished.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   * @param {Object} [options.animationOptions]
   *   [en]An object that can be used to specify duration, delay and timing function of the animation.[/en]
   *   [ja][/ja]
   * @description
   *   [en]Specify the index of the `<ons-carousel-item>` to show.[/en]
   *   [ja]表示するons-carousel-itemをindexで指定します。[/ja]
   * @return {Promise}
   *   [en]Resolves to the carousel element.[/en]
   *   [ja][/ja]
   */
  setActiveIndex(index, options = {}) {
    options = {
      animation: this.getAttribute("animation"),
      animationOptions: this.animationOptions || { duration: 0.3, timing: "cubic-bezier(.4, .7, .5, 1)" },
      ...options
    };
    return this._swiper.setActiveIndex(index, options).then(() => {
      options.callback instanceof Function && options.callback(this);
      return Promise.resolve(this);
    });
  }
  /**
   * @method getActiveIndex
   * @signature getActiveIndex()
   * @return {Number}
   *   [en]The current carousel item index.[/en]
   *   [ja]現在表示しているカルーセル要素のインデックスが返されます。[/ja]
   * @description
   *   [en]Returns the index of the currently visible `<ons-carousel-item>`.[/en]
   *   [ja]現在表示されているons-carousel-item要素のインデックスを返します。[/ja]
   */
  getActiveIndex() {
    return this._swiper.getActiveIndex();
  }
  /**
   * @method next
   * @signature next([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja][/ja]
   * @param {Function} [options.callback]
   *   [en]A function that will be executed after the animation has finished.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   * @param {Object} [options.animationOptions]
   *   [en]An object that can be used to specify the duration, delay and timing function of the animation.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the carousel element[/en]
   *   [ja][/ja]
   * @description
   *   [en]Show next `<ons-carousel-item>`.[/en]
   *   [ja]次のons-carousel-itemを表示します。[/ja]
   */
  next(options) {
    return this.setActiveIndex(this.getActiveIndex() + 1, options);
  }
  /**
   * @method prev
   * @signature prev([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja][/ja]
   * @param {Function} [options.callback]
   *   [en]A function that will be executed after the animation has finished.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   * @param {Object} [options.animationOptions]
   *   [en]An object that can be used to specify the duration, delay and timing function of the animation.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the carousel element[/en]
   *   [ja][/ja]
   * @description
   *   [en]Show previous `<ons-carousel-item>`.[/en]
   *   [ja]前のons-carousel-itemを表示します。[/ja]
   */
  prev(options) {
    return this.setActiveIndex(this.getActiveIndex() - 1, options);
  }
  /**
   * @method first
   * @signature first()
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja][/ja]
   * @param {Function} [options.callback]
   *   [en]A function that will be executed after the animation has finished.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this is set to `"none"`, the transitions will not be animated.[/en]
   *   [ja][/ja]
   * @param {Object} [options.animationOptions]
   *   [en]An object that can be used to specify the duration, delay and timing function of the animation.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the carousel element[/en]
   *   [ja][/ja]
   * @description
   *   [en]Show first `<ons-carousel-item>`.[/en]
   *   [ja]最初のons-carousel-itemを表示します。[/ja]
   */
  first(options) {
    return this.setActiveIndex(0, options);
  }
  /**
   * @method last
   * @signature last()
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja][/ja]
   * @param {Function} [options.callback]
   *   [en]A function that will be executed after the animation has finished.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   * @param {Object} [options.animationOptions]
   *   [en]An object that can be used to specify the duration, delay and timing function of the animation.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the carousel element[/en]
   *   [ja]Resolves to the carousel element[/ja]
   * @description
   *   [en]Show last ons-carousel item.[/en]
   *   [ja]最後のons-carousel-itemを表示します。[/ja]
   */
  last(options) {
    this.setActiveIndex(Math.max(this.itemCount - 1, 0), options);
  }
  /**
   * @method refresh
   * @signature refresh()
   * @description
   *   [en]Update the layout of the carousel. Used when adding `<ons-carousel-items>` dynamically or to automatically adjust the size.[/en]
   *   [ja]レイアウトや内部の状態を最新のものに更新します。ons-carousel-itemを動的に増やしたり、ons-carouselの大きさを動的に変える際に利用します。[/ja]
   */
  refresh() {
    this._swiper.refresh();
  }
  /**
   * @property itemCount
   * @readonly
   * @type {Number}
   * @description
   *   [en]The number of carousel items.[/en]
   *   [ja]カルーセル要素の数です。[/ja]
   */
  get itemCount() {
    return this._swiper.itemCount;
  }
  /**
   * @property swipeable
   * @type {Boolean}
   * @description
   *   [en]true if the carousel is swipeable.[/en]
   *   [ja]swipeableであればtrueを返します。[/ja]
   */
  /**
   * @property onSwipe
   * @type {Function}
   * @description
   *   [en]Hook called whenever the user slides the carousel. It gets a decimal index and an animationOptions object as arguments.[/en]
   *   [ja][/ja]
   */
  /**
   * @property autoScroll
   * @type {Boolean}
   * @description
   *   [en]true if auto scroll is enabled.[/en]
   *   [ja]オートスクロールが有効であればtrueを返します。[/ja]
   */
  get vertical() {
    return this.getAttribute("direction") === "vertical";
  }
  get itemSize() {
    const itemSizeAttr = (this.getAttribute(`item-${this.vertical ? "height" : "width"}`) || "").trim();
    return itemSizeAttr.match(/^\d+(px|%)$/) ? itemSizeAttr : "100%";
  }
  /**
   * @property autoScrollRatio
   * @type {Number}
   * @description
   *   [en]The current auto scroll ratio. [/en]
   *   [ja]現在のオートスクロールのratio値。[/ja]
   */
  get autoScrollRatio() {
    return parseFloat(this.getAttribute("auto-scroll-ratio"));
  }
  set autoScrollRatio(ratio) {
    this.setAttribute("auto-scroll-ratio", ratio);
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the carousel is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  /**
   * @property overscrollable
   * @type {Boolean}
   * @description
   *   [en]Whether the carousel is overscrollable or not.[/en]
   *   [ja]overscrollできればtrueを返します。[/ja]
   */
  /**
   * @property centered
   * @type {Boolean}
   * @description
   *   [en]Whether the carousel is centered or not.[/en]
   *   [ja]centered状態になっていればtrueを返します。[/ja]
   */
  static get events() {
    return ["postchange", "refresh", "overscroll", "prechange", "swipe"];
  }
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *   [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。例：{duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  get animationOptions() {
    const attr = this.getAttribute("animation-options");
    if (attr) {
      return util_default.animationOptionsParse(attr);
    } else {
      return attr;
    }
  }
  set animationOptions(value) {
    if (value === void 0 || value === null) {
      this.removeAttribute("animation-options");
    } else {
      this.setAttribute("animation-options", JSON.stringify(value));
    }
  }
  /**
   * @attribute active-index
   * @type {Number}
   * @description
   *   [en]Specify the index of the carousel item that should be shown.[/en]
   *   [ja][/ja]
   */
  /**
   * @property activeIndex
   * @type {Number}
   * @description
   *   [en]Specify the index of the carousel item that should be shown.[/en]
   *   [ja][/ja]
   */
  get activeIndex() {
    return parseInt(this.getAttribute("active-index"));
  }
  set activeIndex(value) {
    if (value !== void 0 && value !== null) {
      this.setAttribute("active-index", value);
    }
  }
};
util_default.defineBooleanProperties(CarouselElement, ["swipeable", "disabled", "overscrollable", "auto-scroll", "centered", "fullscreen", "auto-refresh"]);
elements_default.Carousel = CarouselElement;
customElements.define("ons-carousel", CarouselElement);

// node_modules/vue-onsenui/esm/components/VOnsCarousel.js
var name37 = "v-ons-carousel";
var script37 = {
  name: name37,
  mixins: [deriveEvents(name37)],
  emits: ["update:activeIndex"]
};
var _hoisted_128 = createBaseVNode("div", null, null, -1);
function render38(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-carousel", {
    onPostchange: _cache[0] || (_cache[0] = withModifiers(($event) => _ctx.$emit("update:activeIndex", $event.activeIndex), ["self"]))
  }, [
    createBaseVNode("div", null, [
      renderSlot(_ctx.$slots, "default")
    ]),
    _hoisted_128
  ], 32);
}
script37.render = render38;

// node_modules/onsenui/esm/elements/ons-tabbar.js
var scheme27 = {
  ".tabbar__content": "tabbar--*__content",
  ".tabbar__border": "tabbar--*__border",
  ".tabbar": "tabbar--*"
};
var rewritables2 = {
  /**
   * @param {Element} tabbarElement
   * @param {Function} callback
   */
  ready(tabbarElement, callback) {
    callback();
  }
};
var nullPage = internal_default.nullElement;
var lerp = (x0, x1, t) => (1 - t) * x0 + t * x1;
var TabbarElement = class extends BaseElement {
  /**
   * @event prechange
   * @description
   *   [en]Fires just before the tab is changed.[/en]
   *   [ja]アクティブなタブが変わる前に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Number} event.index
   *   [en]Current index.[/en]
   *   [ja]現在アクティブになっているons-tabのインデックスを返します。[/ja]
   * @param {Object} event.tabItem
   *   [en]Tab item object.[/en]
   *   [ja]tabItemオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call this function to cancel the change event.[/en]
   *   [ja]この関数を呼び出すと、アクティブなタブの変更がキャンセルされます。[/ja]
   */
  /**
   * @event postchange
   * @description
   *   [en]Fires just after the tab is changed.[/en]
   *   [ja]アクティブなタブが変わった後に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Number} event.index
   *   [en]Current index.[/en]
   *   [ja]現在アクティブになっているons-tabのインデックスを返します。[/ja]
   * @param {Object} event.tabItem
   *   [en]Tab item object.[/en]
   *   [ja]tabItemオブジェクト。[/ja]
   */
  /**
   * @event reactive
   * @description
   *   [en]Fires if the already open tab is tapped again.[/en]
   *   [ja]すでにアクティブになっているタブがもう一度タップやクリックされた場合に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Number} event.index
   *   [en]Current index.[/en]
   *   [ja]現在アクティブになっているons-tabのインデックスを返します。[/ja]
   * @param {Object} event.tabItem
   *   [en]Tab item object.[/en]
   *   [ja]tabItemオブジェクト。[/ja]
   */
  /**
   * @event swipe
   * @description
   *   [en]Fires when the tabbar swipes.[/en]
   *   [ja][/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Number} event.index
   *   [en]Current index.[/en]
   *   [ja]現在アクティブになっているons-tabのインデックスを返します。[/ja]
   * @param {Object} event.options
   *   [en]Animation options object.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default none
   * @description
   *   [en]If this attribute is set to `"none"` the transitions will not be animated.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. {duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. {duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute position
   * @initonly
   * @type {String}
   * @default bottom
   * @description
   *   [en]Tabbar's position. Available values are `"bottom"` and `"top"`. Use `"auto"` to choose position depending on platform (bottom for iOS flat design, top for Material Design).[/en]
   *   [ja]タブバーの位置を指定します。"bottom"もしくは"top"を選択できます。デフォルトは"bottom"です。[/ja]
   */
  /**
   * @attribute swipeable
   * @description
   *   [en]If this attribute is set the tab bar can be scrolled by drag or swipe.[/en]
   *   [ja]この属性がある時、タブバーをスワイプやドラッグで移動できるようになります。[/ja]
   */
  /**
   * @attribute ignore-edge-width
   * @type {Number}
   * @default 20
   * @description
   *   [en]Distance in pixels from both edges. Swiping on these areas will prioritize parent components such as `ons-splitter` or `ons-navigator`.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute active-index
   * @type {Number}
   * @default 0
   * @description
   *   [en]The index of the tab that is currently active.[/en]
   *   [ja][/ja]
   */
  /**
   * @property activeIndex
   * @type {Number}
   * @default 0
   * @description
   *   [en]The index of the tab that is currently active.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute hide-tabs
   * @description
   *   [en]Whether to hide the tabs.[/en]
   *   [ja]タブを非表示にする場合に指定します。[/ja]
   */
  /**
   * @property hideTabs
   * @description
   *   [en]Whether to hide the tabs.[/en]
   *   [ja]タブを非表示にする場合に指定します。[/ja]
   */
  /**
   * @attribute tab-border
   * @description
   *   [en]If this attribute is set the tabs show a dynamic bottom border. Only works for iOS flat design since the border is always visible in Material Design.[/en]
   *   [ja][/ja]
   */
  /**
   * @property tabBorder
   * @type {Boolean}
   * @description
   *   [en]If this property is set the tabs show a dynamic bottom border. Only works for iOS flat design since the border is always visible in Material Design.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]The appearance of the tabbar.[/en]
   *   [ja]タブバーの表現を指定します。[/ja]
   */
  constructor() {
    super();
    this._loadInactive = util_default.defer();
    contentReady(this, () => this._compile());
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "swipe");
    this._connectOnSwipe = onConnected;
    this._disconnectOnSwipe = onDisconnected;
  }
  connectedCallback() {
    if (!this._swiper) {
      this._swiper = new Swiper({
        getElement: () => this._contentElement,
        getInitialIndex: () => this.activeIndex || this.getAttribute("activeIndex"),
        getAutoScrollRatio: this._getAutoScrollRatio.bind(this),
        getBubbleWidth: () => parseInt(this.getAttribute("ignore-edge-width") || 25, 10),
        isAutoScrollable: () => true,
        preChangeHook: this._onPreChange.bind(this),
        postChangeHook: this._onPostChange.bind(this),
        refreshHook: this._onRefresh.bind(this),
        scrollHook: this._onScroll.bind(this)
      });
      contentReady(this, () => {
        this._tabbarBorder = util_default.findChild(this._tabbarElement, ".tabbar__border");
        this._swiper.init({ swipeable: this.hasAttribute("swipeable") });
      });
    }
    contentReady(this, () => {
      this._updatePosition();
      this._updateVisibility();
      if (!util_default.findParent(this, "ons-page", (p) => p === document.body)) {
        this._show();
      }
    });
    this._connectOnSwipe();
  }
  disconnectedCallback() {
    if (this._swiper && this._swiper.initialized) {
      this._swiper.dispose();
      this._swiper = null;
      this._tabbarBorder = null;
      this._tabsRect = null;
    }
    this._disconnectOnSwipe();
  }
  _normalizeEvent(event) {
    return { ...event, index: event.activeIndex, tabItem: this.tabs[event.activeIndex] };
  }
  _onPostChange(event) {
    event = this._normalizeEvent(event);
    util_default.triggerElementEvent(this, "postchange", event);
    const page = event.tabItem.pageElement;
    page && page._show();
  }
  _onPreChange(event) {
    event = this._normalizeEvent(event);
    event.cancel = () => event.canceled = true;
    util_default.triggerElementEvent(this, "prechange", event);
    if (!event.canceled) {
      const { activeIndex, lastActiveIndex } = event;
      const tabs = this.tabs;
      tabs[activeIndex].setActive(true);
      if (lastActiveIndex >= 0) {
        const prevTab = tabs[lastActiveIndex];
        prevTab.setActive(false);
        prevTab.pageElement && prevTab.pageElement._hide();
      }
    }
    return event.canceled;
  }
  _onScroll(index, options = {}) {
    if (this._tabbarBorder) {
      this._tabbarBorder.style.transition = `all ${options.duration || 0}s ${options.timing || ""}`;
      if (this._autogrow && this._tabsRect.length > 0) {
        const a = Math.floor(index), b = Math.ceil(index), r = index % 1;
        this._tabbarBorder.style.width = lerp(this._tabsRect[a].width, this._tabsRect[b].width, r) + "px";
        this._tabbarBorder.style.transform = `translate3d(${lerp(this._tabsRect[a].left, this._tabsRect[b].left, r)}px, 0, 0)`;
      } else {
        this._tabbarBorder.style.transform = `translate3d(${index * 100}%, 0, 0)`;
      }
    }
    util_default.triggerElementEvent(this, "swipe", { index, options });
  }
  _onRefresh() {
    this._autogrow = util_default.hasModifier(this, "autogrow");
    this._tabsRect = this.tabs.map((tab) => tab.getBoundingClientRect());
    if (this._tabbarBorder) {
      this._tabbarBorder.style.display = this.hasAttribute("tab-border") || util_default.hasModifier(this, "material") ? "block" : "none";
      const index = this.getActiveTabIndex();
      if (this._tabsRect.length > 0 && index >= 0) {
        this._tabbarBorder.style.width = this._tabsRect[index].width + "px";
      }
    }
  }
  _getAutoScrollRatio(matches, velocity, size) {
    const ratio = 0.6;
    const modifier2 = size / 300 * (matches ? -1 : 1);
    return Math.min(1, Math.max(0, ratio + velocity * modifier2));
  }
  get _tabbarElement() {
    return util_default.findChild(this, ".tabbar");
  }
  get _contentElement() {
    return util_default.findChild(this, ".tabbar__content");
  }
  get _targetElement() {
    const content = this._contentElement;
    return content && content.children[0] || null;
  }
  _compile() {
    autostyle_default.prepare(this);
    const content = this._contentElement || util_default.create(".tabbar__content");
    content.classList.add("ons-tabbar__content");
    const tabbar = this._tabbarElement || util_default.create(".tabbar");
    tabbar.classList.add("ons-tabbar__footer");
    if (!tabbar.parentNode) {
      while (this.firstChild) {
        tabbar.appendChild(this.firstChild);
      }
    }
    if (tabbar.children.length > this.activeIndex && !util_default.findChild(tabbar, "[active]")) {
      tabbar.children[this.activeIndex].setAttribute("active", "");
    }
    this._tabbarBorder = util_default.findChild(tabbar, ".tabbar__border") || util_default.create(".tabbar__border");
    tabbar.appendChild(this._tabbarBorder);
    tabbar.classList.add("ons-swiper-tabbar");
    !content.children[0] && content.appendChild(document.createElement("div"));
    !content.children[1] && content.appendChild(document.createElement("div"));
    content.appendChild = content.appendChild.bind(content.children[0]);
    content.insertBefore = content.insertBefore.bind(content.children[0]);
    this.appendChild(content);
    this.appendChild(tabbar);
    ModifierUtil.initModifier(this, scheme27);
  }
  _updatePosition(position = this.getAttribute("position")) {
    const top = this._top = position === "top" || position === "auto" && util_default.hasModifier(this, "material");
    const action = top ? util_default.addModifier : util_default.removeModifier;
    action(this, "top");
    const page = util_default.findParent(this, "ons-page");
    if (page) {
      contentReady(page, () => {
        let p = 0;
        if (page.children[0] && util_default.match(page.children[0], "ons-toolbar")) {
          action(page.children[0], "noshadow");
          p = 1;
        }
        const content = page._getContentElement();
        const cs = window.getComputedStyle(page._getContentElement(), null);
        this.style.top = top ? parseInt(cs.getPropertyValue("padding-top"), 10) - p + "px" : "";
        content.style.top = cs.top;
        content.style.top = "";
      });
    }
    internal_default.autoStatusBarFill(() => {
      const filled = util_default.findParent(this, (e) => e.hasAttribute("status-bar-fill"));
      util_default.toggleAttribute(this, "status-bar-fill", top && !filled);
    });
  }
  get topPage() {
    const tabs = this.tabs, index = this.getActiveTabIndex();
    return tabs[index] ? tabs[index].pageElement || this.pages[0] || null : null;
  }
  get pages() {
    return util_default.arrayFrom(this._targetElement.children);
  }
  get tabs() {
    return Array.prototype.filter.call(this._tabbarElement.children, (e) => e.tagName === "ONS-TAB");
  }
  /**
   * @method setActiveTab
   * @signature setActiveTab(index, [options])
   * @param {Number} index
   *   [en]Tab index.[/en]
   *   [ja]タブのインデックスを指定します。[/ja]
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {Function} [options.callback]
   *   [en]Function that runs when the new page has loaded.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]If this option is "none", the transition won't slide.[/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @description
   *   [en]Show specified tab page. Animations and their options can be specified by the second parameter.[/en]
   *   [ja]指定したインデックスのタブを表示します。アニメーションなどのオプションを指定できます。[/ja]
   * @return {Promise}
   *   [en]A promise that resolves to the new page element.[/en]
   *   [ja][/ja]
   */
  setActiveTab(nextIndex, options = {}) {
    const previousIndex = this.activeIndex;
    this._activeIndexSkipEffect = true;
    this.activeIndex = nextIndex;
    return this._updateActiveIndex(nextIndex, previousIndex, options);
  }
  _updateActiveIndex(nextIndex, prevIndex, options = {}) {
    const prevTab = this.tabs[prevIndex], nextTab = this.tabs[nextIndex];
    if (!nextTab) {
      return Promise.reject("Specified index does not match any tab.");
    }
    if (nextIndex === prevIndex) {
      util_default.triggerElementEvent(this, "reactive", { index: nextIndex, activeIndex: nextIndex, tabItem: nextTab });
      return Promise.resolve(nextTab.pageElement);
    }
    const nextPage = nextTab.pageElement;
    return (nextPage ? Promise.resolve(nextPage) : nextTab.loaded).then((nextPage2) => this._swiper.setActiveIndex(nextIndex, {
      reject: true,
      ...options,
      animation: prevTab && nextPage2 ? options.animation || this.getAttribute("animation") : "none",
      animationOptions: util_default.extend(
        { duration: 0.3, timing: "cubic-bezier(.4, .7, .5, 1)" },
        this.animationOptions,
        options.animationOptions || {}
      )
    }).then(() => {
      options.callback instanceof Function && options.callback(nextPage2);
      return nextPage2;
    }));
  }
  /**
   * @method setTabbarVisibility
   * @signature setTabbarVisibility(visible)
   * @param {Boolean} visible
   * @description
   *   [en]Used to hide or show the tab bar.[/en]
   *   [ja][/ja]
   */
  setTabbarVisibility(visible) {
    this.hideTabs = !visible;
  }
  show() {
    this.hideTabs = false;
  }
  hide() {
    this.hideTabs = true;
  }
  _updateVisibility() {
    contentReady(this, () => {
      const visible = !this.hideTabs;
      this._contentElement.style[this._top ? "top" : "bottom"] = visible ? "" : "0px";
      this._tabbarElement.style.display = visible ? "" : "none";
      visible && this._onRefresh();
    });
  }
  /**
   * @property visible
   * @readonly
   * @type {Boolean}
   * @description
   *   [en]Whether the tabbar is visible or not.[/en]
   *   [ja]タブバーが見える場合に`true`。[/ja]
   */
  get visible() {
    return this._tabbarElement.style.display !== "none";
  }
  /**
   * @property swipeable
   * @type {Boolean}
   * @description
   *   [en]Enable swipe interaction.[/en]
   *   [ja]swipeableであればtrueを返します。[/ja]
   */
  /**
   * @property onSwipe
   * @type {Function}
   * @description
   *   [en]Hook called whenever the user slides the tabbar. It gets a decimal index and an animationOptions object as arguments.[/en]
   *   [ja][/ja]
   */
  /**
   * @method getActiveTabIndex
   * @signature getActiveTabIndex()
   * @return {Number}
   *   [en]The index of the currently active tab.[/en]
   *   [ja]現在アクティブになっているタブのインデックスを返します。[/ja]
   * @description
   *   [en]Returns tab index on current active tab. If active tab is not found, returns -1.[/en]
   *   [ja]現在アクティブになっているタブのインデックスを返します。現在アクティブなタブがない場合には-1を返します。[/ja]
   */
  getActiveTabIndex(tabs = this.tabs) {
    for (let i = 0; i < tabs.length; i++) {
      if (tabs[i] && tabs[i].tagName === "ONS-TAB" && tabs[i].isActive()) {
        return i;
      }
    }
    return -1;
  }
  get activeIndex() {
    return Number(this.getAttribute("active-index"));
  }
  set activeIndex(value) {
    if (value !== null && value !== void 0) {
      this.setAttribute("active-index", value);
    }
  }
  _show() {
    this._swiper.show();
    setImmediate(() => {
      const tabs = this.tabs;
      const activeIndex = this.getActiveTabIndex(tabs);
      this._loadInactive.resolve();
      if (tabs.length > 0 && activeIndex >= 0) {
        tabs[activeIndex].loaded.then((el) => el && setImmediate(() => el._show()));
      }
    });
  }
  _hide() {
    this._swiper.hide();
    const topPage = this.topPage;
    topPage && topPage._hide();
  }
  _destroy() {
    this.tabs.forEach((tab) => tab.remove());
    this.remove();
  }
  static get observedAttributes() {
    return ["modifier", "position", "swipeable", "tab-border", "hide-tabs", "active-index"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "modifier") {
      ModifierUtil.onModifierChanged(last, current, this, scheme27);
      const isTop = (m) => /(^|\s+)top($|\s+)/i.test(m);
      isTop(last) !== isTop(current) && this._updatePosition();
    } else if (name44 === "position") {
      util_default.isAttached(this) && this._updatePosition();
    } else if (name44 === "swipeable") {
      this._swiper && this._swiper.updateSwipeable(this.hasAttribute("swipeable"));
    } else if (name44 === "hide-tabs") {
      this.isConnected && this._updateVisibility();
    } else if (name44 === "active-index") {
      if (this._activeIndexSkipEffect) {
        this._activeIndexSkipEffect = false;
      } else if (this.isConnected) {
        contentReady(this, () => this._updateActiveIndex(current, last));
      }
    }
  }
  static get rewritables() {
    return rewritables2;
  }
  static get events() {
    return ["prechange", "postchange", "reactive", "swipe"];
  }
  get animationOptions() {
    return this.hasAttribute("animation-options") ? util_default.animationOptionsParse(this.getAttribute("animation-options")) : {};
  }
  set animationOptions(value) {
    if (value === void 0 || value === null) {
      this.removeAttribute("animation-options");
    } else {
      this.setAttribute("animation-options", JSON.stringify(value));
    }
  }
};
util_default.defineBooleanProperties(TabbarElement, ["hide-tabs", "swipeable", "tab-border"]);
elements_default.Tabbar = TabbarElement;
customElements.define("ons-tabbar", TabbarElement);

// node_modules/onsenui/esm/elements/ons-tab.js
var defaultClassName14 = "tabbar__item";
var scheme28 = {
  "": "tabbar--*__item",
  ".tabbar__button": "tabbar--*__button"
};
var TabElement = class extends BaseElement {
  /**
   * @attribute page
   * @initonly
   * @type {String}
   * @description
   *   [en]The page that is displayed when the tab is tapped.[/en]
   *   [ja]ons-tabが参照するページへのURLを指定します。[/ja]
   */
  /**
   * @attribute icon
   * @type {String}
   * @description
   *   [en]
   *     The icon name for the tab. Can specify the same icon name as `<ons-icon>`. Check [See also](#seealso) section for more information.
   *   [/en]
   *   [ja]
   *     アイコン名を指定します。ons-iconと同じアイコン名を指定できます。
   *     個別にアイコンをカスタマイズする場合は、background-imageなどのCSSスタイルを用いて指定できます。
   *   [/ja]
   */
  /**
   * @attribute active-icon
   * @type {String}
   * @description
   *   [en]The name of the icon when the tab is active.[/en]
   *   [ja]アクティブの際のアイコン名を指定します。[/ja]
   */
  /**
   * @attribute label
   * @type {String}
   * @description
   *   [en]The label of the tab item.[/en]
   *   [ja]アイコン下に表示されるラベルを指定します。[/ja]
   */
  /**
   * @attribute badge
   * @type {String}
   * @description
   *   [en]Display a notification badge on top of the tab.[/en]
   *   [ja]バッジに表示する内容を指定します。[/ja]
   */
  /**
   * @attribute active
   * @description
   *   [en]This attribute should be set to the tab that is active by default.[/en]
   *   [ja][/ja]
   */
  constructor() {
    super();
    if (["label", "icon", "badge"].some(this.hasAttribute.bind(this))) {
      this._compile();
    } else {
      contentReady(this, () => this._compile());
    }
    this._pageLoader = defaultPageLoader;
    this._onClick = this._onClick.bind(this);
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "click");
    this._connectOnClick = onConnected;
    this._disconnectOnClick = onDisconnected;
  }
  set pageLoader(loader) {
    if (!(loader instanceof PageLoader)) {
      util_default.throwPageLoader();
    }
    this._pageLoader = loader;
  }
  get pageLoader() {
    return this._pageLoader;
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName14);
    if (this._button) {
      return;
    }
    const button = util_default.create("button.tabbar__button");
    while (this.childNodes[0]) {
      button.appendChild(this.childNodes[0]);
    }
    const input = util_default.create("input", { display: "none" });
    input.type = "radio";
    this.appendChild(input);
    this.appendChild(button);
    this._updateButtonContent();
    ModifierUtil.initModifier(this, scheme28);
    this._updateRipple();
  }
  _updateRipple() {
    this._button && util_default.updateRipple(this._button, this.hasAttribute("ripple"));
  }
  _updateButtonContent() {
    const button = this._button;
    let iconWrapper = this._icon;
    if (this.hasAttribute("icon")) {
      iconWrapper = iconWrapper || util_default.createElement('<div class="tabbar__icon"><ons-icon></ons-icon></div>');
      const icon = iconWrapper.children[0];
      const fix = ((last) => () => icon.attributeChangedCallback("icon", last, this.getAttribute("icon")))(icon.getAttribute("icon"));
      if (this.hasAttribute("icon") && this.hasAttribute("active-icon")) {
        icon.setAttribute("icon", this.getAttribute(this.isActive() ? "active-icon" : "icon"));
      } else if (this.hasAttribute("icon")) {
        icon.setAttribute("icon", this.getAttribute("icon"));
      }
      iconWrapper.parentElement !== button && button.insertBefore(iconWrapper, button.firstChild);
      icon.attributeChangedCallback instanceof Function ? fix() : setImmediate(() => icon.attributeChangedCallback instanceof Function && fix());
    } else {
      iconWrapper && iconWrapper.remove();
    }
    ["label", "badge"].forEach((attr, index) => {
      let prop = this.querySelector(`.tabbar__${attr}`);
      if (this.hasAttribute(attr)) {
        prop = prop || util_default.create(`.tabbar__${attr}` + (attr === "badge" ? " notification" : ""));
        prop.textContent = this.getAttribute(attr);
        prop.parentElement !== button && button.appendChild(prop);
      } else {
        prop && prop.remove();
      }
    });
  }
  get _input() {
    return util_default.findChild(this, "input");
  }
  get _button() {
    return util_default.findChild(this, ".tabbar__button");
  }
  get _icon() {
    return this.querySelector(".tabbar__icon");
  }
  get _tabbar() {
    return util_default.findParent(this, "ons-tabbar");
  }
  get index() {
    return Array.prototype.indexOf.call(this.parentElement.children, this);
  }
  _onClick(event) {
    setTimeout(() => {
      if (!event.defaultPrevented) {
        this._tabbar.setActiveTab(this.index, { reject: false });
      }
    });
  }
  setActive(active = true) {
    contentReady(this, () => {
      this._input.checked = active;
      this.classList.toggle("active", active);
      util_default.toggleAttribute(this, "active", active);
      if (this.hasAttribute("icon") && this.hasAttribute("active-icon")) {
        this._icon.children[0].setAttribute("icon", this.getAttribute(active ? "active-icon" : "icon"));
      }
    });
  }
  _loadPageElement(parent, page) {
    this._hasLoaded = true;
    return new Promise((resolve) => {
      this._pageLoader.load({ parent, page }, (pageElement) => {
        parent.replaceChild(pageElement, parent.children[this.index]);
        this._loadedPage = pageElement;
        resolve(pageElement);
      });
    });
  }
  get pageElement() {
    if (this._loadedPage) {
      return this._loadedPage;
    }
    const tabbar = this._tabbar;
    if (tabbar.pages.length === tabbar.tabs.length) {
      return tabbar.pages[this.index];
    }
    return null;
  }
  /**
   * @return {Boolean}
   */
  isActive() {
    return this.classList.contains("active");
  }
  disconnectedCallback() {
    this.removeEventListener("click", this._onClick, false);
    if (this._loadedPage) {
      this._hasLoaded = false;
      this.loaded = null;
    }
    this._disconnectOnClick();
  }
  connectedCallback() {
    this.addEventListener("click", this._onClick, false);
    if (!util_default.isAttached(this) || this.loaded) {
      return;
    }
    const deferred = util_default.defer();
    this.loaded = deferred.promise;
    contentReady(this, () => {
      const index = this.index;
      const tabbar = this._tabbar;
      if (!tabbar) {
        util_default.throw("Tab elements must be children of Tabbar");
      }
      if (tabbar.hasAttribute("modifier")) {
        util_default.addModifier(this, tabbar.getAttribute("modifier"));
      }
      if (!this._hasLoaded) {
        if (this.hasAttribute("active")) {
          this.setActive(true);
          tabbar.activeIndex = index;
        }
        if (index === tabbar.tabs.length - 1) {
          tabbar._onRefresh();
          setImmediate(() => tabbar._onRefresh());
        }
        TabbarElement.rewritables.ready(tabbar, () => {
          const pageTarget = this.page || this.getAttribute("page");
          if (!this.pageElement && pageTarget) {
            const parentTarget = tabbar._targetElement;
            const dummyPage = util_default.create("div", { height: "100%", width: "100%", visibility: "hidden" });
            parentTarget.insertBefore(dummyPage, parentTarget.children[index]);
            const load = () => this._loadPageElement(parentTarget, pageTarget).then(deferred.resolve);
            return this.isActive() ? load() : tabbar._loadInactive.promise.then(load);
          }
          return deferred.resolve(this.pageElement);
        });
      }
    });
    this._connectOnClick();
  }
  static get observedAttributes() {
    return ["modifier", "ripple", "icon", "label", "page", "badge", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName14, scheme28);
        break;
      case "modifier":
        contentReady(this, () => ModifierUtil.onModifierChanged(last, current, this, scheme28));
        break;
      case "ripple":
        contentReady(this, () => this._updateRipple());
        break;
      case "icon":
      case "label":
      case "badge":
        contentReady(this, () => this._updateButtonContent());
        break;
      case "page":
        this.page = current || "";
        break;
    }
  }
};
elements_default.Tab = TabElement;
customElements.define("ons-tab", TabElement);

// node_modules/vue-onsenui/esm/components/VOnsTab.js
var script38 = {
  name: "v-ons-tab",
  inject: ["tabbar"],
  emits: ["click"],
  props: {
    page: {},
    props: {},
    active: {
      type: Boolean
    }
  },
  methods: {
    action(event) {
      let runDefault = true;
      this.$emit("click", { preventDefault: () => runDefault = false });
      if (runDefault) {
        this.tabbar.$el.setActiveTab(this.$el.index, { reject: false, ...this.tabbar.options });
      }
    }
  },
  watch: {
    active() {
      this.$el.setActive(this.active);
    }
  }
};
var _hoisted_129 = ["active"];
function render39(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-tab", {
    active: $props.active || null,
    onClick: _cache[0] || (_cache[0] = withModifiers((...args) => $options.action && $options.action(...args), ["prevent"]))
  }, null, 8, _hoisted_129);
}
script38.render = render39;

// node_modules/vue-onsenui/esm/components/VOnsTabbar.js
var name38 = "v-ons-tabbar";
var script39 = {
  name: name38,
  mixins: [deriveEvents(name38), hidable, selfProvider, modifier, unrecognizedListeners(TabbarElement)],
  emits: ["update:activeIndex"],
  props: {
    tabs: {
      type: Array,
      validator(value) {
        return value.every((tab) => ["icon", "label", "page"].some((prop) => !!Object.getOwnPropertyDescriptor(tab, prop)));
      }
    },
    tabbarStyle: {
      type: null
    }
  },
  methods: {
    _tabKey(tab) {
      return tab.key || tab.label || tab.icon;
    }
  }
};
var _hoisted_130 = ["modifier"];
var _hoisted_22 = { class: "tabbar__content" };
var _hoisted_32 = createBaseVNode("div", null, null, -1);
var _hoisted_42 = createBaseVNode("div", { class: "tabbar__border" }, null, -1);
function render40(_ctx, _cache, $props, $setup, $data, $options) {
  const _component_v_ons_tab = resolveComponent("v-ons-tab");
  return openBlock(), createElementBlock("ons-tabbar", {
    modifier: _ctx.normalizedModifier,
    onPrechange: _cache[0] || (_cache[0] = withModifiers(($event) => _ctx.$nextTick(() => !$event.detail.canceled && _ctx.$emit("update:activeIndex", $event.index)), ["self"]))
  }, [
    createBaseVNode("div", _hoisted_22, [
      createBaseVNode("div", null, [
        renderSlot(_ctx.$slots, "pages", {}, () => [
          (openBlock(true), createElementBlock(Fragment, null, renderList($props.tabs, (tab) => {
            return openBlock(), createBlock(resolveDynamicComponent(tab.page), mergeProps({ ..._ctx.unrecognizedListeners, ...tab.props }, {
              key: tab.page.key || tab.page.name || $options._tabKey(tab)
            }), null, 16);
          }), 128))
        ])
      ]),
      _hoisted_32
    ]),
    createBaseVNode("div", {
      class: "tabbar",
      style: normalizeStyle($props.tabbarStyle)
    }, [
      renderSlot(_ctx.$slots, "default", {}, () => [
        (openBlock(true), createElementBlock(Fragment, null, renderList($props.tabs, (tab) => {
          return openBlock(), createBlock(_component_v_ons_tab, mergeProps(tab, {
            key: $options._tabKey(tab)
          }), null, 16);
        }), 128))
      ]),
      _hoisted_42
    ], 4)
  ], 40, _hoisted_130);
}
script39.render = render40;

// node_modules/onsenui/esm/elements/ons-back-button.js
var defaultClassName15 = "back-button";
var scheme29 = {
  "": "back-button--*",
  ".back-button__icon": "back-button--*__icon",
  ".back-button__label": "back-button--*__label"
};
var iosBackButtonIcon = `
  <?xml version="1.0" encoding="UTF-8"?>
  <svg width="13px" height="21px" viewBox="0 0 13 21" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
      <title>ios-back-button-icon</title>
      <desc>Created with Sketch.</desc>
      <defs></defs>
      <g id="toolbar-back-button" stroke="none" stroke-width="1" fill-rule="evenodd">
          <g id="ios" transform="translate(-34.000000, -30.000000)">
              <polygon id="ios-back-button-icon" points="34 40.5 44.5 30 46.5 32 38 40.5 46.5 49 44.5 51"></polygon>
          </g>
      </g>
  </svg>
`;
var mdBackButtonIcon = `
  <?xml version="1.0" encoding="UTF-8"?>
  <svg width="16px" height="16px" viewBox="0 0 16 16" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
      <title>md-back-button-icon</title>
      <desc>Created with Sketch.</desc>
      <defs></defs>
      <g id="toolbar-back-button" stroke="none" stroke-width="1" fill-rule="evenodd">
          <g id="android" transform="translate(-32.000000, -32.000000)" fill-rule="nonzero">
              <polygon id="md-back-button-icon" points="48 39 35.83 39 41.42 33.41 40 32 32 40 40 48 41.41 46.59 35.83 41 48 41"></polygon>
          </g>
      </g>
  </svg>
`;
var BackButtonElement = class extends BaseElement {
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the back button.[/en]
   *  [ja]バックボタンの見た目を指定します。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => {
      this._compile();
    });
    this._options = {};
    this._boundOnClick = this._onClick.bind(this);
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "click");
    this._connectOnClick = onConnected;
    this._disconnectOnClick = onDisconnected;
  }
  _updateIcon(icon = util_default.findChild(this, ".back-button__icon")) {
    icon.innerHTML = autostyle_default.getPlatform(this) === "android" || util_default.hasModifier(this, "material") ? mdBackButtonIcon : iosBackButtonIcon;
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName15);
    if (!util_default.findChild(this, ".back-button__label")) {
      const label = util_default.create("span.back-button__label");
      while (this.childNodes[0]) {
        label.appendChild(this.childNodes[0]);
      }
      this.appendChild(label);
    }
    if (!util_default.findChild(this, ".back-button__icon")) {
      const icon = util_default.create("span.back-button__icon");
      this._updateIcon(icon);
      this.insertBefore(icon, this.children[0]);
    }
    util_default.updateRipple(this, void 0, { center: "", "size": "contain", "background": "transparent" });
    ModifierUtil.initModifier(this, scheme29);
  }
  /**
   * @property options
   * @type {Object}
   * @description
   *   [en]Options object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   */
  /**
   * @property options.animation
   * @type {String}
   * @description
   *   [en]Animation name. Available animations are "slide", "lift", "fade" and "none".
   *     These are platform based animations. For fixed animations, add "-ios" or "-md"
   *     suffix to the animation name. E.g. "lift-ios", "lift-md". Defaults values are "slide-ios" and "fade-md".
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @property options.animationOptions
   * @type {String}
   * @description
   *   [en]Specify the animation's duration, delay and timing. E.g.  `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}` [/ja]
   */
  /**
   * @property options.callback
   * @type {String}
   * @description
   *   [en]Function that is called when the transition has ended.[/en]
   *   [ja]このメソッドによる画面遷移が終了した際に呼び出される関数オブジェクトを指定します。[/ja]
   */
  get options() {
    return this._options;
  }
  set options(object) {
    this._options = object;
  }
  /**
   * @property onClick
   * @type {Function}
   * @description
   *   [en]Used to override the default back button behavior.[/en]
   *   [ja][/ja]
   */
  _onClick(event) {
    setTimeout(() => {
      if (!event.defaultPrevented) {
        const navigator = util_default.findParent(this, "ons-navigator");
        if (navigator) {
          navigator.popPage({ ...this.options, onsBackButton: true });
        }
      }
    });
  }
  connectedCallback() {
    this.addEventListener("click", this._boundOnClick, false);
    this._connectOnClick();
  }
  static get observedAttributes() {
    return ["modifier", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName15, scheme29);
        break;
      case "modifier": {
        ModifierUtil.onModifierChanged(last, current, this, scheme29) && this._updateIcon();
        break;
      }
    }
  }
  disconnectedCallback() {
    this.removeEventListener("click", this._boundOnClick, false);
    this._disconnectOnClick();
  }
  show() {
    this.style.display = "inline-block";
  }
  hide() {
    this.style.display = "none";
  }
};
elements_default.BackButton = BackButtonElement;
customElements.define("ons-back-button", BackButtonElement);

// node_modules/vue-onsenui/esm/components/VOnsBackButton.js
var script40 = {
  name: "v-ons-back-button",
  inject: ["navigator"],
  mixins: [modifier]
};
var _hoisted_131 = ["modifier"];
function render41(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-back-button", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_131);
}
script40.render = render41;

// node_modules/onsenui/esm/ons/internal/swipe-reveal.js
var widthToPx = (width) => {
  const [value, px] = [parseInt(width, 10), /px/.test(width)];
  return px ? value : Math.round(document.body.offsetWidth * value / 100);
};
var SwipeReveal = class {
  constructor(params) {
    "element ignoreSwipe isInitialState onDragCallback swipeMax swipeMin swipeMid".split(/\s+/).forEach((key) => this[key] = params[key]);
    this.elementHandler = params.elementHandler || params.element;
    this.getThreshold = params.getThreshold || (() => 0.5);
    this.getSide = params.getSide || (() => "left");
    this.handleGesture = this.handleGesture.bind(this);
    this._shouldFixScroll = util_default.globals.actualMobileOS === "ios";
  }
  update(swipeable = this.element.hasAttribute("swipeable")) {
    if (!this.gestureDetector) {
      this.gestureDetector = new gesture_detector_default(this.elementHandler, { dragMinDistance: 1, passive: !this._shouldFixScroll });
    }
    const action = swipeable ? "on" : "off";
    this.gestureDetector[action]("drag dragstart dragend", this.handleGesture);
  }
  handleGesture(e) {
    if (e.gesture) {
      if (e.type === "dragstart") {
        this.onDragStart(e);
      } else if (!this._ignoreDrag) {
        e.type === "dragend" ? this.onDragEnd(e) : this.onDrag(e);
      }
    }
  }
  onDragStart(event) {
    const getDistance = () => this.getSide() === "left" ? event.gesture.center.clientX : window.innerWidth - event.gesture.center.clientX;
    this._ignoreDrag = event.consumed || !util_default.isValidGesture(event) || this.ignoreSwipe(event, getDistance());
    if (!this._ignoreDrag) {
      event.consume && event.consume();
      event.consumed = true;
      this._width = widthToPx(this.element.style.width || "100%");
      this._startDistance = this._distance = !(this.isInitialState instanceof Function) || this.isInitialState() ? 0 : this._width;
      util_default.iosPreventScroll(this.gestureDetector);
    }
  }
  onDrag(event) {
    event.stopPropagation();
    const delta = this.getSide() === "left" ? event.gesture.deltaX : -event.gesture.deltaX;
    const distance = Math.max(0, Math.min(this._width, this._startDistance + delta));
    if (distance !== this._distance) {
      this._distance = distance;
      this.swipeMid(this._distance, this._width);
    }
  }
  onDragEnd(event) {
    event.stopPropagation();
    const direction = event.gesture.interimDirection;
    const isSwipeMax = this.getSide() !== direction && this._distance > this._width * this.getThreshold();
    isSwipeMax ? this.swipeMax() : this.swipeMin();
  }
  dispose() {
    this.gestureDetector && this.gestureDetector.dispose();
    this.gestureDetector = this.element = this.elementHandler = null;
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/animator.js
var NavigatorAnimator = class extends BaseAnimator {
  /**
   * @param {Object} options
   * @param {String} options.timing
   * @param {Number} options.duration
   * @param {Number} options.delay
   */
  constructor(options) {
    options = util_default.extend({
      timing: "linear",
      duration: "0.4",
      delay: "0"
    }, options || {});
    super(options);
  }
  push(enterPage, leavePage, callback) {
    callback();
  }
  pop(enterPage, leavePage, callback) {
    callback();
  }
  block(page) {
    const blocker = util_default.createElement(`
      <div style="position: absolute; background-color: transparent; width: 100%; height: 100%; z-index: 100000"></div>
    `);
    page.parentNode.appendChild(blocker);
    return () => blocker.remove();
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/ios-swipe-animator.js
var IOSSwipeNavigatorAnimator = class _IOSSwipeNavigatorAnimator extends NavigatorAnimator {
  static get swipeable() {
    return true;
  }
  constructor({ durationRestore = 0.1, durationSwipe = 0.15, timingSwipe = "linear", ...rest } = {}) {
    super({ ...rest });
    if (this.constructor === _IOSSwipeNavigatorAnimator) {
      util_default.throwAbstract();
    }
    this.durationRestore = durationRestore;
    this.durationSwipe = durationSwipe;
    this.timingSwipe = timingSwipe;
    this.optSwipe = { timing: timingSwipe, duration: durationSwipe };
    this.optRestore = { timing: timingSwipe, duration: durationRestore };
    this.swipeShadow = util_default.createElement(
      `<div style="position: absolute; height: 100%; width: 12px; right: 100%; top: 0; bottom: 0; z-index: -1;background: linear-gradient(to right, transparent 0, rgba(0,0,0,.04) 40%, rgba(0,0,0,.12) 80%, rgba(0,0,0,.16) 100%);"></div>`
    );
    this.isDragStart = true;
  }
  _decompose() {
    util_default.throwMember();
  }
  _shouldAnimateToolbar() {
    util_default.throwMember();
  }
  _calculateDelta() {
    util_default.throwMember();
  }
  _dragStartSetup(enterPage, leavePage) {
    this.isDragStart = false;
    this.unblock = super.block(leavePage);
    enterPage.parentElement.insertBefore(this.backgroundMask, enterPage);
    this.target = {
      enter: util_default.findToolbarPage(enterPage) || enterPage,
      leave: util_default.findToolbarPage(leavePage) || leavePage
    };
    this.decomp = {
      enter: this._decompose(this.target.enter),
      leave: this._decompose(this.target.leave)
    };
    this.delta = this._calculateDelta(leavePage, this.decomp.leave);
    this.shouldAnimateToolbar = this._shouldAnimateToolbar(this.target.enter, this.target.leave);
    if (this.shouldAnimateToolbar) {
      this.swipeShadow.style.top = this.decomp.leave.toolbar.offsetHeight + "px";
      this.target.leave.appendChild(this.swipeShadow);
      this._saveStyle(this.target.enter, this.target.leave);
    } else {
      leavePage.appendChild(this.swipeShadow);
      this._saveStyle(enterPage, leavePage);
    }
    leavePage.classList.add("overflow-visible");
    this.overflowElement = leavePage;
    this.decomp.leave.content.classList.add("content-swiping");
  }
  translate(distance, maxWidth, enterPage, leavePage) {
    this.isSwiping = true;
    if (enterPage.style.display === "none") {
      enterPage.style.display = "";
    }
    if (this.isDragStart) {
      this.maxWidth = maxWidth;
      this._dragStartSetup(enterPage, leavePage);
    }
    const swipeRatio = (distance - maxWidth) / maxWidth;
    if (this.shouldAnimateToolbar) {
      animit_default.runAll(
        /* Enter page */
        animit_default([this.decomp.enter.content, this.decomp.enter.bottomToolbar, this.decomp.enter.background]).queue({
          transform: `translate3d(${swipeRatio * 25}%, 0, 0)`,
          opacity: 1 + swipeRatio * 10 / 100
          // 0.9 -> 1
        }),
        animit_default(this.decomp.enter.toolbarCenter).queue({
          transform: `translate3d(${this.delta.title * swipeRatio}px, 0, 0)`,
          opacity: 1 + swipeRatio
          // 0 -> 1
        }),
        animit_default(this.decomp.enter.backButtonLabel).queue({
          opacity: 1 + swipeRatio * 10 / 100,
          // 0.9 -> 1
          transform: `translate3d(${this.delta.label * swipeRatio}px, 0, 0)`
        }),
        animit_default(this.decomp.enter.other).queue({
          opacity: 1 + swipeRatio
          // 0 -> 1
        }),
        /* Leave page */
        animit_default([this.decomp.leave.content, this.decomp.leave.bottomToolbar, this.decomp.leave.background, this.swipeShadow]).queue({
          transform: `translate3d(${distance}px, 0, 0)`
        }),
        animit_default(this.decomp.leave.toolbar).queue({
          opacity: -1 * swipeRatio
          // 1 -> 0
        }),
        animit_default(this.decomp.leave.toolbarCenter).queue({
          transform: `translate3d(${(1 + swipeRatio) * 125}%, 0, 0)`
        }),
        animit_default(this.decomp.leave.backButtonLabel).queue({
          opacity: -1 * swipeRatio,
          // 1 -> 0
          transform: `translate3d(${this.delta.title * (1 + swipeRatio)}px, 0, 0)`
        }),
        /* Other */
        animit_default(this.swipeShadow).queue({
          opacity: -1 * swipeRatio
          // 1 -> 0
        })
      );
    } else {
      animit_default.runAll(
        animit_default(leavePage).queue({
          transform: `translate3d(${distance}px, 0, 0)`
        }),
        animit_default(enterPage).queue({
          transform: `translate3d(${swipeRatio * 25}%, 0, 0)`,
          opacity: 1 + swipeRatio * 10 / 100
          // 0.9 -> 1
        }),
        animit_default(this.swipeShadow).queue({
          opacity: -1 * swipeRatio
          // 1 -> 0
        })
      );
    }
  }
  restore(enterPage, leavePage, callback) {
    if (this.isDragStart) {
      return;
    }
    if (this.shouldAnimateToolbar) {
      animit_default.runAll(
        /* Enter page */
        animit_default([this.decomp.enter.content, this.decomp.enter.bottomToolbar, this.decomp.enter.background]).queue({
          transform: "translate3d(-25%, 0, 0)",
          opacity: 0.9
        }, this.optRestore),
        animit_default(this.decomp.enter.toolbarCenter).queue({
          transform: `translate3d(-${this.delta.title}px, 0, 0)`,
          transition: `opacity ${this.durationRestore}s linear, transform ${this.durationRestore}s ${this.timingSwipe}`,
          opacity: 0
        }),
        animit_default(this.decomp.enter.backButtonLabel).queue({
          transform: `translate3d(-${this.delta.label}px, 0, 0)`
        }, this.optRestore),
        animit_default(this.decomp.enter.other).queue({
          opacity: 0
        }, this.optRestore),
        /* Leave page */
        animit_default([this.decomp.leave.content, this.decomp.leave.bottomToolbar, this.decomp.leave.background, this.swipeShadow]).queue({
          transform: `translate3d(0, 0, 0)`
        }, this.optRestore),
        animit_default(this.decomp.leave.toolbar).queue({
          opacity: 1
        }, this.optRestore),
        animit_default(this.decomp.leave.toolbarCenter).queue({
          transform: `translate3d(0, 0, 0)`
        }, this.optRestore),
        animit_default(this.decomp.leave.backButtonLabel).queue({
          opacity: 1,
          transform: `translate3d(0, 0, 0)`,
          transition: `opacity ${this.durationRestore}s linear, transform ${this.durationRestore}s ${this.timingSwipe}`
        }),
        /* Other */
        animit_default(this.swipeShadow).queue({
          opacity: 0
        }, this.optRestore).queue((done) => {
          this._reset(this.target.enter, this.target.leave);
          enterPage.style.display = "none";
          callback && callback();
          done();
        })
      );
    } else {
      animit_default.runAll(
        animit_default(enterPage).queue({
          transform: "translate3D(-25%, 0, 0)",
          opacity: 0.9
        }, this.optRestore),
        animit_default(leavePage).queue({
          transform: "translate3D(0, 0, 0)"
        }, this.optRestore).queue((done) => {
          this._reset(enterPage, leavePage);
          enterPage.style.display = "none";
          callback && callback();
          done();
        })
      );
    }
  }
  popSwipe(enterPage, leavePage, callback) {
    if (this.isDragStart) {
      return;
    }
    if (this.shouldAnimateToolbar) {
      animit_default.runAll(
        /* Enter page */
        animit_default([this.decomp.enter.content, this.decomp.enter.bottomToolbar, this.decomp.enter.background]).queue({
          transform: "translate3d(0, 0, 0)",
          opacity: 1
        }, this.optSwipe),
        animit_default(this.decomp.enter.toolbarCenter).queue({
          transform: `translate3d(0, 0, 0)`,
          transition: `opacity ${this.durationSwipe}s linear, transform ${this.durationSwipe}s ${this.timingSwipe}`,
          opacity: 1
        }),
        animit_default(this.decomp.enter.backButtonLabel).queue({
          transform: `translate3d(0, 0, 0)`
        }, this.optSwipe),
        animit_default(this.decomp.enter.other).queue({
          opacity: 1
        }, this.optSwipe),
        /* Leave page */
        animit_default([this.decomp.leave.content, this.decomp.leave.bottomToolbar, this.decomp.leave.background]).queue({
          transform: `translate3d(100%, 0, 0)`
        }, this.optSwipe),
        animit_default(this.decomp.leave.toolbar).queue({
          opacity: 0
        }, this.optSwipe),
        animit_default(this.decomp.leave.toolbarCenter).queue({
          transform: `translate3d(125%, 0, 0)`
        }, this.optSwipe),
        animit_default(this.decomp.leave.backButtonLabel).queue({
          opacity: 0,
          transform: `translate3d(${this.delta.title}px, 0, 0)`,
          transition: `opacity ${this.durationSwipe}s linear, transform ${this.durationSwipe}s ${this.timingSwipe}`
        }),
        /* Other */
        animit_default(this.swipeShadow).queue({
          opacity: 0,
          transform: `translate3d(${this.maxWidth}px, 0, 0)`
        }, this.optSwipe).queue((done) => {
          this._reset(this.target.enter, this.target.leave);
          callback && callback();
          done();
        })
      );
    } else {
      animit_default.runAll(
        animit_default(enterPage).queue({
          transform: "translate3D(0, 0, 0)",
          opacity: 1
        }, this.optSwipe),
        animit_default(leavePage).queue({
          transform: "translate3D(100%, 0, 0)"
        }, this.optSwipe).queue((done) => {
          this._reset(enterPage, leavePage);
          callback && callback();
          done();
        })
      );
    }
  }
  _saveStyle(...args) {
    this._savedStyle = /* @__PURE__ */ new WeakMap();
    const save = (el) => this._savedStyle.set(el, el.getAttribute("style"));
    args.forEach(save);
    Object.keys(this.decomp).forEach((p) => {
      Object.keys(this.decomp[p]).forEach((k) => {
        (this.decomp[p][k] instanceof Array ? this.decomp[p][k] : [this.decomp[p][k]]).forEach(save);
      });
    });
  }
  _restoreStyle(...args) {
    const restore = (el) => {
      this._savedStyle.get(el) === null ? el.removeAttribute("style") : el.setAttribute("style", this._savedStyle.get(el));
      this._savedStyle.delete(el);
    };
    args.forEach(restore);
    Object.keys(this.decomp).forEach((p) => {
      Object.keys(this.decomp[p]).forEach((k) => {
        (this.decomp[p][k] instanceof Array ? this.decomp[p][k] : [this.decomp[p][k]]).forEach(restore);
      });
    });
  }
  _reset(...args) {
    this.isSwiping = false;
    this._savedStyle && this._restoreStyle(...args);
    this.unblock && this.unblock();
    this.swipeShadow.remove();
    this.backgroundMask.remove();
    this.overflowElement.classList.remove("overflow-visible");
    this.decomp.leave.content.classList.remove("content-swiping");
    this.decomp = this.target = this.overflowElement = this._savedStyle = null;
    this.isDragStart = true;
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/ios-slide-animator.js
var translate3d = (x = 0, y = 0, z = 0) => `translate3d(${x}, ${y}, ${z})`;
var IOSSlideNavigatorAnimator = class extends IOSSwipeNavigatorAnimator {
  constructor({ timing = "cubic-bezier(0.3, .4, 0, .9)", delay = 0, duration = 0.4, ...rest } = {}) {
    super({ timing, delay, duration, ...rest });
    this.backgroundMask = util_default.createElement(
      `<div style="position: absolute; width: 100%; height: 100%;background-color: black; z-index: 2"></div>`
    );
  }
  _decompose(page) {
    const toolbar = page._getToolbarElement();
    const left = toolbar._getToolbarLeftItemsElement();
    const right = toolbar._getToolbarRightItemsElement();
    const excludeBackButton = function(elements) {
      const result = [];
      for (let i = 0; i < elements.length; i++) {
        if (elements[i].nodeName.toLowerCase() !== "ons-back-button") {
          result.push(elements[i]);
        }
      }
      return result;
    };
    const other = [].concat(left.children.length === 0 ? left : excludeBackButton(left.children)).concat(right.children.length === 0 ? right : excludeBackButton(right.children));
    return {
      toolbarCenter: toolbar._getToolbarCenterItemsElement(),
      backButtonIcon: toolbar._getToolbarBackButtonIconElement(),
      backButtonLabel: toolbar._getToolbarBackButtonLabelElement(),
      other,
      content: page._getContentElement(),
      background: page._getBackgroundElement(),
      toolbar,
      bottomToolbar: page._getBottomToolbarElement()
    };
  }
  _shouldAnimateToolbar(enterPage, leavePage) {
    const toolbars = enterPage._canAnimateToolbar() && leavePage._canAnimateToolbar();
    const enterToolbar = enterPage._getToolbarElement();
    const leaveToolbar = leavePage._getToolbarElement();
    const isStatic = enterToolbar.hasAttribute("static") || leaveToolbar.hasAttribute("static");
    const isMaterial = util_default.hasModifier(enterToolbar, "material") || util_default.hasModifier(leaveToolbar, "material");
    const isTransparent = util_default.hasModifier(enterToolbar, "transparent") || util_default.hasModifier(leaveToolbar, "transparent");
    return toolbars && !isStatic && !isMaterial && !isTransparent;
  }
  _calculateDelta(element, decomposition) {
    let title, label;
    const pageRect = element.getBoundingClientRect();
    if (decomposition.backButtonLabel.classList.contains("back-button__label")) {
      const labelRect = decomposition.backButtonLabel.getBoundingClientRect();
      title = Math.round(pageRect.width / 2 - labelRect.width / 2 - labelRect.left);
    } else {
      title = Math.round(pageRect.width / 2 * 0.6);
    }
    if (decomposition.backButtonIcon.classList.contains("back-button__icon")) {
      label = decomposition.backButtonIcon.getBoundingClientRect().right - 2;
    }
    return { title, label };
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    leavePage.parentNode.insertBefore(this.backgroundMask, leavePage);
    const unblock = super.block(enterPage);
    contentReady(enterPage, () => {
      const enterPageTarget = util_default.findToolbarPage(enterPage) || enterPage;
      const leavePageTarget = util_default.findToolbarPage(leavePage) || leavePage;
      const enterPageDecomposition = this._decompose(enterPageTarget);
      const leavePageDecomposition = this._decompose(leavePageTarget);
      const delta = this._calculateDelta(leavePage, enterPageDecomposition);
      const shouldAnimateToolbar = this._shouldAnimateToolbar(enterPageTarget, leavePageTarget);
      if (shouldAnimateToolbar) {
        animit_default.runAll(
          animit_default([enterPageDecomposition.content, enterPageDecomposition.bottomToolbar, enterPageDecomposition.background], this.def).default(
            { transform: translate3d("100%") },
            { transform: translate3d() }
          ),
          animit_default(enterPageDecomposition.toolbar, this.def).default({ opacity: 0 }, { opacity: 1 }),
          animit_default(enterPageDecomposition.toolbarCenter, this.def).default(
            { transform: translate3d("125%"), opacity: 1 },
            { transform: translate3d(), opacity: 1 }
          ),
          animit_default(enterPageDecomposition.backButtonLabel, this.def).default(
            { transform: translate3d(`${delta.title}px`), opacity: 0 },
            {
              transform: translate3d(),
              opacity: 1,
              transition: `opacity ${this.duration}s linear, transform ${this.duration}s ${this.timing}`
            }
          ),
          animit_default(enterPageDecomposition.other, this.def).default(
            { opacity: 0 },
            { css: { opacity: 1 }, timing: "linear" }
          ),
          animit_default([leavePageDecomposition.content, leavePageDecomposition.bottomToolbar, leavePageDecomposition.background], this.def).default(
            { transform: translate3d(), opacity: 1 },
            { transform: translate3d("-25%"), opacity: 0.9 }
          ).queue((done) => {
            this.backgroundMask.remove();
            unblock();
            callback();
            done();
          }),
          animit_default(leavePageDecomposition.toolbarCenter, this.def).default(
            { transform: translate3d(), opacity: 1 },
            {
              transform: translate3d(`-${delta.title}px`),
              opacity: 0,
              transition: `opacity ${this.duration}s linear, transform ${this.duration}s ${this.timing}`
            }
          ),
          animit_default(leavePageDecomposition.backButtonLabel, this.def).default(
            { transform: translate3d(), opacity: 1 },
            { transform: translate3d(`-${delta.label}px`), opacity: 0 }
          ),
          animit_default(leavePageDecomposition.other, this.def).default({ opacity: 1 }, { css: { opacity: 0 }, timing: "linear" })
        );
      } else {
        animit_default.runAll(
          animit_default(enterPage, this.def).default({ transform: translate3d("100%") }, { transform: translate3d() }),
          animit_default(leavePage, this.def).default({ transform: translate3d(), opacity: 1 }, { transform: translate3d("-25%"), opacity: 0.9 }).queue((done) => {
            this.backgroundMask.remove();
            unblock();
            callback();
            done();
          })
        );
      }
    });
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  pop(enterPage, leavePage, callback) {
    if (this.isSwiping) {
      return this.popSwipe(enterPage, leavePage, callback);
    }
    this.backgroundMask.remove();
    enterPage.parentNode.insertBefore(this.backgroundMask, enterPage);
    const unblock = super.block(enterPage);
    const enterPageTarget = util_default.findToolbarPage(enterPage) || enterPage;
    const leavePageTarget = util_default.findToolbarPage(leavePage) || leavePage;
    const enterPageDecomposition = this._decompose(enterPageTarget);
    const leavePageDecomposition = this._decompose(leavePageTarget);
    const delta = this._calculateDelta(leavePage, leavePageDecomposition);
    const shouldAnimateToolbar = this._shouldAnimateToolbar(enterPageTarget, leavePageTarget);
    if (shouldAnimateToolbar) {
      animit_default.runAll(
        animit_default([enterPageDecomposition.content, enterPageDecomposition.bottomToolbar, enterPageDecomposition.background], this.def).default(
          { transform: translate3d("-25%"), opacity: 0.9 },
          { transform: translate3d(), opacity: 1 }
        ),
        animit_default(enterPageDecomposition.toolbarCenter, this.def).default(
          { transform: translate3d(`-${delta.title}px`), opacity: 0 },
          {
            transform: translate3d(),
            opacity: 1,
            transition: `opacity ${this.duration}s linear, transform ${this.duration}s ${this.timing}`
          }
        ),
        animit_default(enterPageDecomposition.backButtonLabel, this.def).default(
          { transform: translate3d(`-${delta.label}px`) },
          { transform: translate3d() }
        ),
        animit_default(enterPageDecomposition.other, this.def).default(
          { opacity: 0 },
          { css: { opacity: 1 }, timing: "linear" }
        ),
        animit_default([leavePageDecomposition.content, leavePageDecomposition.bottomToolbar, leavePageDecomposition.background], this.def).default(
          { transform: translate3d() },
          { transform: translate3d("100%") }
        ).wait(0).queue((done) => {
          this.backgroundMask.remove();
          unblock();
          callback();
          done();
        }),
        animit_default(leavePageDecomposition.toolbar, this.def).default(
          { opacity: 1 },
          { opacity: 0 }
        ),
        animit_default(leavePageDecomposition.toolbarCenter, this.def).default(
          { transform: translate3d() },
          { transform: translate3d("125%") }
        ),
        animit_default(leavePageDecomposition.backButtonLabel, this.def).default(
          { transform: translate3d(), opacity: 1 },
          {
            transform: translate3d(`${delta.title}px`),
            opacity: 0,
            transition: `opacity ${this.duration}s linear, transform ${this.duration}s ${this.timing}`
          }
        )
      );
    } else {
      animit_default.runAll(
        animit_default(enterPage, this.def).default(
          { transform: translate3d("-25%"), opacity: 0.9 },
          { transform: translate3d(), opacity: 1 }
        ),
        animit_default(leavePage, this.def).default(
          { transform: translate3d() },
          { transform: translate3d("100%") }
        ).queue((done) => {
          this.backgroundMask.remove();
          unblock();
          callback();
          done();
        })
      );
    }
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/ios-lift-animator.js
var IOSLiftNavigatorAnimator = class extends NavigatorAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .1, 1)", delay = 0, duration = 0.4 } = {}) {
    super({ timing, delay, duration });
    this.backgroundMask = util_default.createElement(
      '<div style="position: absolute; width: 100%; height: 100%;background: linear-gradient(black, white);"></div>'
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    leavePage.parentNode.insertBefore(this.backgroundMask, leavePage);
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(enterPage, this.def).default(
        { transform: "translate3D(0, 100%, 0)" },
        { transform: "translate3D(0, 0, 0)" }
      ),
      animit_default(leavePage, this.def).default(
        { transform: "translate3D(0, 0, 0)", opacity: 1 },
        { transform: "translate3D(0, -10%, 0)", opacity: 0.9 }
      ).queue((done) => {
        this.backgroundMask.remove();
        unblock();
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  pop(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    enterPage.parentNode.insertBefore(this.backgroundMask, enterPage);
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(enterPage, this.def).default(
        { transform: "translate3D(0, -43px, 0)", opacity: 0.9 },
        { transform: "translate3D(0, 0, 0)", opacity: 1 }
      ).queue((done) => {
        this.backgroundMask.remove();
        unblock();
        callback();
        done();
      }),
      animit_default(leavePage, this.def).default(
        { transform: "translate3D(0, 0, 0)" },
        { transform: "translate3D(0, 100%, 0)" }
      )
    );
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/ios-fade-animator.js
var transform = "translate3d(0, 0, 0)";
var IOSFadeNavigatorAnimator = class extends NavigatorAnimator {
  constructor({ timing = "linear", delay = 0, duration = 0.4 } = {}) {
    super({ timing, delay, duration });
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(enterPage, this.def).default(
        { transform, opacity: 0 },
        { transform, opacity: 1 }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} done
   */
  pop(enterPage, leavePage, callback) {
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(leavePage, this.def).default(
        { transform, opacity: 1 },
        { transform, opacity: 0 }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/md-slide-animator.js
var MDSlideNavigatorAnimator = class extends NavigatorAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .4, 1)", delay = 0, duration = 0.3 } = {}) {
    super({ timing, delay, duration });
    this.blackMaskOpacity = 0.4;
    this.backgroundMask = util_default.createElement(
      '<div style="position: absolute; width: 100%; height: 100%; z-index: 2;background-color: black; opacity: 0;"></div>'
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    leavePage.parentElement.insertBefore(this.backgroundMask, leavePage.nextSibling);
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(this.backgroundMask, this.def).default(
        { transform: "translate3d(0, 0, 0)", opacity: 0 },
        { opacity: this.blackMaskOpacity }
      ).queue((done) => {
        this.backgroundMask.remove();
        done();
      }),
      animit_default(enterPage, this.def).default(
        { transform: "translate3d(100%, 0, 0)" },
        { transform: "translate3d(0, 0, 0)" }
      ),
      animit_default(leavePage, this.def).default(
        { transform: "translate3d(0, 0, 0)" },
        { transform: "translate3d(-45%, 0, 0)" }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  pop(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    enterPage.parentNode.insertBefore(this.backgroundMask, enterPage.nextSibling);
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(this.backgroundMask, this.def).default(
        { transform: "translate3d(0, 0, 0)", opacity: this.blackMaskOpacity },
        { opacity: 0 }
      ).queue((done) => {
        this.backgroundMask.remove();
        done();
      }),
      animit_default(enterPage, this.def).default(
        { transform: "translate3d(-45%, 0, 0)", opacity: 0.9 },
        { transform: "translate3d(0, 0, 0)", opacity: 1 }
      ),
      animit_default(leavePage, this.def).default(
        { transform: "translate3d(0, 0, 0)" },
        { transform: "translate3d(100%, 0, 0)" }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/md-lift-animator.js
var MDLiftNavigatorAnimator = class extends NavigatorAnimator {
  constructor({ timing = "cubic-bezier(.1, .7, .1, 1)", delay = 0.05, duration = 0.4 } = {}) {
    super({ timing, delay, duration });
    this.backgroundMask = util_default.createElement(
      '<div style="position: absolute; width: 100%; height: 100%;background-color: black;"></div>'
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    leavePage.parentNode.insertBefore(this.backgroundMask, leavePage);
    const unblock = super.block(enterPage);
    const maskClear = animit_default(this.backgroundMask).wait(this.delay + this.duration).queue((done) => {
      this.backgroundMask.remove();
      done();
    });
    animit_default.runAll(
      maskClear,
      animit_default(enterPage, this.def).default(
        { transform: "translate3d(0, 100%, 0)" },
        { transform: "translate3d(0, 0, 0)" }
      ),
      animit_default(leavePage, this.def).default({ opacity: 1 }, { opacity: 0.4 }).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  pop(enterPage, leavePage, callback) {
    this.backgroundMask.remove();
    enterPage.parentNode.insertBefore(this.backgroundMask, enterPage);
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(this.backgroundMask).wait(this.delay + this.duration).queue((done) => {
        this.backgroundMask.remove();
        done();
      }),
      animit_default(enterPage, this.def).default({ opacity: 0.4 }, { opacity: 1 }).queue((done) => {
        unblock();
        callback();
        done();
      }),
      animit_default(leavePage, this.def).default(
        { transform: "translate3d(0, 0, 0)" },
        { transform: "translate3d(0, 100%, 0)" }
      )
    );
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/md-fade-animator.js
var MDFadeNavigatorAnimator = class extends NavigatorAnimator {
  constructor({ timing = "cubic-bezier(0.4, 0, 0.2, 1)", timingPop = "cubic-bezier(0.4, 0, 1, 1)", delay = 0, duration = 0.2 } = {}) {
    super({ timing, delay, duration });
    this.timingPop = timingPop;
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} callback
   */
  push(enterPage, leavePage, callback) {
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(enterPage, this.def).default(
        { transform: "translate3D(0, 42px, 0)", opacity: 0 },
        { transform: "translate3D(0, 0, 0)", opacity: 1 }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
  /**
   * @param {Object} enterPage
   * @param {Object} leavePage
   * @param {Function} done
   */
  pop(enterPage, leavePage, callback) {
    const unblock = super.block(enterPage);
    animit_default.runAll(
      animit_default(leavePage, this.def).default(
        { transform: "translate3D(0, 0, 0)", opacity: 1 },
        { css: { transform: "translate3D(0, 38px, 0)", opacity: 0 }, timing: this.timingPop }
      ).queue((done) => {
        unblock();
        callback();
        done();
      })
    );
  }
};

// node_modules/onsenui/esm/elements/ons-navigator/none-animator.js
var NoneNavigatorAnimator = class extends NavigatorAnimator {
  constructor(options) {
    super(options);
  }
  push(enterPage, leavePage, callback) {
    callback();
  }
  pop(enterPage, leavePage, callback) {
    callback();
  }
};

// node_modules/onsenui/esm/elements/ons-navigator.js
var _animatorDict9 = {
  "default": function() {
    return platform_default.isAndroid() ? MDFadeNavigatorAnimator : IOSSlideNavigatorAnimator;
  },
  "slide": function() {
    return platform_default.isAndroid() ? MDSlideNavigatorAnimator : IOSSlideNavigatorAnimator;
  },
  "lift": function() {
    return platform_default.isAndroid() ? MDLiftNavigatorAnimator : IOSLiftNavigatorAnimator;
  },
  "fade": function() {
    return platform_default.isAndroid() ? MDFadeNavigatorAnimator : IOSFadeNavigatorAnimator;
  },
  "slide-ios": IOSSlideNavigatorAnimator,
  "slide-md": MDSlideNavigatorAnimator,
  "lift-ios": IOSLiftNavigatorAnimator,
  "lift-md": MDLiftNavigatorAnimator,
  "fade-ios": IOSFadeNavigatorAnimator,
  "fade-md": MDFadeNavigatorAnimator,
  "none": NoneNavigatorAnimator
};
var rewritables3 = {
  /**
   * @param {Element} navigatorSideElement
   * @param {Function} callback
   */
  ready(navigatorElement, callback) {
    callback();
  }
};
var verifyPageElement = (el) => el.nodeName !== "ONS-PAGE" && util_default.throw("Only page elements can be children of navigator");
var NavigatorElement = class extends BaseElement {
  /**
   * @attribute page
   * @initonly
   * @type {String}
   * @description
   *   [en]First page to show when navigator is initialized.[/en]
   *   [ja]ナビゲーターが初期化された時に表示するページを指定します。[/ja]
   */
  /**
   * @attribute swipeable
   * @type {Boolean}
   * @description
   *   [en]Enable iOS "swipe to pop" feature.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute swipe-target-width
   * @type {String}
   * @default 20px
   * @description
   *   [en]The width of swipeable area calculated from the edge (in pixels). Use this to enable swipe only when the finger touch on the screen edge.[/en]
   *   [ja]スワイプの判定領域をピクセル単位で指定します。画面の端から指定した距離に達するとページが表示されます。[/ja]
   */
  /**
   * @attribute swipe-threshold
   * @type {Number}
   * @default 0.2
   * @description
   *  [en]Specify how much the page needs to be swiped before popping. A value between `0` and `1`.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default default
   * @description
   *   [en]
   *     Animation name. Available animations are `"slide"`, `"lift"`, `"fade"` and `"none"`.
   *
   *     These are platform based animations. For fixed animations, add `"-ios"` or `"-md"` suffix to the animation name. E.g. `"lift-ios"`, `"lift-md"`. Defaults values are `"slide-ios"` and `"fade-md"` depending on the platform.
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`[/ja]
   */
  /**
   * @event prepush
   * @description
   *   [en]Fired just before a page is pushed.[/en]
   *   [ja]pageがpushされる直前に発火されます。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.navigator
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Object} event.currentPage
   *   [en]Current page object.[/en]
   *   [ja]現在のpageオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call this function to cancel the push.[/en]
   *   [ja]この関数を呼び出すと、push処理がキャンセルされます。[/ja]
   */
  /**
   * @event prepop
   * @description
   *   [en]Fired just before a page is popped.[/en]
   *   [ja]pageがpopされる直前に発火されます。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.navigator
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Object} event.currentPage
   *   [en]Current page object.[/en]
   *   [ja]現在のpageオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call this function to cancel the pop.[/en]
   *   [ja]この関数を呼び出すと、pageのpopがキャンセルされます。[/ja]
   */
  /**
   * @event postpush
   * @description
   *   [en]Fired just after a page is pushed.[/en]
   *   [ja]pageがpushされてアニメーションが終了してから発火されます。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.navigator
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Object} event.enterPage
   *   [en]Object of the next page.[/en]
   *   [ja]pushされたpageオブジェクト。[/ja]
   * @param {Object} event.leavePage
   *   [en]Object of the previous page.[/en]
   *   [ja]以前のpageオブジェクト。[/ja]
   */
  /**
   * @event postpop
   * @description
   *   [en]Fired just after a page is popped.[/en]
   *   [ja]pageがpopされてアニメーションが終わった後に発火されます。[/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.navigator
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Object} event.enterPage
   *   [en]Object of the next page.[/en]
   *   [ja]popされて表示されるページのオブジェクト。[/ja]
   * @param {Object} event.leavePage
   *   [en]Object of the previous page.[/en]
   *   [ja]popされて消えるページのオブジェクト。[/ja]
   * @param {Object} event.swipeToPop
   *   [en]True if the pop was triggered by the user swiping to pop.[/en]
   *   [ja][/ja]
   * @param {Object} event.onsBackButton
   *   [en]True if the pop was caused by pressing an ons-back-button.[/en]
   *   [ja][/ja]
   */
  /**
   * @event swipe
   * @description
   *   [en]Fired whenever the user slides the navigator (swipe-to-pop).[/en]
   *   [ja][/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.ratio
   *   [en]Decimal ratio (0-1).[/en]
   *   [ja][/ja]
   * @param {Object} event.animationOptions
   *   [en][/en]
   *   [ja][/ja]
   */
  get animatorFactory() {
    return this._animatorFactory;
  }
  constructor() {
    super();
    this._isRunning = false;
    this._initialized = false;
    this._pageLoader = defaultPageLoader;
    this._pageMap = /* @__PURE__ */ new WeakMap();
    this._updateAnimatorFactory();
  }
  /**
   * @property pageLoader
   * @type {PageLoader}
   * @description
   *   [en]PageLoader instance. It can be overriden to change the way pages are loaded by this element. Useful for lib developers.[/en]
   *   [ja]PageLoaderインスタンスを格納しています。[/ja]
   */
  get pageLoader() {
    return this._pageLoader;
  }
  set pageLoader(pageLoader) {
    if (!(pageLoader instanceof PageLoader)) {
      util_default.throwPageLoader();
    }
    this._pageLoader = pageLoader;
  }
  _getPageTarget() {
    return this._page || this.getAttribute("page");
  }
  /**
   * @property page
   * @type {*}
   * @description
   *   [en]Specify the page to be loaded during initialization. This value takes precedence over the `page` attribute. Useful for lib developers.[/en]
   *   [ja]初期化時に読み込むページを指定します。`page`属性で指定した値よりも`page`プロパティに指定した値を優先します。[/ja]
   */
  get page() {
    return this._page;
  }
  set page(page) {
    this._page = page;
  }
  connectedCallback() {
    this.onDeviceBackButton = this._onDeviceBackButton.bind(this);
    if (!platform_default.isAndroid() || this.getAttribute("swipeable") === "force") {
      let swipeAnimator;
      this._swipe = new SwipeReveal({
        element: this,
        getThreshold: () => Math.max(0.2, parseFloat(this.getAttribute("swipe-threshold")) || 0),
        swipeMax: () => {
          const ratio = 1;
          const animationOptions = { duration: swipeAnimator.durationSwipe, timing: swipeAnimator.timingSwipe };
          this._onSwipe && this._onSwipe(ratio, animationOptions);
          util_default.triggerElementEvent(this, "swipe", { ratio, animationOptions });
          this[this.swipeMax ? "swipeMax" : "popPage"]({ animator: swipeAnimator, swipeToPop: true });
          swipeAnimator = null;
        },
        swipeMid: (distance, width) => {
          const ratio = distance / width;
          this._onSwipe && this._onSwipe(ratio);
          util_default.triggerElementEvent(this, "swipe", { ratio });
          swipeAnimator.translate(distance, width, this.topPage.previousElementSibling, this.topPage);
        },
        swipeMin: () => {
          const ratio = 0;
          const animationOptions = { duration: swipeAnimator.durationRestore, timing: swipeAnimator.timingSwipe };
          this._onSwipe && this._onSwipe(ratio, animationOptions);
          util_default.triggerElementEvent(this, "swipe", { ratio, animationOptions });
          swipeAnimator.restore(this.topPage.previousElementSibling, this.topPage);
          swipeAnimator = null;
        },
        ignoreSwipe: (event, distance) => {
          if (!this._isRunning && this.children.length > 1) {
            const area = parseInt(this.getAttribute("swipe-target-width") || 25, 10);
            if (event.gesture.direction === "right" && area > distance) {
              const isBB = (el) => /ons-back-button/i.test(el.tagName);
              if (!isBB(event.target) && !util_default.findParent(event.target, isBB, (p) => /ons-page/i.test(p.tagName))) {
                const animation = (this.topPage.pushedOptions || {}).animation || this.animatorFactory._animation;
                const Animator = _animatorDict9[animation] instanceof Function ? _animatorDict9[animation].call() : _animatorDict9[animation];
                if (typeof Animator !== "undefined" && Animator.swipeable) {
                  swipeAnimator = new Animator();
                  return false;
                }
              }
            }
          }
          return true;
        }
      });
      this.attributeChangedCallback("swipeable");
    }
    if (this._initialized) {
      return;
    }
    this._initialized = true;
    const deferred = util_default.defer();
    this.loaded = deferred.promise;
    rewritables3.ready(this, () => {
      const show = !util_default.hasAnyComponentAsParent(this);
      const options = { animation: "none", show };
      if (this.pages.length === 0 && this._getPageTarget()) {
        this.pushPage(this._getPageTarget(), options).then(() => deferred.resolve());
      } else if (this.pages.length > 0) {
        for (var i = 0; i < this.pages.length; i++) {
          verifyPageElement(this.pages[i]);
        }
        if (this.topPage) {
          contentReady(
            this.topPage,
            () => setTimeout(() => {
              deferred.resolve();
              show && this.topPage._show();
              this._updateLastPageBackButton();
            }, 0)
          );
        }
      } else {
        contentReady(this, () => {
          if (this.pages.length === 0 && this._getPageTarget()) {
            this.pushPage(this._getPageTarget(), options).then(() => deferred.resolve());
          } else {
            deferred.resolve();
          }
        });
      }
    });
  }
  _updateAnimatorFactory() {
    this._animatorFactory = new AnimatorFactory({
      animators: _animatorDict9,
      baseClass: NavigatorAnimator,
      baseClassName: "NavigatorAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
  }
  disconnectedCallback() {
    this._backButtonHandler.destroy();
    this._backButtonHandler = null;
    this._swipe && this._swipe.dispose();
    this._swipe = null;
  }
  static get observedAttributes() {
    return ["animation", "swipeable"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "animation":
        this._updateAnimatorFactory();
        break;
      case "swipeable":
        this._swipe && this._swipe.update();
        break;
    }
  }
  /**
   * @method popPage
   * @signature popPage([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.animation]
   *   [en]
   *     Animation name. Available animations are `"slide"`, `"lift"`, `"fade"` and `"none"`.
   *
   *     These are platform based animations. For fixed animations, add `"-ios"` or `"-md"` suffix to the animation name. E.g. `"lift-ios"`, `"lift-md"`. Defaults values are `"slide-ios"` and `"fade-md"`.
   *   [/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. {duration: 0.2, delay: 0.4, timing: 'ease-in'}[/ja]
   * @param {Function} [options.callback]
   *   [en]Function that is called when the transition has ended.[/en]
   *   [ja]このメソッドによる画面遷移が終了した際に呼び出される関数オブジェクトを指定します。[/ja]
   * @param {Object} [options.data]
   *   [en]Custom data that will be stored in the new page element.[/en]
   *   [ja][/ja]
   * @param {Number} [options.times]
   *   [en]Number of pages to be popped. Only one animation will be shown.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Promise which resolves to the revealed page.[/en]
   *   [ja]明らかにしたページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Pops the current page from the page stack. The previous page will be displayed.[/en]
   *   [ja]現在表示中のページをページスタックから取り除きます。一つ前のページに戻ります。[/ja]
   */
  popPage(options = {}) {
    ({ options } = this._preparePageAndOptions(null, options));
    if (util_default.isInteger(options.times) && options.times > 1) {
      this._removePages(options.times);
    }
    const popUpdate = () => new Promise((resolve) => {
      this._pageLoader.unload(this.pages[this.pages.length - 1]);
      resolve();
    });
    return this._popPage(options, popUpdate);
  }
  _popPage(options, update = () => Promise.resolve()) {
    if (this._isRunning) {
      return Promise.reject("popPage is already running.");
    }
    if (this.pages.length <= 1) {
      return Promise.reject("ons-navigator's page stack is empty.");
    }
    if (this._emitPrePopEvent()) {
      return Promise.reject("Canceled in prepop event.");
    }
    const length = this.pages.length;
    this._isRunning = true;
    this.pages[length - 2].updateBackButton(length - 2 > 0);
    return new Promise((resolve) => {
      const leavePage = this.pages[length - 1];
      const enterPage = this.pages[length - 2];
      options = util_default.extend({}, this.options || {}, options);
      if (options.data) {
        enterPage.data = util_default.extend({}, enterPage.data || {}, options.data || {});
      }
      const done = () => {
        update().then(() => {
          this._isRunning = false;
          enterPage._show();
          util_default.triggerElementEvent(this, "postpop", {
            leavePage,
            enterPage,
            navigator: this,
            swipeToPop: !!options.swipeToPop,
            // whether the pop was triggered by the user swiping
            onsBackButton: !!options.onsBackButton
            // whether the pop was triggered by clicking ons-back-button
          });
          options.callback && options.callback(enterPage);
          resolve(enterPage);
        });
      };
      leavePage._hide();
      enterPage.style.display = "";
      const animator = options.animator || this._animatorFactory.newAnimator(options);
      animator.pop(this.pages[length - 2], this.pages[length - 1], done);
    }).catch(() => this._isRunning = false);
  }
  /**
   * @method pushPage
   * @signature pushPage(page, [options])
   * @param {String} page
   *   [en]Page URL. Can be either a HTML document or a template defined with the `<template>` tag.[/en]
   *   [ja]pageのURLか、もしくは`<template>`で宣言したテンプレートのid属性の値を指定できます。[/ja]
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {String} [options.page]
   *   [en]Page URL. Only necessary if `page` parameter is null or undefined.[/en]
   *   [ja][/ja]
   * @param {String} [options.pageHTML]
   *   [en]HTML code that will be computed as a new page. Overwrites `page` parameter.[/en]
   *   [ja][/ja]
   * @param {String} [options.animation]
   *   [en]
   *     Animation name. Available animations are `"slide"`, `"lift"`, `"fade"` and `"none"`.
   *
   *     These are platform based animations. For fixed animations, add `"-ios"` or `"-md"` suffix to the animation name. E.g. `"lift-ios"`, `"lift-md"`. Defaults values are `"slide-ios"` and `"fade-md"`.
   *   [/en]
   *   [ja][/ja]
   * @param {String} [options.animationOptions]
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}` [/ja]
   * @param {Function} [options.callback]
   *   [en]Function that is called when the transition has ended.[/en]
   *   [ja]pushPage()による画面遷移が終了した時に呼び出される関数オブジェクトを指定します。[/ja]
   * @param {Object} [options.data]
   *   [en]Custom data that will be stored in the new page element.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Promise which resolves to the pushed page.[/en]
   *   [ja]追加したページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Pushes the specified page into the stack.[/en]
   *   [ja]指定したpageを新しいページスタックに追加します。新しいページが表示されます。[/ja]
   */
  pushPage(page, options = {}) {
    ({ page, options } = this._preparePageAndOptions(page, options));
    const prepare = (pageElement) => {
      verifyPageElement(pageElement);
      this._pageMap.set(pageElement, page);
      pageElement = util_default.extend(pageElement, {
        data: options.data
      });
      pageElement.style.visibility = "hidden";
    };
    if (options.pageHTML) {
      return this._pushPage(options, () => new Promise((resolve) => {
        instantPageLoader.load({ page: options.pageHTML, parent: this, params: options.data }, (pageElement) => {
          prepare(pageElement);
          resolve();
        });
      }));
    }
    return this._pushPage(options, () => new Promise((resolve, reject) => {
      this._pageLoader.load({ page, parent: this, params: options.data }, (pageElement) => {
        prepare(pageElement);
        resolve();
      }, (error) => {
        this._isRunning = false;
        reject(error);
      });
    }));
  }
  _pushPage(options = {}, update = () => Promise.resolve()) {
    if (this._isRunning) {
      return Promise.reject("pushPage is already running.");
    }
    if (this._emitPrePushEvent()) {
      return Promise.reject("Canceled in prepush event.");
    }
    this._isRunning = true;
    const animationOptions = this.animationOptions;
    options = util_default.extend({}, this.options || {}, { animationOptions }, options);
    const animator = this._animatorFactory.newAnimator(options);
    return update().then(() => {
      const pageLength = this.pages.length;
      const enterPage = this.pages[pageLength - 1];
      const leavePage = options.leavePage || this.pages[pageLength - 2];
      verifyPageElement(enterPage);
      enterPage.updateBackButton(pageLength > (options._replacePage ? 2 : 1));
      enterPage.pushedOptions = util_default.extend({}, enterPage.pushedOptions || {}, options || {});
      enterPage.data = util_default.extend({}, enterPage.data || {}, options.data || {});
      enterPage.unload = enterPage.unload || options.unload;
      return new Promise((resolve) => {
        const done = () => {
          this._isRunning = false;
          options.show !== false && setImmediate(() => enterPage._show());
          util_default.triggerElementEvent(this, "postpush", { leavePage, enterPage, navigator: this });
          if (leavePage) {
            leavePage.style.display = "none";
          }
          options.callback && options.callback(enterPage);
          resolve(enterPage);
        };
        enterPage.style.visibility = "";
        if (leavePage) {
          leavePage._hide();
          animator.push(enterPage, leavePage, done);
        } else {
          done();
        }
      });
    }).catch((error) => {
      this._isRunning = false;
      throw error;
    });
  }
  /**
   * @method replacePage
   * @signature replacePage(page, [options])
   * @return {Promise}
   *   [en]Promise which resolves to the new page.[/en]
   *   [ja]新しいページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Replaces the current top page with the specified one. Extends `pushPage()` parameters.[/en]
   *   [ja]現在表示中のページをを指定したページに置き換えます。[/ja]
   */
  replacePage(page, options = {}) {
    return this.pushPage(page, options).then((resolvedValue) => {
      if (this.pages.length > 1) {
        this._pageLoader.unload(this.pages[this.pages.length - 2]);
      }
      this._updateLastPageBackButton();
      return Promise.resolve(resolvedValue);
    });
  }
  /**
   * @method insertPage
   * @signature insertPage(index, page, [options])
   * @param {Number} index
   *   [en]The index where it should be inserted.[/en]
   *   [ja]スタックに挿入する位置のインデックスを指定します。[/ja]
   * @return {Promise}
   *   [en]Promise which resolves to the inserted page.[/en]
   *   [ja]指定したページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Insert the specified page into the stack with at a position defined by the `index` argument. Extends `pushPage()` parameters.[/en]
   *   [ja]指定したpageをページスタックのindexで指定した位置に追加します。[/ja]
   */
  insertPage(index, page, options = {}) {
    ({ page, options } = this._preparePageAndOptions(page, options));
    index = this._normalizeIndex(index);
    if (index >= this.pages.length) {
      return this.pushPage(page, options);
    }
    page = typeof options.pageHTML === "string" ? options.pageHTML : page;
    const loader = typeof options.pageHTML === "string" ? instantPageLoader : this._pageLoader;
    return new Promise((resolve) => {
      loader.load({ page, parent: this }, (pageElement) => {
        verifyPageElement(pageElement);
        this._pageMap.set(pageElement, page);
        pageElement = util_default.extend(pageElement, {
          data: options.data,
          pushedOptions: options
        });
        options.animationOptions = util_default.extend(
          {},
          this.animationOptions,
          options.animationOptions || {}
        );
        pageElement.style.display = "none";
        this.insertBefore(pageElement, this.pages[index]);
        this.topPage.updateBackButton(true);
        setTimeout(() => {
          pageElement = null;
          resolve(this.pages[index]);
        }, 1e3 / 60);
      });
    });
  }
  /**
   * @method removePage
   * @signature removePage(index, [options])
   * @param {Number} index
   *   [en]The index where it should be removed.[/en]
   *   [ja]スタックから削除するページのインデックスを指定します。[/ja]
   * @return {Promise}
   *   [en]Promise which resolves to the revealed page.[/en]
   *   [ja]削除によって表示されたページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Remove the specified page at a position in the stack defined by the `index` argument. Extends `popPage()` parameters.[/en]
   *   [ja]指定したインデックスにあるページを削除します。[/ja]
   */
  removePage(index, options = {}) {
    index = this._normalizeIndex(index);
    if (index < this.pages.length - 1) {
      return new Promise((resolve) => {
        const leavePage = this.pages[index];
        const enterPage = this.topPage;
        this._pageMap.delete(leavePage);
        this._pageLoader.unload(leavePage);
        if (this.pages.length === 1) {
          this.topPage.updateBackButton(false);
        }
        resolve(enterPage);
      });
    } else {
      return this.popPage(options);
    }
  }
  /**
   * @method resetToPage
   * @signature resetToPage(page, [options])
   * @return {Promise}
   *   [en]Promise which resolves to the new top page.[/en]
   *   [ja]新しいトップページを解決するPromiseを返します。[/ja]
   * @param {Boolean} [options.pop]
   *   [en]Performs 'pop' effect if `true` instead of 'push' or none. This also sets `options.animation` value to `default` instead of `none`.[/en]
   *   [ja][/ja]
   * @description
   *   [en]Clears page stack and adds the specified page to the stack. Extends `pushPage()` parameters.[/en]
   *   [ja]ページスタックをリセットし、指定したページを表示します。[/ja]
   */
  resetToPage(page, options = {}) {
    ({ page, options } = this._preparePageAndOptions(page, options));
    if (!options.animator && !options.animation && !options.pop) {
      options.animation = "none";
    }
    if (!options.page && !options.pageHTML && this._getPageTarget()) {
      page = options.page = this._getPageTarget();
    }
    if (options.pop) {
      this._removePages();
      return this.insertPage(0, page, { data: options.data }).then(() => this.popPage(options));
    }
    const callback = options.callback;
    options.callback = (newPage) => {
      this._removePages();
      newPage.updateBackButton(false);
      callback && callback(newPage);
    };
    return this.pushPage(page, options);
  }
  /**
   * @method bringPageTop
   * @signature bringPageTop(item, [options])
   * @param {String|Number} item
   *   [en]Page URL or index of an existing page in navigator's stack.[/en]
   *   [ja]ページのURLかもしくはons-navigatorのページスタックのインデックス値を指定します。[/ja]
   * @return {Promise}
   *   [en]Promise which resolves to the new top page.[/en]
   *   [ja]新しいトップページを解決するPromiseを返します。[/ja]
   * @description
   *   [en]Brings the given page to the top of the page stack if it already exists or pushes it into the stack if doesn't. Extends `pushPage()` parameters.[/en]
   *   [ja]指定したページをページスタックの一番上に移動します。もし指定したページが無かった場合新しくpushされます。[/ja]
   */
  bringPageTop(item, options = {}) {
    if (["number", "string"].indexOf(typeof item) === -1) {
      util_default.throw("First argument must be a page name or the index of an existing page. You supplied " + item);
    }
    const index = typeof item === "number" ? this._normalizeIndex(item) : this._lastIndexOfPage(item);
    const page = this.pages[index];
    if (index < 0) {
      return this.pushPage(item, options);
    }
    ({ options } = this._preparePageAndOptions(page, options));
    if (index === this.pages.length - 1) {
      return Promise.resolve(page);
    }
    if (!page) {
      util_default.throw("Failed to find item " + item);
    }
    if (this._isRunning) {
      return Promise.reject("pushPage is already running.");
    }
    if (this._emitPrePushEvent()) {
      return Promise.reject("Canceled in prepush event.");
    }
    page.style.display = "";
    page.style.visibility = "hidden";
    page.parentNode.appendChild(page);
    return this._pushPage(options);
  }
  _preparePageAndOptions(page, options = {}) {
    if (typeof options != "object") {
      util_default.throw("options must be an object. You supplied " + options);
    }
    if ((page === null || page === void 0) && options.page) {
      page = options.page;
    }
    options = util_default.extend({}, this.options || {}, options, { page });
    return { page, options };
  }
  _removePages(times) {
    const pages = this.pages;
    let until = times === void 0 ? 0 : pages.length - times;
    until = until < 0 ? 1 : until;
    for (let i = pages.length - 2; i >= until; i--) {
      this._pageMap.delete(pages[i]);
      this._pageLoader.unload(pages[i]);
    }
  }
  _updateLastPageBackButton() {
    const index = this.pages.length - 1;
    if (index >= 0) {
      this.pages[index].updateBackButton(index > 0);
    }
  }
  _normalizeIndex(index) {
    return index >= 0 ? index : Math.abs(this.pages.length + index) % this.pages.length;
  }
  _onDeviceBackButton(event) {
    if (this.pages.length > 1) {
      this.popPage();
    } else {
      event.callParentHandler();
    }
  }
  _lastIndexOfPage(pageName) {
    let index;
    for (index = this.pages.length - 1; index >= 0; index--) {
      if (pageName === this._pageMap.get(this.pages[index])) {
        break;
      }
    }
    return index;
  }
  _emitPreEvent(name44, data = {}) {
    let isCanceled = false;
    util_default.triggerElementEvent(this, "pre" + name44, util_default.extend({
      navigator: this,
      currentPage: this.pages[this.pages.length - 1],
      cancel: () => isCanceled = true
    }, data));
    return isCanceled;
  }
  _emitPrePushEvent() {
    return this._emitPreEvent("push");
  }
  _emitPrePopEvent() {
    const l = this.pages.length;
    return this._emitPreEvent("pop", {
      leavePage: this.pages[l - 1],
      enterPage: this.pages[l - 2]
    });
  }
  // TODO: 書き直す
  _createPageElement(templateHTML) {
    const pageElement = util_default.createElement(internal_default.normalizePageHTML(templateHTML));
    verifyPageElement(pageElement);
    return pageElement;
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  get onDeviceBackButton() {
    return this._backButtonHandler;
  }
  set onDeviceBackButton(callback) {
    if (this._backButtonHandler) {
      this._backButtonHandler.destroy();
    }
    this._backButtonHandler = device_back_button_dispatcher_default.createHandler(this, callback);
  }
  /**
   * @property topPage
   * @readonly
   * @type {HTMLElement}
   * @description
   *   [en]Current top page element. Use this method to access options passed by `pushPage()`-like methods.[/en]
   *   [ja]現在のページを取得します。pushPage()やresetToPage()メソッドの引数を取得できます。[/ja]
   */
  get topPage() {
    let last = this.lastElementChild;
    while (last && last.tagName !== "ONS-PAGE") {
      last = last.previousElementSibling;
    }
    return last;
  }
  /**
   * @property pages
   * @readonly
   * @type {Array}
   * @description
   *   [en]Copy of the navigator's page stack.[/en]
   *   [ja][/ja]
   */
  get pages() {
    return util_default.arrayFrom(this.children).filter((element) => element.tagName === "ONS-PAGE");
  }
  /**
   * @property onSwipe
   * @type {Function}
   * @description
   *   [en]Hook called whenever the user slides the navigator (swipe-to-pop). It gets a decimal ratio (0-1) and an animationOptions object as arguments.[/en]
   *   [ja][/ja]
   */
  get onSwipe() {
    return this._onSwipe;
  }
  set onSwipe(value) {
    if (value && !(value instanceof Function)) {
      util_default.throw('"onSwipe" must be a function');
    }
    this._onSwipe = value;
  }
  /**
   * @property options
   * @type {Object}
   * @description
   *   [en]Default options object. Attributes have priority over this property.[/en]
   *   [ja][/ja]
   */
  /**
   * @property options.animation
   * @type {String}
   * @description
   *   [en]
   *     Animation name. Available animations are `"slide"`, `"lift"`, `"fade"` and `"none"`.
   *     These are platform based animations. For fixed animations, add `"-ios"` or `"-md"` suffix to the animation name. E.g. `"lift-ios"`, `"lift-md"`. Defaults values are `"slide-ios"` and `"fade-md"`.
   *   [/en]
   *   [ja][/ja]
   */
  /**
   * @property options.animationOptions
   * @type {String}
   * @description
   *   [en]Specify the animation's duration, delay and timing. E.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}`.[/en]
   *   [ja]アニメーション時のduration, delay, timingを指定します。e.g. `{duration: 0.2, delay: 0.4, timing: 'ease-in'}` [/ja]
   */
  /**
   * @property options.callback
   * @type {String}
   * @description
   *   [en]Function that is called when the transition has ended.[/en]
   *   [ja]このメソッドによる画面遷移が終了した際に呼び出される関数オブジェクトを指定します。[/ja]
   */
  get options() {
    return this._options;
  }
  set options(object) {
    this._options = object;
  }
  get animationOptions() {
    return this.hasAttribute("animation-options") ? AnimatorFactory.parseAnimationOptionsString(this.getAttribute("animation-options")) : {};
  }
  set animationOptions(value) {
    if (value === void 0 || value === null) {
      this.removeAttribute("animation-options");
    } else {
      this.setAttribute("animation-options", JSON.stringify(value));
    }
  }
  set _isRunning(value) {
    this.setAttribute("_is-running", value ? "true" : "false");
  }
  get _isRunning() {
    return JSON.parse(this.getAttribute("_is-running"));
  }
  _show() {
    this.loaded.then(() => this.topPage && this.topPage._show());
  }
  _hide() {
    this.topPage && this.topPage._hide();
  }
  _destroy() {
    for (let i = this.pages.length - 1; i >= 0; i--) {
      this._pageLoader.unload(this.pages[i]);
    }
    this.remove();
  }
  /**
   * @param {String} name
   * @param {Function} Animator
   */
  static registerAnimator(name44, Animator) {
    if (!(Animator.prototype instanceof NavigatorAnimator)) {
      util_default.throwAnimator("Navigator");
    }
    _animatorDict9[name44] = Animator;
  }
  static get animators() {
    return _animatorDict9;
  }
  static get NavigatorAnimator() {
    return NavigatorAnimator;
  }
  static get events() {
    return ["prepush", "postpush", "prepop", "postpop", "swipe"];
  }
  static get rewritables() {
    return rewritables3;
  }
};
elements_default.Navigator = NavigatorElement;
customElements.define("ons-navigator", NavigatorElement);

// node_modules/vue-onsenui/esm/components/VOnsNavigator.js
var name39 = "v-ons-navigator";
var script41 = {
  name: name39,
  mixins: [hasOptions, selfProvider, deriveEvents(name39), deriveDBB, unrecognizedListeners(NavigatorElement)],
  emits: ["update:pageStack"],
  props: {
    pageStack: {
      type: Array,
      required: true
    }
  },
  data() {
    return {
      internalPageStack: this.pageStack
    };
  },
  computed: {
    extendsFix() {
      return this.internalPageStack.map(
        (page) => page.extends ? { ...page, template: page.extends.template } : page
      );
    }
  },
  methods: {
    isReady() {
      if (this.hasOwnProperty("_ready") && this._ready instanceof Promise) {
        return this._ready;
      }
      return Promise.resolve();
    },
    onDeviceBackButton(event) {
      if (this.internalPageStack.length > 1) {
        const lastTopPage = this.$el.children[this.internalPageStack.length - 1];
        const scrollElement = this._findScrollPage(lastTopPage);
        const scrollValue = scrollElement.scrollTop || 0;
        this._pageStackUpdate = {
          lastTopPage,
          lastLength: this.internalPageStack.length,
          currentLength: this.internalPageStack.length - 1,
          restoreScroll: () => scrollElement.scrollTop = scrollValue
        };
        this._popPage();
      } else {
        event.callParentHandler();
      }
    },
    _findScrollPage(page) {
      const nextPage = page._contentElement.children.length === 1 && this.$ons._ons._util.getTopPage(page._contentElement.children[0]);
      return nextPage ? this._findScrollPage(nextPage) : page;
    },
    _eachPage(start, end, cb) {
      for (let i = start; i < end; i++) {
        cb(this.$el.children[i]);
      }
    },
    _reattachPage(pageElement, position = null, restoreScroll) {
      this.$el.insertBefore(pageElement, position);
      restoreScroll instanceof Function && restoreScroll();
      pageElement._isShown = true;
    },
    _redetachPage(pageElement) {
      pageElement._destroy();
      return Promise.resolve();
    },
    _animate({ lastLength, currentLength, lastTopPage, currentTopPage, currentTopPageOptions, restoreScroll }) {
      const pushedOptions = this.internalPageStack[this.internalPageStack.length - 1].onsNavigatorOptions || currentTopPageOptions || {};
      if (currentLength > lastLength) {
        let isReattached = false;
        if (lastTopPage.parentElement !== this.$el) {
          this._reattachPage(lastTopPage, this.$el.children[lastLength - 1], restoreScroll);
          isReattached = true;
          lastLength--;
        }
        this._eachPage(lastLength, currentLength, (el) => {
          el.style.visibility = "hidden";
        });
        this._eachPage(lastLength, currentLength - 1, (el) => {
          el.pushedOptions = pushedOptions;
        });
        return this.$el._pushPage({ ...pushedOptions, leavePage: lastTopPage }).then(() => {
          setImmediate(() => {
            this._eachPage(lastLength, currentLength, (el) => {
              el.style.visibility = "";
            });
            this._eachPage(lastLength - 1, currentLength - 1, (el) => {
              el.style.display = "none";
            });
          });
          if (isReattached) {
            this._redetachPage(lastTopPage);
          }
        }, () => {
          this._canceled = true;
          this._popPage();
        });
      }
      if (currentLength < lastLength) {
        this._reattachPage(lastTopPage, null, restoreScroll);
        return this.$el._popPage({}, () => this._redetachPage(lastTopPage));
      }
      currentTopPage.style.visibility = "hidden";
      this._reattachPage(lastTopPage, currentTopPage, restoreScroll);
      return this.$el._pushPage({ ...pushedOptions, _replacePage: true }).then(() => this._redetachPage(lastTopPage));
    },
    _checkUserInteraction(event) {
      if (event.swipeToPop || event.onsBackButton) {
        this._popPage();
      }
    },
    _popPage() {
      this.internalPageStack = this.internalPageStack.slice(0, -1);
    }
  },
  watch: {
    pageStack(after, before) {
      if (this.pageStack !== this.internalPageStack) {
        const lastTopPage = this.$el.children[this.internalPageStack.length - 1];
        const scrollElement = this._findScrollPage(lastTopPage);
        const scrollValue = scrollElement.scrollTop || 0;
        this._pageStackUpdate = {
          lastTopPage,
          lastLength: before.length,
          currentLength: after.length,
          restoreScroll: () => scrollElement.scrollTop = scrollValue
        };
        this.internalPageStack = this.pageStack;
      }
    },
    internalPageStack(after, before) {
      this.$emit("update:pageStack", this.internalPageStack);
    }
  },
  updated() {
    if (this._pageStackUpdate) {
      let currentTopPage = this.$el.children[this.internalPageStack.length - 1];
      const currentTopPageOptions = this.internalPageStack[this.internalPageStack.length - 1].onsNavigatorOptions;
      let { lastTopPage } = this._pageStackUpdate;
      const { lastLength, restoreScroll, currentLength } = this._pageStackUpdate;
      if (currentTopPage !== lastTopPage) {
        this._ready = this._animate({ lastLength, currentLength, lastTopPage, currentTopPage, currentTopPageOptions, restoreScroll });
      } else if (currentLength !== lastLength) {
        currentTopPage.updateBackButton(currentLength > 1);
      }
      lastTopPage = currentTopPage = this._pageStackUpdate = null;
    }
  }
};
var _hoisted_132 = [".options"];
function render42(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-navigator", {
    onPostpop: _cache[0] || (_cache[0] = withModifiers((...args) => $options._checkUserInteraction && $options._checkUserInteraction(...args), ["self"])),
    ".options": _ctx.options
  }, [
    renderSlot(_ctx.$slots, "default", {}, () => [
      (openBlock(true), createElementBlock(Fragment, null, renderList($options.extendsFix, (page, index) => {
        return openBlock(), createBlock(resolveDynamicComponent(page), mergeProps({ key: index }, { ..._ctx.unrecognizedListeners, ...page.onsNavigatorProps }), null, 16);
      }), 128))
    ])
  ], 40, _hoisted_132);
}
script41.render = render42;

// node_modules/onsenui/esm/elements/ons-splitter-side.js
var SPLIT_MODE = "split";
var COLLAPSE_MODE = "collapse";
var CLOSED_STATE = "closed";
var OPEN_STATE = "open";
var CHANGING_STATE = "changing";
var rewritables4 = {
  /**
   * @param {Element} splitterSideElement
   * @param {Function} callback
   */
  ready(splitterSideElement, callback) {
    setImmediate(callback);
  }
};
var CollapseDetection = class {
  constructor(element, target) {
    this._element = element;
    this._onChange = this._onChange.bind(this);
    target && this.changeTarget(target);
  }
  changeTarget(target) {
    this.disable();
    this._target = target;
    if (target) {
      this._orientation = ["portrait", "landscape"].indexOf(target) !== -1;
      this.activate();
    }
  }
  _match(value) {
    if (this._orientation) {
      return this._target === (value.isPortrait ? "portrait" : "landscape");
    }
    return value.matches;
  }
  _onChange(value) {
    this._element._updateMode(this._match(value) ? COLLAPSE_MODE : SPLIT_MODE);
  }
  activate() {
    if (this._orientation) {
      orientation_default.on("change", this._onChange);
      this._onChange({ isPortrait: orientation_default.isPortrait() });
    } else {
      this._queryResult = window.matchMedia(this._target);
      this._queryResult.addListener(this._onChange);
      this._onChange(this._queryResult);
    }
  }
  disable() {
    if (this._orientation) {
      orientation_default.off("change", this._onChange);
    } else if (this._queryResult) {
      this._queryResult.removeListener(this._onChange);
      this._queryResult = null;
    }
  }
};
var SplitterSideElement = class extends BaseElement {
  /**
   * @event modechange
   * @description
   *   [en]Fired just after the component's mode changes.[/en]
   *   [ja]この要素のモードが変化した際に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.side
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {String} event.mode
   *   [en]Returns the current mode. Can be either `"collapse"` or `"split"`.[/en]
   *   [ja]現在のモードを返します。[/ja]
   */
  /**
   * @event preopen
   * @description
   *   [en]Fired just before the sliding menu is opened.[/en]
   *   [ja]スライディングメニューが開く前に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Function} event.cancel
   *   [en]Call to cancel opening sliding menu.[/en]
   *   [ja]スライディングメニューが開くのをキャンセルします。[/ja]
   * @param {Object} event.side
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event postopen
   * @description
   *   [en]Fired just after the sliding menu is opened.[/en]
   *   [ja]スライディングメニューが開いた後に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.side
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event preclose
   * @description
   *   [en]Fired just before the sliding menu is closed.[/en]
   *   [ja]スライディングメニューが閉じる前に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.side
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {Function} event.cancel
   *   [en]Call to cancel opening sliding-menu.[/en]
   *   [ja]スライディングメニューが閉じるのをキャンセルします。[/ja]
   */
  /**
   * @event postclose
   * @description
   *   [en]Fired just after the sliding menu is closed.[/en]
   *   [ja]スライディングメニューが閉じた後に発火します。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクトです。[/ja]
   * @param {Object} event.side
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   */
  /**
   * @event swipe
   * @description
   *   [en]Fired whenever the user slides the splitter.[/en]
   *   [ja][/ja]
   * @param {Object} event [en]Event object.[/en]
   * @param {Object} event.ratio
   *   [en]Decimal ratio (0-1).[/en]
   *   [ja][/ja]
   * @param {Object} event.animationOptions
   *   [en][/en]
   *   [ja][/ja]
   */
  /**
   * @attribute animation
   * @type {String}
   * @default  default
   * @description
   *  [en]Specify the animation. Use one of `overlay`, `push`, `reveal` or  `default`.[/en]
   *  [ja]アニメーションを指定します。"overlay", "push", "reveal", "default"のいずれかを指定できます。[/ja]
   */
  /**
   * @attribute animation-options
   * @type {Expression}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. {duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @property animationOptions
   * @type {Object}
   * @description
   *  [en]Specify the animation's duration, timing and delay with an object literal. E.g. `{duration: 0.2, delay: 1, timing: 'ease-in'}`.[/en]
   *  [ja]アニメーション時のduration, timing, delayをオブジェクトリテラルで指定します。e.g. {duration: 0.2, delay: 1, timing: 'ease-in'}[/ja]
   */
  /**
   * @attribute open-threshold
   * @type {Number}
   * @default  0.3
   * @description
   *  [en]Specify how much the menu needs to be swiped before opening. A value between `0` and `1`.[/en]
   *  [ja]どのくらいスワイプすればスライディングメニューを開くかどうかの割合を指定します。0から1の間の数値を指定します。スワイプの距離がここで指定した数値掛けるこの要素の幅よりも大きければ、スワイプが終わった時にこの要素を開きます。デフォルトは0.3です。[/ja]
   */
  /**
   * @attribute collapse
   * @type {String}
   * @description
   *   [en]
   *     Specify the collapse behavior. Valid values are `"portrait"`, `"landscape"` or a media query.
   *     The string `"portrait"` means the view will collapse when the device is in portrait orientation.
   *     The string `"landscape"` means the view will collapse when the device is in landscape orientation.
   *     If the value is a media query, the view will collapse when the media query resolves to `true`.
   *     If the attribute is set, including as an empty string, the view will always be in `"collapse"` mode.
   *     If the attribute is not set, the view will be in `"split"` mode.
   *   [/en]
   *   [ja]
   *     左側のページを非表示にする条件を指定します。portrait, landscape、width #pxもしくはメディアクエリの指定が可能です。
   *     portraitもしくはlandscapeを指定すると、デバイスの画面が縦向きもしくは横向きになった時に適用されます。
   *     メディアクエリを指定すると、指定したクエリに適合している場合に適用されます。
   *     値に何も指定しない場合には、常にcollapseモードになります。
   *   [/ja]
   */
  /**
   * @attribute swipe-target-width
   * @type {String}
   * @description
   *   [en]The width of swipeable area calculated from the edge (in pixels). Use this to enable swipe only when the finger touch on the screen edge.[/en]
   *   [ja]スワイプの判定領域をピクセル単位で指定します。画面の端から指定した距離に達するとページが表示されます。[/ja]
   */
  /**
   * @attribute width
   * @type {String}
   * @description
   *   [en]Can be specified in either pixels or as a percentage, e.g. `90%` or `200px`.[/en]
   *   [ja]この要素の横幅を指定します。pxと%での指定が可能です。eg. 90%, 200px[/ja]
   */
  /**
   * @attribute side
   * @type {String}
   * @default left
   * @description
   *   [en]Specify which side of the screen the `<ons-splitter-side>` element is located. Possible values are `"left"` and `"right"`.[/en]
   *   [ja]この要素が左か右かを指定します。指定できる値は"left"か"right"のみです。[/ja]
   */
  /**
   * @attribute mode
   * @type {String}
   * @description
   *   [en]Current mode. Possible values are `"collapse"` or `"split"`. This attribute is read only.[/en]
   *   [ja]現在のモードが設定されます。"collapse"もしくは"split"が指定されます。この属性は読み込み専用です。[/ja]
   */
  /**
   * @attribute page
   * @initonly
   * @type {String}
   * @description
   *   [en]The URL of the menu page.[/en]
   *   [ja]ons-splitter-side要素に表示するページのURLを指定します。[/ja]
   */
  /**
   * @attribute swipeable
   * @type {Boolean}
   * @description
   *   [en]Whether to enable swipe interaction on collapse mode.[/en]
   *   [ja]collapseモード時にスワイプ操作を有効にする場合に指定します。[/ja]
   */
  /**
   * @property swipeable
   * @type {Boolean}
   * @description
   *   [en]Whether to enable swipe interaction on collapse mode.[/en]
   *   [ja]collapseモード時にスワイプ操作を有効にする場合に指定します。[/ja]
   */
  constructor() {
    super();
    this._page = null;
    this._state = CLOSED_STATE;
    this._lock = new DoorLock();
    this._pageLoader = defaultPageLoader;
    this._collapseDetection = new CollapseDetection(this);
    this._animatorFactory = new AnimatorFactory({
      animators: SplitterElement.animators,
      baseClass: SplitterAnimator2,
      baseClassName: "SplitterAnimator",
      defaultAnimation: this.getAttribute("animation")
    });
    contentReady(this, () => {
      this.attributeChangedCallback("width");
      if (!this.hasAttribute("side")) {
        this.setAttribute("side", "left");
      }
      rewritables4.ready(this, () => {
        const page = this._page || this.getAttribute("page");
        page && this.load(page);
      });
    });
  }
  connectedCallback() {
    if (!util_default.match(this.parentNode, "ons-splitter")) {
      util_default.throw("Parent must be an ons-splitter element");
    }
    if (!this._swipe) {
      this._swipe = new SwipeReveal({
        element: this,
        elementHandler: this.parentElement,
        swipeMax: () => {
          const ratio = 1;
          this._onSwipe && this._onSwipe(ratio, this._animationOpt);
          util_default.triggerElementEvent(this, "swipe", { ratio, animationOptions: this._animationOpt });
          this.open();
        },
        swipeMid: (distance, width) => {
          const ratio = distance / width;
          this._onSwipe && this._onSwipe(ratio);
          util_default.triggerElementEvent(this, "swipe", { ratio });
          this._animator.translate(distance);
        },
        swipeMin: () => {
          const ratio = 0;
          this._onSwipe && this._onSwipe(ratio, this._animationOpt);
          util_default.triggerElementEvent(this, "swipe", { ratio, animationOptions: this._animationOpt });
          this.close();
        },
        getThreshold: () => Math.max(0, Math.min(1, parseFloat(this.getAttribute("open-threshold")) || 0.3)),
        getSide: () => this.side,
        isInitialState: () => {
          const closed = this._state === CLOSED_STATE;
          this._state = CHANGING_STATE;
          return closed;
        },
        ignoreSwipe: (event, distance) => {
          const isOpen = this.isOpen;
          const validDrag = (d) => this.side === "left" ? d === "left" && isOpen || d === "right" && !isOpen : d === "left" && !isOpen || d === "right" && isOpen;
          const area = Math.max(0, parseInt(this.getAttribute("swipe-target-width"), 10) || 0);
          return this._mode === SPLIT_MODE || this._lock.isLocked() || this._isOtherSideOpen() || !validDrag(event.gesture.direction) || !isOpen && area !== 0 && distance > area;
        }
      });
      this.attributeChangedCallback("swipeable");
    }
    contentReady(this, () => {
      this.constructor.observedAttributes.forEach((attr) => this.attributeChangedCallback(attr, null, this.getAttribute(attr)));
    });
  }
  get side() {
    return this.getAttribute("side") === "right" ? "right" : "left";
  }
  set side(value) {
    if (value) {
      this.setAttribute("side", value);
    } else {
      tihs.removeAttribute("side");
    }
  }
  disconnectedCallback() {
    this._swipe && this._swipe.dispose();
    this._animator = this._animationOpt = this._swipe = null;
  }
  static get observedAttributes() {
    return ["animation", "width", "collapse", "swipeable", "animation-options"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "swipeable":
        this._swipe && this._swipe.update();
        break;
      case "width":
        current = this.getAttribute("width");
        this.style.width = /^\d+(px|%)$/.test(current) ? current : "80%";
        break;
      case "animation":
      case "animation-options":
        this._updateAnimation();
        break;
      default:
        this[util_default.camelize(`_update-${name44}`)](current);
    }
  }
  _emitEvent(name44) {
    if (name44.slice(0, 3) !== "pre") {
      return util_default.triggerElementEvent(this, name44, { side: this });
    }
    let isCanceled = false;
    util_default.triggerElementEvent(this, name44, {
      side: this,
      cancel: () => isCanceled = true
    });
    return isCanceled;
  }
  _isOtherSideOpen() {
    return !!util_default.findChild(
      this.parentElement,
      (el) => el instanceof this.constructor && el !== this && el._mode === COLLAPSE_MODE && el.isOpen
    );
  }
  _updateCollapse(value = this.getAttribute("collapse")) {
    if (value === null || value === "split") {
      this._collapseDetection.disable();
      return this._updateMode(SPLIT_MODE);
    }
    if (value === "" || value === "collapse") {
      this._collapseDetection.disable();
      return this._updateMode(COLLAPSE_MODE);
    }
    this._collapseDetection.changeTarget(value);
  }
  _updateMode(mode) {
    if (mode !== this._mode) {
      this._mode = mode;
      this.setAttribute("mode", mode);
      if (mode === SPLIT_MODE) {
        this._animator && this._animator.deactivate();
        this._state = CLOSED_STATE;
      } else {
        this._animator && this._animator.activate(this);
        this._state === OPEN_STATE && this._animator.open();
      }
      util_default.triggerElementEvent(this, "modechange", { side: this, mode });
    }
  }
  _updateAnimation(animation = this.getAttribute("animation")) {
    if (this.parentNode) {
      this._animator && this._animator.deactivate();
      this._animator = this._animatorFactory.newAnimator({ animation });
      this._animator.activate(this);
      this._animationOpt = {
        timing: this._animator.duration,
        duration: this._animator.duration
      };
      this._animator.updateOptions(this.animationOptions);
    }
  }
  /**
   * @property page
   * @type {*}
   * @description
   *   [en]Page location to load in the splitter side.[/en]
   *   [ja]この要素内に表示するページを指定します。[/ja]
   */
  get page() {
    return this._page;
  }
  /**
   * @param {*} page
   */
  set page(page) {
    this._page = page;
  }
  get _content() {
    return this.children[0];
  }
  /**
   * @property pageLoader
   * @description
   *   [en][/en]
   *   [ja][/ja]
   */
  get pageLoader() {
    return this._pageLoader;
  }
  set pageLoader(loader) {
    if (!(loader instanceof PageLoader)) {
      util_default.throwPageLoader();
    }
    this._pageLoader = loader;
  }
  /**
   * @property mode
   * @readonly
   * @type {String}
   * @description
   *   [en]Current mode. Possible values are "split", "collapse", "closed", "open" or "changing".[/en]
   *   [ja][/ja]
   */
  get mode() {
    return this._mode;
  }
  /**
   * @property onSwipe
   * @type {Function}
   * @description
   *   [en]Hook called whenever the user slides the splitter. It gets a decimal ratio (0-1) and an animationOptions object as arguments.[/en]
   *   [ja][/ja]
   */
  get onSwipe() {
    return this._onSwipe;
  }
  set onSwipe(value) {
    if (value && !(value instanceof Function)) {
      util_default.throw('"onSwipe" must be a function');
    }
    this._onSwipe = value;
  }
  get animationOptions() {
    return this.hasAttribute("animation-options") ? AnimatorFactory.parseAnimationOptionsString(this.getAttribute("animation-options")) : {};
  }
  set animationOptions(value) {
    if (value === void 0 || value === null) {
      this.removeAttribute("animation-options");
    } else {
      this.setAttribute("animation-options", JSON.stringify(value));
    }
  }
  /**
   * @property isOpen
   * @type {Boolean}
   * @description
   *   [en]Specifies whether the menu is opened.[/en]
   *   [ja][/ja]
   */
  get isOpen() {
    return this._mode === COLLAPSE_MODE && this._state !== CLOSED_STATE;
  }
  set isOpen(value) {
    this.toggle({}, value);
  }
  /**
   * @method open
   * @signature open([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {Function} [options.callback]
   *   [en]This function will be called after the menu has been opened.[/en]
   *   [ja]メニューが開いた後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Open menu in collapse mode.[/en]
   *   [ja]collapseモードになっているons-splitter-side要素を開きます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the splitter side element or false if not in collapse mode[/en]
   *   [ja][/ja]
   */
  open(options) {
    return this.toggle(options, true);
  }
  /**
   * @method close
   * @signature close([options])
   * @param {Object} [options]
   *   [en]Parameter object.[/en]
   *   [ja]オプションを指定するオブジェクト。[/ja]
   * @param {Function} [options.callback]
   *   [en]This function will be called after the menu has been closed.[/en]
   *   [ja]メニューが閉じた後に呼び出される関数オブジェクトを指定します。[/ja]
   * @description
   *   [en]Close menu in collapse mode.[/en]
   *   [ja]collapseモードになっているons-splitter-side要素を閉じます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the splitter side element or false if not in collapse mode[/en]
   *   [ja][/ja]
   */
  close(options) {
    return this.toggle(options, false);
  }
  /**
   * @method toggle
   * @signature toggle([options])
   * @param {Object} [options]
   * @description
   *   [en]Opens if it's closed. Closes if it's open.[/en]
   *   [ja]開けている場合は要素を閉じますそして開けている場合は要素を開きます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the splitter side element or false if not in collapse mode[/en]
   *   [ja][/ja]
   */
  toggle(options = {}, force) {
    const shouldOpen = typeof force === "boolean" ? force : !this.isOpen;
    const action = shouldOpen ? "open" : "close";
    const FINAL_STATE = shouldOpen ? OPEN_STATE : CLOSED_STATE;
    if (this._mode === SPLIT_MODE) {
      return Promise.resolve(false);
    }
    if (this._state === FINAL_STATE) {
      return Promise.resolve(this);
    }
    if (this._lock.isLocked()) {
      return Promise.reject("Another splitter-side action is already running.");
    }
    if (shouldOpen && this._isOtherSideOpen()) {
      return Promise.reject("Another menu is already open.");
    }
    if (this._emitEvent(`pre${action}`)) {
      return Promise.reject(`Canceled in pre${action} event.`);
    }
    const unlock = this._lock.lock();
    this._state = CHANGING_STATE;
    if (options.animation) {
      this._updateAnimation(options.animation);
    }
    return new Promise((resolve) => {
      this._animator[action](() => {
        util_default.iosPageScrollFix(shouldOpen);
        this._state = FINAL_STATE;
        unlock();
        this._emitEvent(`post${action}`);
        options.callback instanceof Function && options.callback(this);
        resolve(this);
      });
    });
  }
  /**
   * @method load
   * @signature load(page, [options])
   * @param {String} page
   *   [en]Page URL. Can be either an HTML document or a `<template>`.[/en]
   *   [ja]pageのURLか、`<template>`で宣言したテンプレートのid属性の値を指定します。[/ja]
   * @param {Object} [options]
   * @param {Function} [options.callback]
   * @description
   *   [en]Show the page specified in pageUrl in the right section[/en]
   *   [ja]指定したURLをメインページを読み込みます。[/ja]
   * @return {Promise}
   *   [en]Resolves to the new page element[/en]
   *   [ja][/ja]
   */
  load(page, options = {}) {
    this._page = page;
    const callback = options.callback || (() => {
    });
    return new Promise((resolve) => {
      let oldContent = this._content || null;
      this._pageLoader.load({ page, parent: this }, (pageElement) => {
        if (oldContent) {
          this._pageLoader.unload(oldContent);
          oldContent = null;
        }
        setImmediate(() => this._show());
        callback(pageElement);
        resolve(pageElement);
      });
    });
  }
  _show() {
    if (this._content) {
      this._content._show();
    }
  }
  _hide() {
    if (this._content) {
      this._content._hide();
    }
  }
  _destroy() {
    if (this._content) {
      this._pageLoader.unload(this._content);
    }
    this.remove();
  }
  static get events() {
    return ["preopen", "postopen", "preclose", "postclose", "modechange", "swipe"];
  }
  static get rewritables() {
    return rewritables4;
  }
};
util_default.defineBooleanProperties(SplitterSideElement, ["swipeable"]);
elements_default.SplitterSide = SplitterSideElement;
customElements.define("ons-splitter-side", SplitterSideElement);

// node_modules/vue-onsenui/esm/components/VOnsSplitterSide.js
var name40 = "v-ons-splitter-side";
var script42 = {
  name: name40,
  mixins: [hasOptions, deriveEvents(name40)],
  emits: ["update:open"],
  props: {
    open: {
      type: Boolean,
      default: void 0
    }
  },
  methods: {
    action() {
      this._shouldUpdate() && this.$el[this.open ? "open" : "close"].call(this.$el, this.options).catch(() => {
      });
    },
    _shouldUpdate() {
      return this.open !== void 0 && this.open !== this.$el.isOpen;
    }
  },
  watch: {
    open() {
      this.action();
    }
  },
  mounted() {
    this._updateOpenHandler = () => this._shouldUpdate() && this.$emit("update:open", this.$el.isOpen);
    ["postopen", "postclose", "modechange"].forEach((event) => this.$el.addEventListener(event, this._updateOpenHandler));
    this.action();
  },
  beforeDestroy() {
    ["postopen", "postclose", "modechange"].forEach((event) => this.$el.removeEventListener(event, this._updateOpenHandler));
  }
};
function render43(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-splitter-side", null, [
    renderSlot(_ctx.$slots, "default")
  ]);
}
script42.render = render43;

// node_modules/onsenui/esm/ons/internal/lazy-repeat.js
var LazyRepeatDelegate = class {
  constructor(userDelegate, templateElement = null) {
    if (typeof userDelegate !== "object" || userDelegate === null) {
      util_default.throw('"delegate" parameter must be an object');
    }
    this._userDelegate = userDelegate;
    if (!(templateElement instanceof Element) && templateElement !== null) {
      util_default.throw('"templateElement" parameter must be an instance of Element or null');
    }
    this._templateElement = templateElement;
  }
  get itemHeight() {
    return this._userDelegate.itemHeight;
  }
  /**
   * @return {Boolean}
   */
  hasRenderFunction() {
    return this._userDelegate._render instanceof Function;
  }
  /**
   * @return {void}
   */
  _render() {
    this._userDelegate._render.apply(this._userDelegate, arguments);
  }
  /**
   * @param {Number} index
   * @param {Function} done A function that take item object as parameter.
   */
  loadItemElement(index, done) {
    if (this._userDelegate.loadItemElement instanceof Function) {
      this._userDelegate.loadItemElement(index, done);
    } else {
      const element = this._userDelegate.createItemContent(index, this._templateElement);
      if (!(element instanceof Element)) {
        util_default.throw('"createItemContent" must return an instance of Element');
      }
      done({ element });
    }
  }
  /**
   * @return {Number}
   */
  countItems() {
    const count = this._userDelegate.countItems();
    if (typeof count !== "number") {
      util_default.throw('"countItems" must return a number');
    }
    return count;
  }
  /**
   * @param {Number} index
   * @param {Object} item
   * @param {Element} item.element
   */
  updateItem(index, item) {
    if (this._userDelegate.updateItemContent instanceof Function) {
      this._userDelegate.updateItemContent(index, item);
    }
  }
  /**
   * @return {Number}
   */
  calculateItemHeight(index) {
    if (this._userDelegate.calculateItemHeight instanceof Function) {
      const height = this._userDelegate.calculateItemHeight(index);
      if (typeof height !== "number") {
        util_default.throw('"calculateItemHeight" must return a number');
      }
      return height;
    }
    return 0;
  }
  /**
   * @param {Number} index
   * @param {Object} item
   */
  destroyItem(index, item) {
    if (this._userDelegate.destroyItem instanceof Function) {
      this._userDelegate.destroyItem(index, item);
    }
  }
  /**
   * @return {void}
   */
  destroy() {
    if (this._userDelegate.destroy instanceof Function) {
      this._userDelegate.destroy();
    }
    this._userDelegate = this._templateElement = null;
  }
};
var LazyRepeatProvider = class {
  /**
   * @param {Element} wrapperElement
   * @param {LazyRepeatDelegate} delegate
   */
  constructor(wrapperElement, delegate) {
    if (!(delegate instanceof LazyRepeatDelegate)) {
      util_default.throw('"delegate" parameter must be an instance of LazyRepeatDelegate');
    }
    this._wrapperElement = wrapperElement;
    this._delegate = delegate;
    this._insertIndex = this._wrapperElement.children[0] && this._wrapperElement.children[0].tagName === "ONS-LAZY-REPEAT" ? 1 : 0;
    if (wrapperElement.tagName.toLowerCase() === "ons-list") {
      wrapperElement.classList.add("lazy-list");
    }
    this._pageContent = this._findPageContentElement(wrapperElement);
    if (!this._pageContent) {
      util_default.throw("LazyRepeat must be descendant of a Page element");
    }
    this.lastScrollTop = this._pageContent.scrollTop;
    this.padding = 0;
    this._topPositions = [0];
    this._renderedItems = {};
    if (!this._delegate.itemHeight && !this._delegate.calculateItemHeight(0)) {
      this._unknownItemHeight = true;
    }
    this._addEventListeners();
    this._onChange();
  }
  get padding() {
    return parseInt(this._wrapperElement.style.paddingTop, 10);
  }
  set padding(newValue) {
    this._wrapperElement.style.paddingTop = newValue + "px";
  }
  _findPageContentElement(wrapperElement) {
    const pageContent = util_default.findParent(wrapperElement, ".page__content");
    if (pageContent) {
      return pageContent;
    }
    const page = util_default.findParent(wrapperElement, "ons-page");
    if (page) {
      const content = util_default.findChild(page, ".content");
      if (content) {
        return content;
      }
    }
    return null;
  }
  _checkItemHeight(callback) {
    this._delegate.loadItemElement(0, (item) => {
      if (!this._unknownItemHeight) {
        util_default.throw("Invalid state");
      }
      this._wrapperElement.appendChild(item.element);
      const done = () => {
        this._delegate.destroyItem(0, item);
        item.element && item.element.remove();
        delete this._unknownItemHeight;
        callback();
      };
      this._itemHeight = item.element.offsetHeight;
      if (this._itemHeight > 0) {
        done();
        return;
      }
      this._wrapperElement.style.visibility = "hidden";
      item.element.style.visibility = "hidden";
      setImmediate(() => {
        this._itemHeight = item.element.offsetHeight;
        if (this._itemHeight == 0) {
          util_default.throw('Invalid state: "itemHeight" must be greater than zero');
        }
        this._wrapperElement.style.visibility = "";
        done();
      });
    });
  }
  get staticItemHeight() {
    return this._delegate.itemHeight || this._itemHeight;
  }
  _countItems() {
    return this._delegate.countItems();
  }
  _getItemHeight(i) {
    if (Object.prototype.hasOwnProperty.call(this._renderedItems, i)) {
      if (!Object.prototype.hasOwnProperty.call(this._renderedItems[i], "height")) {
        this._renderedItems[i].height = this._renderedItems[i].element.offsetHeight;
      }
      return this._renderedItems[i].height;
    }
    if (this._topPositions[i + 1] && this._topPositions[i]) {
      return this._topPositions[i + 1] - this._topPositions[i];
    }
    return this.staticItemHeight || this._delegate.calculateItemHeight(i);
  }
  _calculateRenderedHeight() {
    return Object.keys(this._renderedItems).reduce((a, b) => a + this._getItemHeight(+b), 0);
  }
  _onChange() {
    this._render();
  }
  _lastItemRendered() {
    return Math.max(...Object.keys(this._renderedItems));
  }
  _firstItemRendered() {
    return Math.min(...Object.keys(this._renderedItems));
  }
  refresh() {
    const forceRender = { forceScrollDown: true };
    const firstItemIndex = this._firstItemRendered();
    if (util_default.isInteger(firstItemIndex)) {
      this._wrapperElement.style.height = this._topPositions[firstItemIndex] + this._calculateRenderedHeight() + "px";
      this.padding = this._topPositions[firstItemIndex];
      forceRender.forceFirstIndex = firstItemIndex;
    }
    this._removeAllElements();
    this._render(forceRender);
    this._wrapperElement.style.height = "inherit";
  }
  _render({ forceScrollDown = false, forceFirstIndex, forceLastIndex } = {}) {
    if (this._unknownItemHeight) {
      return this._checkItemHeight(this._render.bind(this, arguments[0]));
    }
    const isScrollUp = !forceScrollDown && this.lastScrollTop > this._pageContent.scrollTop;
    this.lastScrollTop = this._pageContent.scrollTop;
    const keep = {};
    const offset = this._wrapperElement.getBoundingClientRect().top;
    const limit = 4 * window.innerHeight - offset;
    const count = this._countItems();
    const items = [];
    const start = forceFirstIndex || Math.max(0, this._calculateStartIndex(offset) - 30);
    let i = start;
    for (let top = this._topPositions[i]; i < count && top < limit; i++) {
      if (i >= this._topPositions.length) {
        this._topPositions.length += 100;
      }
      this._topPositions[i] = top;
      top += this._getItemHeight(i);
    }
    if (this._delegate.hasRenderFunction && this._delegate.hasRenderFunction()) {
      return this._delegate._render(start, i, () => {
        this.padding = this._topPositions[start];
      });
    }
    if (isScrollUp) {
      for (let j = i - 1; j >= start; j--) {
        keep[j] = true;
        this._renderElement(j, isScrollUp);
      }
    } else {
      const lastIndex = forceLastIndex || Math.max(i - 1, ...Object.keys(this._renderedItems));
      for (let j = start; j <= lastIndex; j++) {
        keep[j] = true;
        this._renderElement(j, isScrollUp);
      }
    }
    Object.keys(this._renderedItems).forEach((key) => keep[key] || this._removeElement(key, isScrollUp));
  }
  /**
   * @param {Number} index
   * @param {Boolean} isScrollUp
   */
  _renderElement(index, isScrollUp) {
    const item = this._renderedItems[index];
    if (item) {
      this._delegate.updateItem(index, item);
      return;
    }
    this._delegate.loadItemElement(index, (item2) => {
      if (isScrollUp) {
        this._wrapperElement.insertBefore(item2.element, this._wrapperElement.children[this._insertIndex]);
        this.padding = this._topPositions[index];
        item2.height = this._topPositions[index + 1] - this._topPositions[index];
      } else {
        this._wrapperElement.appendChild(item2.element);
      }
      this._renderedItems[index] = item2;
    });
  }
  /**
   * @param {Number} index
   * @param {Boolean} isScrollUp
   */
  _removeElement(index, isScrollUp = true) {
    index = +index;
    const item = this._renderedItems[index];
    this._delegate.destroyItem(index, item);
    if (isScrollUp) {
      this._topPositions[index + 1] = void 0;
    } else {
      this.padding = this.padding + this._getItemHeight(index);
    }
    if (item.element.parentElement) {
      item.element.parentElement.removeChild(item.element);
    }
    delete this._renderedItems[index];
  }
  _removeAllElements() {
    Object.keys(this._renderedItems).forEach((key) => this._removeElement(key));
  }
  _recalculateTopPositions(start, end) {
    for (let i = start; i <= end; i++) {
      this._topPositions[i + 1] = this._topPositions[i] + this._getItemHeight(i);
    }
  }
  _calculateStartIndex(current) {
    const firstItemIndex = this._firstItemRendered();
    const lastItemIndex = this._lastItemRendered();
    this._recalculateTopPositions(firstItemIndex, lastItemIndex);
    let start = 0;
    let end = this._countItems() - 1;
    for (; ; ) {
      const middle = Math.floor((start + end) / 2);
      const value = current + this._topPositions[middle];
      if (end < start) {
        return 0;
      } else if (value <= 0 && value + this._getItemHeight(middle) > 0) {
        return middle;
      } else if (isNaN(value) || value >= 0) {
        end = middle - 1;
      } else {
        start = middle + 1;
      }
    }
  }
  _debounce(func, wait, immediate) {
    let timeout;
    return function() {
      const callNow = immediate && !timeout;
      clearTimeout(timeout);
      if (callNow) {
        func.apply(this, arguments);
      } else {
        timeout = setTimeout(() => {
          timeout = null;
          func.apply(this, arguments);
        }, wait);
      }
    };
  }
  _doubleFireOnTouchend() {
    this._render();
    this._debounce(this._render.bind(this), 100);
  }
  _addEventListeners() {
    util_default.bindListeners(this, ["_onChange", "_doubleFireOnTouchend"]);
    if (platform_default.isIOS()) {
      this._boundOnChange = this._debounce(this._boundOnChange, 30);
    }
    this._pageContent.addEventListener("scroll", this._boundOnChange, true);
    if (platform_default.isIOS()) {
      util_default.addEventListener(this._pageContent, "touchmove", this._boundOnChange, { capture: true, passive: true });
      this._pageContent.addEventListener("touchend", this._boundDoubleFireOnTouchend, true);
    }
    window.document.addEventListener("resize", this._boundOnChange, true);
  }
  _removeEventListeners() {
    this._pageContent.removeEventListener("scroll", this._boundOnChange, true);
    if (platform_default.isIOS()) {
      util_default.removeEventListener(this._pageContent, "touchmove", this._boundOnChange, { capture: true, passive: true });
      this._pageContent.removeEventListener("touchend", this._boundDoubleFireOnTouchend, true);
    }
    window.document.removeEventListener("resize", this._boundOnChange, true);
  }
  destroy() {
    this._removeAllElements();
    this._delegate.destroy();
    this._parentElement = this._delegate = this._renderedItems = null;
    this._removeEventListeners();
  }
};

// node_modules/onsenui/esm/elements/ons-lazy-repeat.js
var LazyRepeatElement = class extends BaseElement {
  connectedCallback() {
    if (this.hasAttribute("delegate")) {
      this.delegate = window[this.getAttribute("delegate")];
    }
  }
  /**
   * @property delegate
   * @type {Object}
   * @description
   *  [en]Specify a delegate object to load and unload item elements.[/en]
   *  [ja]要素のロード、アンロードなどの処理を委譲するオブジェクトを指定します。[/ja]
   */
  /**
   * @property delegate.createItemContent
   * @type {Function}
   * @description
   *   [en]
   *     This function should return a `HTMLElement`.
   *
   *     To help rendering the element, the current index and a template is supplied as arguments. The template is the initial content of the `<ons-lazy-repeat>` element.
   *   [/en]
   *   [ja]
   *     この関数は`HTMLElement`を返してください。
   *     要素を生成しやすくするために、現在のアイテムのインデックスとテンプレートが引数に渡されます。
   *     このテンプレートは、`<ons-lazy-repeat>`要素のコンテンツが渡されます。
   *   [/ja]
   */
  /**
   * @property delegate.countItems
   * @type {Function}
   * @description
   *   [en]Should return the number of items in the list.[/en]
   *   [ja]リスト内のアイテム数を返してください。[/ja]
   */
  /**
   * @property delegate.calculateItemHeight
   * @type {Function}
   * @description
   *   [en]
   *     Should return the height of an item. The index is provided as an argument.
   *
   *     This is important when rendering lists where the items have different height.
   *
   *     The function is optional and if it isn't present the height of the first item will be automatically calculated and used for all other items.
   *   [/en]
   *   [ja]
   *     アイテムの高さ(ピクセル)を返してください。アイテムのインデックス値は引数で渡されます。
   *     この関数は、それぞれのアイムが違った高さを持つリストをレンダリングする際に重要です。
   *     この関数はオプショナルです。もしこの関数が無い場合には、
   *     最初のアイテムの高さが他のすべてのアイテムの高さとして利用されます。
   *   [/ja]
   */
  /**
   * @property delegate.destroyItem
   * @type {Function}
   * @description
   *   [en]
   *     This function is used called when an item is removed from the DOM. The index and DOM element is provided as arguments.
   *
   *     The function is optional but may be important in order to avoid memory leaks.
   *   [/en]
   *   [ja]
   *     この関数は、あるアイテムがDOMツリーから除かれた時に呼び出されます。
   *     アイテムのインデックス値とDOM要素が引数として渡されます。
   *     この関数はオプショナルですが、各アイテムの後処理が必要な場合にはメモリーリークを避けるために重要です。
   *   [/ja]
   */
  set delegate(userDelegate) {
    this._lazyRepeatProvider && this._lazyRepeatProvider.destroy();
    if (!this._templateElement && this.children[0]) {
      this._templateElement = this.removeChild(this.children[0]);
    }
    const delegate = new LazyRepeatDelegate(userDelegate, this._templateElement || null);
    this._lazyRepeatProvider = new LazyRepeatProvider(this.parentElement, delegate);
  }
  get delegate() {
    util_default.throw("No delegate getter");
  }
  /**
   * @method refresh
   * @signature refresh()
   * @description
   *   [en]Refresh the list. Use this method when the data has changed.[/en]
   *   [ja]リストを更新します。もしデータが変わった場合にはこのメソッドを使ってください。[/ja]
   */
  refresh() {
    this._lazyRepeatProvider && this._lazyRepeatProvider.refresh();
  }
  attributeChangedCallback(name44, last, current) {
  }
  disconnectedCallback() {
    if (this._lazyRepeatProvider) {
      this._lazyRepeatProvider.destroy();
      this._lazyRepeatProvider = null;
    }
  }
};
internal_default.LazyRepeatDelegate = LazyRepeatDelegate;
internal_default.LazyRepeatProvider = LazyRepeatProvider;
elements_default.LazyRepeat = LazyRepeatElement;
customElements.define("ons-lazy-repeat", LazyRepeatElement);

// node_modules/vue-onsenui/esm/components/VOnsLazyRepeat.js
var script43 = {
  name: "v-ons-lazy-repeat",
  props: {
    renderItem: {
      type: Function,
      required: true
    },
    length: {
      type: Number,
      required: true
    },
    calculateItemHeight: {
      type: Function,
      default: void 0
    }
  },
  data() {
    return {
      provider: null
    };
  },
  methods: {
    _setup() {
      this.provider && this.provider.destroy();
      const delegate = new this.$ons._ons._internal.LazyRepeatDelegate({
        calculateItemHeight: this.calculateItemHeight,
        createItemContent: (i) => {
          let vnode = createVNode(this.renderItem(i));
          vnode.appContext = this.$ons._app._context;
          let fragment = document.createDocumentFragment();
          render(vnode, fragment);
          let element = fragment.firstChild;
          element.destroy = () => {
            fragment = null;
            vnode = null;
            element = null;
          };
          return fragment.firstChild;
        },
        destroyItem: (i, { element }) => element.destroy(),
        countItems: () => this.length
      }, null);
      this.provider = new this.$ons._ons._internal.LazyRepeatProvider(this.$parent.$el, delegate);
    },
    refresh() {
      return this.provider.refresh();
    }
  },
  watch: {
    renderItem() {
      this._setup();
    },
    length() {
      this._setup();
    },
    calculateItemHeight() {
      this._setup();
    }
  },
  mounted() {
    this._setup();
  },
  beforeDestroy() {
    this.$el._lazyRepeatProvider = this.provider;
    this.provider = null;
  }
};
function render44(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-lazy-repeat");
}
script43.render = render44;

// node_modules/onsenui/esm/elements/ons-select.js
var scheme30 = {
  "": "select-* select--*",
  ".select-input": "select-input--*"
};
var defaultClassName16 = "select";
var INPUT_ATTRIBUTES2 = [
  "autofocus",
  "disabled",
  "form",
  "multiple",
  "name",
  "required",
  "size"
];
var SelectElement = class extends BaseElement {
  /**
   * @attribute autofocus
   * @type {Boolean}
   * @default false
   * @description
   *  [en]Element automatically gains focus on page load.[/en]
   *  [ja]ページロード時にこのセレクトボックスにフォーカスが移るようにします。[/ja]
   */
  /**
   * @attribute disabled
   * @type {Boolean}
   * @default false
   * @description
   *   [en]Specify if select input should be disabled.[/en]
   *   [ja]このセレクトボックスを無効化する場合に指定します。[/ja]
   */
  /**
   * @attribute form
   * @type {String}
   * @description
   *   [en]Associate a select element to an existing form on the page, even if not nested.[/en]
   *   [ja]このセレクトボックスを、指定した `form` 要素に紐付けます。セレクトボックスを `form` 要素の外側に配置する際に使用します。[/ja]
   */
  /**
   * @attribute multiple
   * @type {Boolean}
   * @default false
   * @description
   *  [en]If this attribute is defined, multiple options can be selected at once.[/en]
   *  [ja]選択肢の複数選択を有効にします。[/ja]
   */
  /**
   * @attribute name
   * @type {String}
   * @description
   *   [en]Name the select element, useful for instance if it is part of a form.[/en]
   *   [ja]このセレクトボックスの名前を指定します。通常 `form` 要素と共に使用します。[/ja]
   */
  /**
   * @attribute required
   * @type {Boolean}
   * @description
   *   [en]Make the select input required for submitting the form it is part of.[/en]
   *   [ja]このセレクトボックスを入力必須にする場合に指定します。通常 `form` 要素と共に使用します。[/ja]
   */
  /**
   * @attribute select-id
   * @type {String}
   * @description
   *   [en]ID given to the inner select, useful for dynamic manipulation.[/en]
   *   [ja]このセレクトボックスが内部に持つ select 要素に与える ID を指定します。セレクトボックスの内容を動的に変更する必要がある場合に使用します。[/ja]
   */
  /**
   * @attribute size
   * @type {Number}
   * @default 1
   * @description
   *   [en]How many options are displayed; if there are more than the size then a scroll appears to navigate them.[/en]
   *   [ja]一度に表示する選択肢の個数を指定します。選択肢がこの属性で指定した個数よりも多い場合、スクロールが有効になります。[/ja]
   */
  constructor() {
    super();
    contentReady(this, () => this._compile());
    this._deriveGetters();
  }
  static get observedAttributes() {
    return ["modifier", "class", ...INPUT_ATTRIBUTES2];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName16, scheme30);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme30);
        break;
    }
    if (INPUT_ATTRIBUTES2.indexOf(name44) >= 0) {
      contentReady(this, () => this._updateBoundAttributes());
    }
  }
  get _select() {
    return this.querySelector("select");
  }
  _updateBoundAttributes() {
    INPUT_ATTRIBUTES2.forEach((attr) => {
      if (this.hasAttribute(attr)) {
        this._select.setAttribute(attr, this.getAttribute(attr));
      } else {
        this._select.removeAttribute(attr);
      }
    });
  }
  /**
   * @property length
   * @description
   *   [en]Number of options in the select box.[/en]
   *   [ja]このセレクトボックスに含まれる選択肢の個数を返します。 `select` 要素[/ja]
   */
  /**
   * @property options
   * @description
   *   [en]Several options for handling the select DOM object.[/en]
   *   [ja]このセレクトボックスに含まれる `option` 要素の配列を返します。[/ja]
   */
  /**
   * @property selectedIndex
   * @description
   *   [en]Index of the currently selected option.[/en]
   *   [ja]現在選択されている選択肢のインデックスを返します。[/ja]
   */
  /**
   * @property value
   * @description
   *   [en]Value of the currently selected option.[/en]
   *   [ja]現在選択されている選択肢の値を返します。[/ja]
   */
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName16);
    const sel = this._select || document.createElement("select");
    if (!sel.id && this.hasAttribute("select-id")) {
      sel.id = this.getAttribute("select-id");
    }
    sel.classList.add("select-input");
    if (!this._select) {
      util_default.arrayFrom(this.childNodes).forEach((element) => sel.appendChild(element));
      this.appendChild(sel);
    }
    ModifierUtil.initModifier(this, scheme30);
  }
  _deriveGetters() {
    ["disabled", "length", "multiple", "name", "options", "selectedIndex", "size", "value", "form", "type"].forEach((key) => {
      Object.defineProperty(this, key, {
        configurable: true,
        enumerable: true,
        get: () => this._select[key],
        set: ["form", "type"].indexOf(key) === -1 ? (value) => contentReady(this, () => this._select[key] = value) : void 0
      });
    });
  }
  add(option, index = null) {
    this._select.add(option, index);
  }
  // If called with an index argument, removes the option element with the given index.
  // If called with no arguments, removes this.
  // This behaviour might sound crazy but it is the same as <select>'s `remove` method.
  // https://developer.mozilla.org/en-US/docs/Web/API/HTMLSelectElement/remove
  remove(index) {
    if (index === void 0) {
      Element.prototype.remove.call(this);
    } else {
      this._select.remove(index);
    }
  }
};
elements_default.Select = SelectElement;
customElements.define("ons-select", SelectElement);

// node_modules/vue-onsenui/esm/components/VOnsSelect.js
var script44 = {
  name: "v-ons-select",
  mixins: [modifier],
  emits: ["update:modelValue"],
  props: {
    modelValue: [Number, String]
  },
  computed: {
    selectedValue: {
      get() {
        return this.modelValue;
      },
      set(val) {
        this.$emit("update:modelValue", val);
      }
    }
  }
};
var _hoisted_133 = ["modifier"];
function render45(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-select", { modifier: _ctx.normalizedModifier }, [
    withDirectives(createBaseVNode("select", {
      "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $options.selectedValue = $event)
    }, [
      renderSlot(_ctx.$slots, "default")
    ], 512), [
      [vModelSelect, $options.selectedValue]
    ])
  ], 8, _hoisted_133);
}
script44.render = render45;

// node_modules/onsenui/esm/elements/ons-segment.js
var defaultClassName17 = "segment";
var scheme31 = {
  "": "segment--*",
  ".segment__item": "segment--*__item",
  ".segment__input": "segment--*__input",
  ".segment__button": "segment--*__button"
};
var generateId = (() => {
  let i = 0;
  return () => "ons-segment-gen-" + i++;
})();
var SegmentElement = class extends BaseElement {
  /**
   * @event postchange
   * @description
   *   [en]Fires after the active button is changed.[/en]
   *   [ja][/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja][/ja]
   * @param {Number} event.index
   *   [en]Tapped button index.[/en]
   *   [ja][/ja]
   * @param {Object} event.segmentItem
   *   [en]Segment item object.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *  [en]The appearance of the segment.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute tabbar-id
   * @initonly
   * @type {String}
   * @description
   *  [en]ID of the tabbar element to "connect" to the segment. Must be inside the same page.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute active-index
   * @default 0
   * @type {Number}
   * @description
   *  [en]Index of the active button. If a tabbar is connected, this will be set to the tabbar's active index.[/en]
   *  [ja][/ja]
   */
  /**
   * @property activeIndex
   * @default 0
   * @type {Number}
   * @description
   *  [en]Index of the active button. If a tabbar is connected, this will be set to the tabbar's active index.[/en]
   *  [ja][/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]Specify if segment should be disabled.[/en]
   *   [ja]ボタンを無効化する場合は指定します。[/ja]
   */
  constructor() {
    super();
    this._segmentId = generateId();
    this._tabbar = null;
    this._onChange = this._onChange.bind(this);
    this._onTabbarPreChange = this._onTabbarPreChange.bind(this);
    contentReady(this, () => {
      this._compile();
      setImmediate(() => this._lastActiveIndex = this._tabbar ? this._tabbar.getActiveTabIndex() : this.getActiveButtonIndex());
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    this.classList.add(defaultClassName17);
    for (let index = this.children.length - 1; index >= 0; index--) {
      const item = this.children[index];
      item.classList.add("segment__item");
      const input = util_default.findChild(item, ".segment__input") || util_default.create("input.segment__input");
      input.type = "radio";
      input.value = index;
      input.name = input.name || this._segmentId;
      input.checked = !this.hasAttribute("tabbar-id") && index === (this.activeIndex || 0);
      const button = util_default.findChild(item, ".segment__button") || util_default.create(".segment__button");
      if (button.parentElement !== item) {
        while (item.firstChild) {
          button.appendChild(item.firstChild);
        }
      }
      item.appendChild(input);
      item.appendChild(button);
    }
    ModifierUtil.initModifier(this, scheme31);
  }
  connectedCallback() {
    contentReady(this, () => {
      if (this.hasAttribute("tabbar-id")) {
        const page = util_default.findParent(this, "ons-page");
        this._tabbar = page && page.querySelector("#" + this.getAttribute("tabbar-id"));
        if (!this._tabbar || this._tabbar.tagName !== "ONS-TABBAR") {
          util_default.throw(`No tabbar with id ${this.getAttribute("tabbar-id")} was found.`);
        }
        this._tabbar.setAttribute("hide-tabs", "");
        setImmediate(() => {
          const index = this._tabbar.getActiveTabIndex();
          this._setChecked(index);
          this.activeIndex = index;
        });
        this._tabbar.addEventListener("prechange", this._onTabbarPreChange);
      }
    });
    this.addEventListener("change", this._onChange);
  }
  disconnectedCallback() {
    contentReady(this, () => {
      if (this._tabbar) {
        this._tabbar.removeEventListener("prechange", this._onTabbarPreChange);
        this._tabbar = null;
      }
    });
    this.removeEventListener("change", this._onChange);
  }
  _setChecked(index) {
    this.children[index].firstElementChild.checked = true;
  }
  /**
   * @method setActiveButton
   * @signature setActiveButton(index, [options])
   * @param {Number} index
   *   [en]Button index.[/en]
   *   [ja][/ja]
   * @param {Object} [options]
   *   [en]Parameter object, works only if there is a connected tabbar. Supports the same options as `ons-tabbar`'s `setActiveTab` method.[/en]
   *   [ja][/ja]
   * @description
   *   [en]Make button with the specified index active. If there is a connected tabbar it shows the corresponding tab page. In this case animations and their options can be specified by the second parameter.[/en]
   *   [ja][/ja]
   * @return {Promise}
   *   [en]Resolves to the selected index or to the new page element if there is a connected tabbar.[/en]
   *   [ja][/ja]
   */
  setActiveButton(index, options) {
    if (this._tabbar) {
      return this._tabbar.setActiveTab(index, options);
    }
    this._setChecked(index);
    this._postChange(index);
    return Promise.resolve(index);
  }
  /**
   * @method getActiveButtonIndex
   * @signature getActiveButtonIndex()
   * @return {Number}
   *   [en]The index of the currently active button.[/en]
   *   [ja][/ja]
   * @description
   *   [en]Returns button index of current active button. If active button is not found, returns -1.[/en]
   *   [ja][/ja]
   */
  getActiveButtonIndex() {
    for (let i = this.children.length - 1; i >= 0; i--) {
      if (this.children[i].firstElementChild.checked) {
        return i;
      }
    }
    return -1;
  }
  _onChange(event) {
    event.stopPropagation();
    this._tabbar ? this._tabbar.setActiveTab(this.getActiveButtonIndex(), { reject: false }) : this._postChange(this.getActiveButtonIndex());
  }
  _onTabbarPreChange(event) {
    setImmediate(() => {
      if (!event.detail.canceled) {
        this._setChecked(event.index);
        this._postChange(event.index);
      }
    });
  }
  _postChange(index) {
    util_default.triggerElementEvent(this, "postchange", {
      index,
      activeIndex: index,
      lastActiveIndex: this._lastActiveIndex,
      segmentItem: this.children[index]
    });
    this._lastActiveIndex = index;
    this.activeIndex = index;
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the segment is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  get activeIndex() {
    return parseInt(this.getAttribute("active-index"));
  }
  set activeIndex(value) {
    if (value !== null && value !== void 0) {
      this.setAttribute("active-index", value);
    }
  }
  static get observedAttributes() {
    return ["class", "modifier", "active-index"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, defaultClassName17, scheme31);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme31);
        break;
      case "active-index":
        contentReady(this, () => {
          if (this.getActiveButtonIndex() !== this.activeIndex) {
            this.setActiveButton(this.activeIndex);
          }
        });
        break;
    }
  }
  static get events() {
    return ["postchange"];
  }
};
util_default.defineBooleanProperties(SegmentElement, ["disabled"]);
elements_default.Segment = SegmentElement;
customElements.define("ons-segment", SegmentElement);

// node_modules/vue-onsenui/esm/components/VOnsSegment.js
var name41 = "v-ons-segment";
var script45 = {
  name: name41,
  mixins: [deriveEvents(name41)],
  emits: ["update:activeIndex"]
};
function render46(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-segment", {
    onPostchange: _cache[0] || (_cache[0] = withModifiers(($event) => _ctx.$emit("update:activeIndex", $event.index), ["self"]))
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 32);
}
script45.render = render46;

// node_modules/onsenui/esm/elements/ons-pull-hook.js
var STATE_INITIAL = "initial";
var STATE_PREACTION = "preaction";
var STATE_ACTION = "action";
var throwType = (el, type) => util_default.throw(`"${el}" must be ${type}`);
var PullHookElement = class extends BaseElement {
  /**
   * @event changestate
   * @description
   *   [en]Fired when the state is changed. The state can be either "initial", "preaction" or "action".[/en]
   *   [ja]コンポーネントの状態が変わった場合に発火します。状態は、"initial", "preaction", "action"のいずれかです。[/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Object} event.pullHook
   *   [en]Component object.[/en]
   *   [ja]コンポーネントのオブジェクト。[/ja]
   * @param {String} event.state
   *   [en]Current state.[/en]
   *   [ja]現在の状態名を参照できます。[/ja]
   */
  /**
   * @event pull
   * @description
   *   [en]Fired when the pull hook is pulled.[/en]
   *   [ja][/ja]
   * @param {Object} event
   *   [en]Event object.[/en]
   *   [ja]イベントオブジェクト。[/ja]
   * @param {Object} event.ratio
   *   [en]The pulled distance ratio (scroll / height).[/en]
   *   [ja][/ja]
   * @param {String} event.animationOptions
   *   [en]The animation options object.[/en]
   *   [ja][/ja]
   */
  /**
   * @attribute disabled
   * @description
   *   [en]If this attribute is set the "pull-to-refresh" functionality is disabled.[/en]
   *   [ja]この属性がある時、disabled状態になりアクションが実行されなくなります[/ja]
   */
  /**
   * @attribute height
   * @type {String}
   * @description
   *   [en]Specify the height of the component. When pulled down further than this value it will switch to the "preaction" state. The default value is "64px".[/en]
   *   [ja]コンポーネントの高さを指定します。この高さ以上にpull downすると"preaction"状態に移行します。デフォルトの値は"64px"です。[/ja]
   */
  /**
   * @attribute threshold-height
   * @type {String}
   * @description
   *   [en]Specify the threshold height. The component automatically switches to the "action" state when pulled further than this value. The default value is "96px". A negative value will disable this property. If this value is lower than the height, it will skip "preaction" state.[/en]
   *   [ja]閾値となる高さを指定します。この値で指定した高さよりもpull downすると、このコンポーネントは自動的に"action"状態に移行します。[/ja]
   */
  /**
   * @attribute fixed-content
   * @description
   *   [en]If this attribute is set the content of the page will not move when pulling.[/en]
   *   [ja]この属性がある時、プルフックが引き出されている時にもコンテンツは動きません。[/ja]
   */
  /**
   * @property fixedContent
   * @type {Boolean}
   * @description
   *   [en]If this property is set the content of the page will not move when pulling.[/en]
   *   [ja]この属性がある時、プルフックが引き出されている時にもコンテンツは動きません。[/ja]
   */
  constructor() {
    super();
    this._onDrag = this._onDrag.bind(this);
    this._onDragStart = this._onDragStart.bind(this);
    this._onDragEnd = this._onDragEnd.bind(this);
    this._onScroll = this._onScroll.bind(this);
    this._setState(STATE_INITIAL, true);
    this._hide();
    const { onConnected, onDisconnected } = util_default.defineListenerProperty(this, "pull");
    this._connectOnPull = onConnected;
    this._disconnectOnPull = onDisconnected;
  }
  _setStyle() {
    const height = this.height + "px";
    styler_default(this, { height, lineHeight: height });
    this.style.display === "" && this._show();
  }
  _onScroll(event) {
    const element = this._pageElement;
    if (element.scrollTop < 0) {
      element.scrollTop = 0;
    }
  }
  _canConsumeGesture(gesture) {
    return gesture.direction === "up" || gesture.direction === "down";
  }
  _onDragStart(event) {
    if (!event.gesture || this.disabled) {
      return;
    }
    const tapY = event.gesture.center.clientY + this._pageElement.scrollTop;
    const maxY = window.innerHeight;
    const draggableAreaRatio = 1;
    this._ignoreDrag = event.consumed || tapY > maxY * draggableAreaRatio;
    if (!this._ignoreDrag) {
      const consume = event.consume;
      event.consume = () => {
        consume && consume();
        this._ignoreDrag = true;
        this._hide();
      };
      if (this._canConsumeGesture(event.gesture)) {
        consume && consume();
        event.consumed = true;
        this._show();
      }
    }
    this._startScroll = this._pageElement.scrollTop;
  }
  _onDrag(event) {
    if (!event.gesture || this.disabled || this._ignoreDrag || !this._canConsumeGesture(event.gesture)) {
      return;
    }
    if (this.style.display === "none") {
      this._show();
    }
    event.stopPropagation();
    const tapY = event.gesture.center.clientY + this._pageElement.scrollTop;
    const maxY = window.innerHeight;
    const scroll = Math.max(event.gesture.deltaY - this._startScroll, 0);
    if (scroll !== this._currentTranslation) {
      const th = this.thresholdHeight;
      if (th > 0 && scroll >= th) {
        event.gesture.stopDetect();
        setImmediate(() => this._finish());
      } else if (scroll >= this.height) {
        this._setState(STATE_PREACTION);
      } else {
        this._setState(STATE_INITIAL);
      }
      this._translateTo(scroll);
    }
  }
  _onDragEnd(event) {
    if (!event.gesture || this.disabled || this._ignoreDrag) {
      return;
    }
    event.stopPropagation();
    if (this._currentTranslation > 0) {
      const scroll = this._currentTranslation;
      if (scroll > this.height) {
        this._finish();
      } else {
        this._translateTo(0, { animate: true });
      }
    }
  }
  /**
   * @property onAction
   * @type {Function}
   * @description
   *   [en]This will be called in the `action` state if it exists. The function will be given a `done` callback as its first argument.[/en]
   *   [ja][/ja]
   */
  get onAction() {
    return this._onAction;
  }
  set onAction(value) {
    if (value && !(value instanceof Function)) {
      throwType("onAction", "function or null");
    }
    this._onAction = value;
  }
  /**
   * @property onPull
   * @type {Function}
   * @description
   *   [en]Hook called whenever the user pulls the element. It gets the pulled distance ratio (scroll / height) and an animationOptions object as arguments.[/en]
   *   [ja][/ja]
   */
  _finish() {
    this._setState(STATE_ACTION);
    this._translateTo(this.height, { animate: true });
    const action = this.onAction || ((done) => done());
    action(() => {
      this._translateTo(0, { animate: true });
      this._setState(STATE_INITIAL);
    });
  }
  /**
   * @property height
   * @type {Number}
   * @description
   *   [en]The height of the pull hook in pixels. The default value is `64px`.[/en]
   *   [ja][/ja]
   */
  set height(value) {
    if (!util_default.isInteger(value)) {
      throwType("height", "integer");
    }
    this.setAttribute("height", `${value}px`);
  }
  get height() {
    return parseInt(this.getAttribute("height") || "64", 10);
  }
  /**
   * @property thresholdHeight
   * @type {Number}
   * @description
   *   [en]The thresholdHeight of the pull hook in pixels. The default value is `96px`.[/en]
   *   [ja][/ja]
   */
  set thresholdHeight(value) {
    if (!util_default.isInteger(value)) {
      throwType("thresholdHeight", "integer");
    }
    this.setAttribute("threshold-height", `${value}px`);
  }
  get thresholdHeight() {
    return parseInt(this.getAttribute("threshold-height") || "96", 10);
  }
  _setState(state, noEvent) {
    const lastState = this.state;
    this.setAttribute("state", state);
    if (!noEvent && lastState !== this.state) {
      util_default.triggerElementEvent(this, "changestate", {
        pullHook: this,
        state,
        lastState
      });
    }
  }
  /**
   * @property state
   * @readonly
   * @type {String}
   * @description
   *   [en]Current state of the element.[/en]
   *   [ja][/ja]
   */
  get state() {
    return this.getAttribute("state");
  }
  /**
   * @property pullDistance
   * @readonly
   * @type {Number}
   * @description
   *   [en]The current number of pixels the pull hook has moved.[/en]
   *   [ja]現在のプルフックが引き出された距離をピクセル数。[/ja]
   */
  get pullDistance() {
    return this._currentTranslation;
  }
  /**
   * @property disabled
   * @type {Boolean}
   * @description
   *   [en]Whether the element is disabled or not.[/en]
   *   [ja]無効化されている場合に`true`。[/ja]
   */
  _show() {
    setImmediate(() => {
      this.style.display = "";
      if (this._pageElement) {
        this._pageElement.style.marginTop = `-${this.height}px`;
      }
    });
  }
  _hide() {
    this.style.display = "none";
    if (this._pageElement) {
      this._pageElement.style.marginTop = "";
    }
  }
  /**
   * @param {Number} scroll
   * @param {Object} options
   * @param {Function} [options.callback]
   */
  _translateTo(scroll, options = {}) {
    if (this._currentTranslation == 0 && scroll == 0) {
      return;
    }
    this._currentTranslation = scroll;
    const opt = options.animate ? { duration: 0.3, timing: "cubic-bezier(.1, .7, .1, 1)" } : {};
    util_default.triggerElementEvent(this, "pull", { ratio: (scroll / this.height).toFixed(2), animationOptions: opt });
    const scrollElement = this.hasAttribute("fixed-content") ? this : this._pageElement;
    animit_default(scrollElement).queue({ transform: `translate3d(0px, ${scroll}px, 0px)` }, opt).play(() => {
      scroll === 0 && styler_default.clear(scrollElement, "transition transform");
      options.callback instanceof Function && options.callback();
    });
  }
  _disableDragLock() {
    this._dragLockDisabled = true;
    this._setupListeners(true);
  }
  _setupListeners(add) {
    const scrollToggle = (action) => this._pageElement[`${action}EventListener`]("scroll", this._onScroll, false);
    const gdToggle = (action) => {
      const passive = { passive: true };
      this._gestureDetector[action]("drag", this._onDrag, passive);
      this._gestureDetector[action]("dragstart", this._onDragStart, passive);
      this._gestureDetector[action]("dragend", this._onDragEnd, passive);
    };
    if (this._gestureDetector) {
      gdToggle("off");
      this._gestureDetector.dispose();
      this._gestureDetector = null;
    }
    scrollToggle("remove");
    if (add) {
      this._gestureDetector = new gesture_detector_default(this._pageElement, {
        dragMinDistance: 1,
        dragDistanceCorrection: false,
        dragLockToAxis: !this._dragLockDisabled,
        passive: true
      });
      gdToggle("on");
      scrollToggle("add");
    }
  }
  connectedCallback() {
    this._currentTranslation = 0;
    this._pageElement = this.parentNode;
    this._setupListeners(true);
    this._setStyle();
    this._connectOnPull();
  }
  disconnectedCallback() {
    this._hide();
    this._setupListeners(false);
    this._disconnectOnPull();
  }
  static get observedAttributes() {
    return ["height"];
  }
  attributeChangedCallback(name44, last, current) {
    if (name44 === "height" && this._pageElement) {
      this._setStyle();
    }
  }
  static get events() {
    return ["changestate", "pull"];
  }
};
util_default.defineBooleanProperties(PullHookElement, ["disabled", "fixed-content"]);
elements_default.PullHook = PullHookElement;
customElements.define("ons-pull-hook", PullHookElement);

// node_modules/vue-onsenui/esm/components/VOnsPullHook.js
var name42 = "v-ons-pull-hook";
var script46 = {
  name: name42,
  mixins: [deriveEvents(name42)],
  props: {
    action: {
      type: Function
    }
  }
};
var _hoisted_134 = [".onAction"];
function render47(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-pull-hook", { ".onAction": $props.action }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_134);
}
script46.render = render47;

// node_modules/onsenui/esm/elements/ons-page.js
var defaultClassName18 = "page";
var scheme32 = {
  "": "page--*",
  ".page__content": "page--*__content",
  ".page__background": "page--*__background"
};
var PageElement = class extends BaseElement {
  /**
   * @event init
   * @description
   *   [en]Fired right after the page is attached.[/en]
   *   [ja]ページがアタッチされた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   */
  /**
   * @event show
   * @description
   *   [en]Fired right after the page is shown.[/en]
   *   [ja]ページが表示された後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   */
  /**
   * @event hide
   * @description
   *   [en]Fired right after the page is hidden.[/en]
   *   [ja]ページが隠れた後に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   */
  /**
   * @event destroy
   * @description
   *   [en]Fired right before the page is destroyed.[/en]
   *   [ja]ページが破棄される前に発火します。[/ja]
   * @param {Object} event [en]Event object.[/en]
   */
  /**
   * @attribute modifier
   * @type {String}
   * @description
   *   [en]Specify modifier name to specify custom styles.[/en]
   *   [ja]スタイル定義をカスタマイズするための名前を指定します。[/ja]
   */
  /**
   * @attribute on-infinite-scroll
   * @type {String}
   * @description
   *   [en]Path of the function to be executed on infinite scrolling. Example: `app.loadData`. The function receives a done callback that must be called when it's finished.[/en]
   *   [ja][/ja]
   */
  constructor() {
    super();
    this._deriveHooks();
    this._defaultClassName = defaultClassName18;
    this.classList.add(defaultClassName18);
    this._initialized = false;
    contentReady(this, () => {
      this._compile();
      this._isShown = false;
      this._contentElement = this._getContentElement();
      this._backgroundElement = this._getBackgroundElement();
    });
  }
  _compile() {
    autostyle_default.prepare(this);
    const toolbar = util_default.findChild(this, "ons-toolbar");
    const background = util_default.findChild(this, ".page__background") || util_default.findChild(this, ".background") || document.createElement("div");
    background.classList.add("page__background");
    this.insertBefore(background, !toolbar && this.firstChild || toolbar && toolbar.nextSibling);
    const content = util_default.findChild(this, ".page__content") || util_default.findChild(this, ".content") || document.createElement("div");
    content.classList.add("page__content");
    if (!content.parentElement) {
      util_default.arrayFrom(this.childNodes).forEach((node) => {
        if (node.nodeType !== 1 || this._elementShouldBeMoved(node)) {
          content.appendChild(node);
        }
      });
    }
    this._tryToFillStatusBar(content);
    this.insertBefore(content, background.nextSibling);
    if ((!toolbar || !util_default.hasModifier(toolbar, "transparent")) && content.children.length === 1 && util_default.isPageControl(content.children[0])) {
      this._defaultClassName += " page--wrapper";
      this.attributeChangedCallback("class");
    }
    const bottomToolbar = util_default.findChild(this, "ons-bottom-toolbar");
    if (bottomToolbar) {
      this._defaultClassName += " page-with-bottom-toolbar";
      this.attributeChangedCallback("class");
    }
    ModifierUtil.initModifier(this, scheme32);
  }
  _elementShouldBeMoved(el) {
    if (el.classList.contains("page__background")) {
      return false;
    }
    const tagName = el.tagName.toLowerCase();
    if (tagName === "ons-fab") {
      return !el.hasAttribute("position");
    }
    const fixedElements = ["script", "ons-toolbar", "ons-bottom-toolbar", "ons-modal", "ons-speed-dial", "ons-dialog", "ons-alert-dialog", "ons-popover", "ons-action-sheet"];
    return el.hasAttribute("inline") || fixedElements.indexOf(tagName) === -1;
  }
  _tryToFillStatusBar(content = this._contentElement) {
    internal_default.autoStatusBarFill(() => {
      util_default.toggleAttribute(
        this,
        "status-bar-fill",
        !util_default.findParent(this, (e) => e.hasAttribute("status-bar-fill")) && (this._canAnimateToolbar(content) || !util_default.findChild(content, util_default.isPageControl))
        // Has toolbar or cannot delegate
      );
    });
  }
  _canAnimateToolbar(content = this._contentElement) {
    if (util_default.findChild(this, "ons-toolbar")) {
      return true;
    }
    return !!util_default.findChild(content, (el) => {
      return util_default.match(el, "ons-toolbar") && !el.hasAttribute("inline");
    });
  }
  connectedCallback() {
    if (!util_default.isAttached(this)) {
      return;
    }
    contentReady(this, () => {
      this._tryToFillStatusBar();
      if (this.hasAttribute("on-infinite-scroll")) {
        this.attributeChangedCallback("on-infinite-scroll", null, this.getAttribute("on-infinite-scroll"));
      }
      if (!this._initialized) {
        this._initialized = true;
        setImmediate(() => {
          this.onInit && this.onInit();
          util_default.triggerElementEvent(this, "init");
        });
        if (!util_default.hasAnyComponentAsParent(this)) {
          setImmediate(() => this._show());
        }
      }
    });
  }
  updateBackButton(show) {
    if (this.backButton) {
      show ? this.backButton.show() : this.backButton.hide();
    }
  }
  set name(str) {
    this.setAttribute("name", str);
  }
  get name() {
    return this.getAttribute("name");
  }
  get backButton() {
    return this.querySelector("ons-back-button");
  }
  /**
   * @property onInfiniteScroll
   * @description
   *  [en]Function to be executed when scrolling to the bottom of the page. The function receives a done callback as an argument that must be called when it's finished.[/en]
   *  [ja][/ja]
   */
  set onInfiniteScroll(value) {
    if (value && !(value instanceof Function)) {
      util_default.throw('"onInfiniteScroll" must be function or null');
    }
    contentReady(this, () => {
      if (!value) {
        this._contentElement.removeEventListener("scroll", this._boundOnScroll);
      } else if (!this._onInfiniteScroll) {
        this._infiniteScrollLimit = 0.9;
        this._boundOnScroll = this._onScroll.bind(this);
        setImmediate(() => this._contentElement.addEventListener("scroll", this._boundOnScroll));
      }
      this._onInfiniteScroll = value;
    });
  }
  get onInfiniteScroll() {
    return this._onInfiniteScroll;
  }
  _onScroll() {
    const c = this._contentElement, overLimit = (c.scrollTop + c.clientHeight) / c.scrollHeight >= this._infiniteScrollLimit;
    if (this._onInfiniteScroll && !this._loadingContent && overLimit) {
      this._loadingContent = true;
      this._onInfiniteScroll(() => this._loadingContent = false);
    }
  }
  /**
   * @property onDeviceBackButton
   * @type {Object}
   * @description
   *   [en]Back-button handler.[/en]
   *   [ja]バックボタンハンドラ。[/ja]
   */
  get onDeviceBackButton() {
    return this._backButtonHandler;
  }
  set onDeviceBackButton(callback) {
    if (this._backButtonHandler) {
      this._backButtonHandler.destroy();
    }
    this._backButtonHandler = device_back_button_dispatcher_default.createHandler(this, callback);
  }
  get scrollTop() {
    return this._contentElement.scrollTop;
  }
  set scrollTop(newValue) {
    this._contentElement.scrollTop = newValue;
  }
  _getContentElement() {
    const result = util_default.findChild(this, ".page__content");
    if (result) {
      return result;
    }
    util_default.throw('Fail to get ".page__content" element');
  }
  _getBackgroundElement() {
    const result = util_default.findChild(this, ".page__background");
    if (result) {
      return result;
    }
    util_default.throw('Fail to get ".page__background" element');
  }
  _getBottomToolbarElement() {
    return util_default.findChild(this, "ons-bottom-toolbar") || internal_default.nullElement;
  }
  _getToolbarElement() {
    return util_default.findChild(this, "ons-toolbar") || document.createElement("ons-toolbar");
  }
  static get observedAttributes() {
    return ["modifier", "on-infinite-scroll", "class"];
  }
  attributeChangedCallback(name44, last, current) {
    switch (name44) {
      case "class":
        util_default.restoreClass(this, this._defaultClassName, scheme32);
        break;
      case "modifier":
        ModifierUtil.onModifierChanged(last, current, this, scheme32);
        break;
      case "on-infinite-scroll":
        if (current === null) {
          this.onInfiniteScroll = null;
        } else {
          this.onInfiniteScroll = (done) => {
            const f = util_default.findFromPath(current);
            this.onInfiniteScroll = f;
            f(done);
          };
        }
        break;
    }
  }
  _show() {
    if (!this._isShown && util_default.isAttached(this)) {
      this._isShown = true;
      this.setAttribute("shown", "");
      this.onShow && this.onShow();
      util_default.triggerElementEvent(this, "show");
      util_default.propagateAction(this, "_show");
    }
  }
  _hide() {
    if (this._isShown) {
      this._isShown = false;
      this.removeAttribute("shown");
      this.onHide && this.onHide();
      util_default.triggerElementEvent(this, "hide");
      util_default.propagateAction(this, "_hide");
    }
  }
  _destroy() {
    this._hide();
    this.onDestroy && this.onDestroy();
    util_default.triggerElementEvent(this, "destroy");
    if (this.onDeviceBackButton) {
      this.onDeviceBackButton.destroy();
    }
    util_default.propagateAction(this, "_destroy");
    this.remove();
  }
  _deriveHooks() {
    this.constructor.events.forEach((event) => {
      const key = "on" + event.charAt(0).toUpperCase() + event.slice(1);
      Object.defineProperty(this, key, {
        configurable: true,
        enumerable: true,
        get: () => this[`_${key}`],
        set: (value) => {
          if (!(value instanceof Function)) {
            util_default.throw(`"${key}" hook must be a function`);
          }
          this[`_${key}`] = value.bind(this);
        }
      });
    });
  }
  static get events() {
    return ["init", "show", "hide", "destroy"];
  }
  /**
   * @property data
   * @type {*}
   * @description
   *   [en]User's custom data passed to `pushPage()`-like methods.[/en]
   *   [ja][/ja]
   */
};
elements_default.Page = PageElement;
customElements.define("ons-page", PageElement);

// node_modules/vue-onsenui/esm/components/VOnsPage.js
var name43 = "v-ons-page";
var script47 = {
  name: name43,
  mixins: [deriveEvents(name43), deriveDBB, modifier],
  props: {
    infiniteScroll: {
      type: Function
    }
  }
};
var _hoisted_135 = [".onInfiniteScroll", "modifier"];
function render48(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("ons-page", {
    ".onInfiniteScroll": $props.infiniteScroll,
    modifier: _ctx.normalizedModifier
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_135);
}
script47.render = render48;
export {
  script30 as VOnsActionSheet,
  script31 as VOnsActionSheetButton,
  script35 as VOnsAlertDialog,
  script4 as VOnsAlertDialogButton,
  script40 as VOnsBackButton,
  script2 as VOnsBottomToolbar,
  script5 as VOnsButton,
  script7 as VOnsCard,
  script37 as VOnsCarousel,
  script17 as VOnsCarouselItem,
  script22 as VOnsCheckbox,
  script14 as VOnsCol,
  script29 as VOnsDialog,
  script27 as VOnsFab,
  script6 as VOnsIcon,
  script23 as VOnsInput,
  script43 as VOnsLazyRepeat,
  script8 as VOnsList,
  script11 as VOnsListHeader,
  script9 as VOnsListItem,
  script10 as VOnsListTitle,
  script32 as VOnsModal,
  script41 as VOnsNavigator,
  script47 as VOnsPage,
  script34 as VOnsPopover,
  script15 as VOnsProgressBar,
  script16 as VOnsProgressCircular,
  script46 as VOnsPullHook,
  script26 as VOnsRadio,
  script25 as VOnsRange,
  script12 as VOnsRipple,
  script13 as VOnsRow,
  script24 as VOnsSearchInput,
  script45 as VOnsSegment,
  script44 as VOnsSelect,
  script36 as VOnsSpeedDial,
  script28 as VOnsSpeedDialItem,
  script20 as VOnsSplitter,
  script19 as VOnsSplitterContent,
  script18 as VOnsSplitterMask,
  script42 as VOnsSplitterSide,
  script21 as VOnsSwitch,
  script38 as VOnsTab,
  script39 as VOnsTabbar,
  script33 as VOnsToast,
  script as VOnsToolbar,
  script3 as VOnsToolbarButton
};
//# sourceMappingURL=vue-onsenui_esm_components.js.map
