import 'onsenui/esm/elements/ons-list-title.js';
import { deriveEvents, modifier } from '../mixins/index.js';
import { openBlock, createElementBlock, renderSlot } from 'vue';

const name = 'v-ons-list-title';

  var script = {
    name,
    mixins: [deriveEvents(name), modifier]
  };

const _hoisted_1 = ["modifier"];

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return (openBlock(), createElementBlock("ons-list-title", { modifier: _ctx.normalizedModifier }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_1))
}

script.render = render;

export { script as default };
