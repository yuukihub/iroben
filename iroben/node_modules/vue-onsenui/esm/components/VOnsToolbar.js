import 'onsenui/esm/elements/ons-toolbar.js';
import { deriveEvents, modifier } from '../mixins/index.js';
import { openBlock, createElementBlock, renderSlot } from 'vue';

const name = 'v-ons-toolbar';

  var script = {
    name,
    mixins: [deriveEvents(name), modifier],
    props: {
      inline: Boolean
    }
  };

const _hoisted_1 = ["modifier", "inline"];

function render(_ctx, _cache, $props, $setup, $data, $options) {
  return (openBlock(), createElementBlock("ons-toolbar", {
    modifier: _ctx.normalizedModifier,
    inline: $props.inline || null
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 8, _hoisted_1))
}

script.render = render;

export { script as default };
